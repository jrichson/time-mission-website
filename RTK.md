# Repository Notes

Public-site changes should preserve the design context in `.impeccable.md` and the domain vocabulary in `CONTEXT.md`.

Use `npm run check` for source-level contracts, `npm run build:astro` before generated-output checks, and `npm run test:smoke` for browser behavior. Preview-dependent checks need a local Astro server on `127.0.0.1`.
