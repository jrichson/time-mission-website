---
phase: 05-booking-cta-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - js/ticket-panel.js
  - scripts/check-booking-architecture.js
autonomous: true
requirements:
  - BOOK-01
  - BOOK-02
  - BOOK-04
must_haves:
  truths:
    - "Open-location checkout intent resolves `trim(rollerCheckoutUrl)` first when non-empty, else `trim(bookingUrl)` (D-03)."
    - "Coming-soon locations never receive an external checkout URL from `getBookingUrl`; they receive the internal clean location path `/slug` (BOOK-02, D-07)."
    - "Location-page `?book=1` auto-redirect uses the same resolver output as `getBookingUrl(pageLocation)` after `window.TM.ready` when TM is present (BOOK-04, D-05)."
  artifacts:
    - path: "js/ticket-panel.js"
      provides: "resolveCheckoutUrl + getBookingUrl behavior exported via window.TMTicketPanel.getBookingUrl"
    - path: "scripts/check-booking-architecture.js"
      provides: "Static asserts that resolver precedence exists and TM.ready guard remains"
  key_links:
    - from: "js/ticket-panel.js"
      to: "data/locations.json via window.TM"
      via: "getLocation(id)"
      pattern: "window.TM.get"
---

<objective>
Implement **validated outbound checkout URL resolution** for open locations inside `js/ticket-panel.js`: **`rollerCheckoutUrl` first, then `bookingUrl`** (per D-03). Preserve **coming-soon** internal routing (BOOK-02). Ensure **`scheduleAutoRedirect`** for `?book=1` uses the updated resolver so external destinations remain correct under clean URLs (BOOK-04).

Purpose: BOOK-01 core — every path that computed checkout from `bookingUrl` alone must now honor Roller URLs when present.

Output: Modified `ticket-panel.js` + extended `check-booking-architecture.js`. Do **not** change `#ticketBookBtn` click semantics beyond href composition already driven by `getBookingUrl` / location-page rules.
</objective>

<threat_model>
Static marketing site — outbound URLs come from validated JSON (`data/locations.json`). Phase risk is **wrong-handoff URL** (integrity) rather than XSS.

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05-01 | Tampering | locations.json | accept | Existing `check-location-contracts.js` HTTPS/mailto rules; executor must not broaden URL schemes |
| T-05-02 | Spoofing / wrong checkout | ticket-panel.js | mitigate | Resolver reads only `window.TM.get`; no `eval`; precedence documented in source comments |
| T-05-03 | Information disclosure | ticket-panel.js | accept | No new logging of URLs beyond existing patterns |

</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/05-booking-cta-flow/05-CONTEXT.md
@.planning/phases/05-booking-cta-flow/05-RESEARCH.md
@.planning/phases/05-booking-cta-flow/05-UI-SPEC.md
@.planning/phases/05-booking-cta-flow/05-VALIDATION.md
@js/ticket-panel.js
@scripts/check-booking-architecture.js
@js/locations.js
@data/locations.json

<interfaces>
`window.TMTicketPanel.getBookingUrl(id)` must remain callable after refactor — signature unchanged (`id` string/slug).

Location objects expose at minimum: `id`, `slug`, `status`, `bookingUrl`, optional `rollerCheckoutUrl`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolver + getBookingUrl refactor in ticket-panel.js</name>
  <files>js/ticket-panel.js</files>
  <read_first>
    <file>js/ticket-panel.js</file>
    <file>.planning/phases/05-booking-cta-flow/05-CONTEXT.md</file>
    <file>data/locations.json</file>
  </read_first>
  <action>
Implement per **D-03**:

1. Add an internal function **`resolveOpenCheckoutUrl(loc)`** immediately after `getLocationPage` (or equivalent placement before first use):
   - If `!loc` return `''`.
   - If `loc.status === 'coming-soon'` return `''` (caller handles coming-soon).
   - `var roller = (loc.rollerCheckoutUrl && String(loc.rollerCheckoutUrl).trim()) || '';`
   - If `roller !== ''` return `roller`.
   - `var book = (loc.bookingUrl && String(loc.bookingUrl).trim()) || '';`
   - Return `book`.

2. Replace **`getBookingUrl`** body so:
   - `var loc = getLocation(id);`
   - If `!loc` return `''`.
   - If `loc.status === 'coming-soon'` return **`getLocationPage(loc.id)`** unchanged (BOOK-02).
   - Else return **`resolveOpenCheckoutUrl(loc)`**.

3. Do **not** change **`syncBookingBtn`** routing rules for index (`page + '?book=1'`) vs location pages — only the leaf `getBookingUrl(selected)` branch uses the new resolver.

4. **`scheduleAutoRedirect`** already calls `getBookingUrl(pageLocation)` — must continue to work with **mailto:** and **https:** results without stripping schemes.

5. Add a **one-line comment** above `resolveOpenCheckoutUrl`: `// Phase 5 BOOK-01 / D-03: rollerCheckoutUrl precedes bookingUrl for open venues.`

  </action>
  <verify>
    <automated>node scripts/check-booking-architecture.js &amp;&amp; node scripts/check-location-contracts.js</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`grep -q 'resolveOpenCheckoutUrl' js/ticket-panel.js` succeeds.</criterion>
    <criterion>`grep -q 'rollerCheckoutUrl' js/ticket-panel.js` succeeds.</criterion>
    <criterion>`awk '/function getBookingUrl/,/^    }/' js/ticket-panel.js | grep -q "coming-soon"` succeeds (coming-soon branch preserved).</criterion>
    <criterion>`grep -q 'window.TM.ready' js/ticket-panel.js` succeeds.</criterion>
    <criterion>`node scripts/check-booking-architecture.js` exits 0.</criterion>
    <criterion>`node scripts/check-location-contracts.js` exits 0.</criterion>
  </acceptance_criteria>
  <done>Checkout resolver honors Roller-first precedence; coming-soon unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Extend check-booking-architecture.js for resolver contract</name>
  <files>scripts/check-booking-architecture.js</files>
  <read_first>
    <file>scripts/check-booking-architecture.js</file>
    <file>js/ticket-panel.js</file>
  </read_first>
  <action>
Append **non-duplicative** assertions after existing reads:

1. If `ticketPanel` source does **not** contain the substring **`resolveOpenCheckoutUrl`**, push error: `'js/ticket-panel.js must define resolveOpenCheckoutUrl for Phase 5 checkout precedence'`.

2. If `ticketPanel` source does **not** contain **`loc.rollerCheckoutUrl`** before **`loc.bookingUrl`** inside the function body of **`resolveOpenCheckoutUrl`** (simple heuristic: extract lines between `function resolveOpenCheckoutUrl` and its closing `}` at same indent — if fragile, fallback: assert file contains regex `/rollerCheckoutUrl[\s\S]{0,400}bookingUrl/` via JS `ticketPanel.match(/rollerCheckoutUrl[\s\S]{0,800}bookingUrl/)`), otherwise error `'resolveOpenCheckoutUrl must test rollerCheckoutUrl before bookingUrl'`.

Keep existing TM.ready and roller-checkout assertions **unchanged** in this task (Plan 05-02 updates roller-checkout expectations).

  </action>
  <verify>
    <automated>node scripts/check-booking-architecture.js</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`node scripts/check-booking-architecture.js` exits 0 after changes.</criterion>
    <criterion>The script file contains the literal substring `resolveOpenCheckoutUrl` inside an error message or comment tied to the new check.</criterion>
  </acceptance_criteria>
  <done>Static gate encforces resolver presence and Roller-first heuristic.</done>
</task>

</tasks>

<verification>
- `npm run check:booking` exits 0.
- `npm run check:locations` exits 0.
- `npm run test:smoke` still passes (href expectations unchanged for homepage funnel).
</verification>

<success_criteria>
- D-03 satisfied in runtime code.
- BOOK-02 / BOOK-04 behaviors preserved by inspection of `getBookingUrl` + `scheduleAutoRedirect`.
</success_criteria>

<output>
Create `.planning/phases/05-booking-cta-flow/05-01-SUMMARY.md` summarizing resolver addition and any manual spot-check notes (e.g. Philadelphia Roller vs tickets.timemission.com precedence).
</output>
