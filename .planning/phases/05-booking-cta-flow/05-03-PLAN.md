---
phase: 05-booking-cta-flow
plan: 03
type: execute
wave: 3
depends_on:
  - "05-02-PLAN.md"
files_modified:
  - js/ticket-panel.js
  - docs/booking-cta-surface-matrix.md
autonomous: true
requirements:
  - BOOK-01
  - BOOK-02
must_haves:
  truths:
    - "`bookOrOpenPanel` navigates **same-tab** for `https:` booking URLs via **`window.location.assign(href)`** (or equivalent `href` navigation), not `window.open(..., '_blank')` — D-01 default."
    - "**mailto:** and **tel:** handling remains same-tab (`window.location.href = href`)."
    - "Operator-facing **CTA matrix** documents ticket panel, hero/nav booking buttons, location overlay Book Now, and location-page inline Book CTAs — D-04."
  artifacts:
    - path: "docs/booking-cta-surface-matrix.md"
      provides: "Same-tab vs native `<a>` defaults per surface"
    - path: "js/ticket-panel.js"
      provides: "Same-tab global booking intercept"
  key_links:
    - from: "js/ticket-panel.js"
      to: "document.querySelectorAll('.btn-tickets"
      via: "bookOrOpenPanel listeners"
      pattern: "bookOrOpenPanel"
---

<objective>
Reconcile **global booking button interception** with **same-tab external checkout default** (D-01): replace `window.open(href, '_blank')` for `https:` URLs in **`bookOrOpenPanel`** with **`window.location.assign(href)`** after `preventDefault`.

Author **`docs/booking-cta-surface-matrix.md`** documenting per-surface behavior — satisfies D-04 traceability without ambiguity for QA.

Purpose: BOOK-01 handoff consistency across surfaces that flow through `bookOrOpenPanel`; BOOK-02 unchanged because coming-soon paths do not produce direct `https:` booking URLs from this handler.

Output: Patched `ticket-panel.js` + new docs file (≤80 lines).
</objective>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05-07 | Tampering / tab-nabbing | ticket-panel.js | mitigate | Same-tab navigation avoids `_blank` without `noopener` pairs on programmatic opens |
| T-05-08 | Open redirect | ticket-panel.js | accept | URLs originate only from authored `href` attributes + TM-backed helpers — unchanged trust model |

</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-booking-cta-flow/05-CONTEXT.md
@.planning/phases/05-booking-cta-flow/05-UI-SPEC.md
@js/ticket-panel.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Same-tab https navigation in bookOrOpenPanel</name>
  <files>js/ticket-panel.js</files>
  <read_first>
    <file>js/ticket-panel.js</file>
    <file>.planning/phases/05-booking-cta-flow/05-CONTEXT.md</file>
  </read_first>
  <action>
Locate **`function bookOrOpenPanel(e)`**.

Inside the branch where **`isDirectBookingUrl(href)`** is true and **`href` is NOT** mailto/tel:

**Replace:**
```javascript
window.open(href, '_blank');
```

**With:**
```javascript
window.location.assign(href);
```

Leave mailto/tel branch using **`window.location.href = href`** unchanged.

Add inline comment: `// D-01: same-tab default for external checkout (Phase 5).`

Verify **`#ticketBookBtn`** dedicated listener remains **`window.location.href = url`** — do not regress.

  </action>
  <verify>
    <automated>node scripts/check-booking-architecture.js</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`grep -q 'window.location.assign' js/ticket-panel.js` succeeds.</criterion>
    <criterion>`grep 'window.open' js/ticket-panel.js` returns exit code **1** (no `window.open` remains anywhere in file).</criterion>
    <criterion>`node scripts/check-booking-architecture.js` exits 0.</criterion>
  </acceptance_criteria>
  <done>Global booking CTAs use same-tab assignment for https URLs.</done>
</task>

<task type="auto">
  <name>Task 2: Document booking CTA surface matrix</name>
  <files>docs/booking-cta-surface-matrix.md</files>
  <read_first>
    <file>js/ticket-panel.js</file>
    <file>js/nav.js</file>
    <file>.planning/phases/05-booking-cta-flow/05-CONTEXT.md</file>
  </read_first>
  <action>
Create **`docs/booking-cta-surface-matrix.md`** with markdown sections:

1. **Purpose** — Phase 5 operator reference for BOOK-01/D-04.

2. **Table** with columns **Surface**, **Selector / element**, **Behavior**, **Same-tab?**
   - Row: Hero primary tickets — `.hero-cta .btn-tickets` — `bookOrOpenPanel` — Yes (`assign`)
   - Row: Nav / mirrored booking buttons matching ticket-panel selectors — `.btn-tickets`, `.btn-book-now`, roller/tickets href buttons — `bookOrOpenPanel` — Yes
   - Row: Ticket panel Continue — `#ticketBookBtn` — dedicated listener — Yes (`location.href`)
   - Row: Location overlay detail Book Now — `.location-info-book` — native `<a>` updates from nav.js (`href = pageUrl + '?book=1'` open venues) — Yes (no `_blank`)
   - Row: Location page inline Book CTAs — authored anchors — native navigation / ticket-panel rules — Yes unless legacy markup adds `target` (flag any exceptions found during audit)

3. **Coming-soon** note — overlay sets Sign Up / internal path — no external checkout.

Keep file **under 120 lines**.

  </action>
  <verify>
    <automated>test -f docs/booking-cta-surface-matrix.md &amp;&amp; wc -l docs/booking-cta-surface-matrix.md | awk '{exit !($1 &lt;= 120)}'</automated>
  </verify>
  <acceptance_criteria>
    <criterion>File exists at `docs/booking-cta-surface-matrix.md`.</criterion>
    <criterion>`grep -q 'bookOrOpenPanel' docs/booking-cta-surface-matrix.md` succeeds.</criterion>
    <criterion>`grep -q 'ticketBookBtn' docs/booking-cta-surface-matrix.md` succeeds.</criterion>
    <criterion>`grep -qi 'coming-soon' docs/booking-cta-surface-matrix.md` succeeds.</criterion>
  </acceptance_criteria>
  <done>D-04 matrix published for operators.</done>
</task>

</tasks>

<verification>
- `npm run check:booking` exits 0.
- `npm run test:smoke` exits 0.
</verification>

<success_criteria>
- No `window.open` left in ticket-panel.js for booking flows.
- Operator matrix matches implemented handlers.
</success_criteria>

<output>
Append `.planning/phases/05-booking-cta-flow/05-03-SUMMARY.md` confirming grep for `window.open` across `js/` booking-related files (executor notes any remaining intentional uses outside ticket-panel).
</output>
