---
phase: 05-booking-cta-flow
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01-PLAN.md"
files_modified:
  - js/roller-checkout.js
  - scripts/check-booking-architecture.js
autonomous: true
requirements:
  - BOOK-03
must_haves:
  truths:
    - "Default pages do not inject `https://cdn.rollerdigital.com/scripts/widget/checkout_iframe.js` or call `RollerCheckout.show()` on ordinary navigation (BOOK-03, D-02)."
    - "`js/roller-checkout.js` remains loadable from legacy HTML and Astro `SiteScripts.astro` without 404 — implemented as documented no-op stub unless PLAN follow-up removes script tags globally."
  artifacts:
    - path: "js/roller-checkout.js"
      provides: "No-op stub + BOOK-03 documentation header"
    - path: "scripts/check-booking-architecture.js"
      provides: "Asserts iframe CDN absent from roller-checkout.js"
  key_links:
    - from: "src/components/SiteScripts.astro"
      to: "js/roller-checkout.js"
      via: "Optional follow-up: script tag may remain for load-order compatibility"
      pattern: "roller-checkout.js"
---

<objective>
Remove the **ROLLER progressive-checkout iframe** from the **default** booking path (BOOK-03, D-02): eliminate CDN script injection and capture-phase interception from `js/roller-checkout.js`. Replace with a **documented no-op stub** so existing `<script src="...roller-checkout.js">` references keep working until a bulk HTML cleanup is explicitly scoped.

Purpose: Default customer journey uses **external checkout URLs** from Phase 05-01 resolver — not overlay iframe.

Output: Rewritten `roller-checkout.js`, updated `check-booking-architecture.js` removing stale requirements that mandated Roller URL handling inside this file (replace with **forbid iframe** assertions).
</objective>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05-04 | Supply-chain | roller-checkout.js | mitigate | Remove third-party CDN fetch from default load path |
| T-05-05 | Broken UX | roller-checkout.js | mitigate | Preserve file path + empty IIFE so script tags do not 404 |
| T-05-06 | Misconfiguration | executors | accept | Document optional iframe restoration procedure only in file header + PLAN |

</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-booking-cta-flow/05-CONTEXT.md
@.planning/phases/05-booking-cta-flow/05-RESEARCH.md
@js/roller-checkout.js
@js/ticket-panel.js
@scripts/check-booking-architecture.js
@src/components/SiteScripts.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: No-op roller-checkout.js with BOOK-03 documentation</name>
  <files>js/roller-checkout.js</files>
  <read_first>
    <file>js/roller-checkout.js</file>
    <file>.planning/phases/05-booking-cta-flow/05-CONTEXT.md</file>
  </read_first>
  <action>
**Replace entire file contents** with:

1. A **block comment** at top stating:
   - Phase 5 BOOK-03 / D-02: **Default path does not load Roller iframe checkout.**
   - External checkout uses **`window.TMTicketPanel.getBookingUrl`** / ticket-panel resolver + same-tab navigation patterns.
   - **Optional iframe fallback** must be documented in ROADMAP follow-up if ever restored — do not silently re-enable CDN without PLAN approval.

2. An empty IIFE:
```javascript
(function () {
  'use strict';
  // Intentionally empty — capture-phase Roller interception removed (BOOK-03).
})();
```

**Forbidden substrings** anywhere in final file source:
- `checkout_iframe.js`
- `RollerCheckout`
- `cdn.rollerdigital.com`

  </action>
  <verify>
    <automated>! grep -q 'checkout_iframe' js/roller-checkout.js &amp;&amp; ! grep -q 'RollerCheckout' js/roller-checkout.js &amp;&amp; ! grep -q 'cdn.rollerdigital.com' js/roller-checkout.js</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`grep -q 'BOOK-03' js/roller-checkout.js` OR file header contains substring `Phase 5` documenting iframe removal.</criterion>
    <criterion>`grep -q 'checkout_iframe.js' js/roller-checkout.js` returns exit code **1** (no match).</criterion>
    <criterion>`grep -q 'cdn.rollerdigital.com' js/roller-checkout.js` returns exit code **1**.</criterion>
    <criterion>File parses as valid JS — `node --check js/roller-checkout.js` exits 0.</criterion>
  </acceptance_criteria>
  <done>Roller CDN and interception removed from default bundle behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh check-booking-architecture.js for stub era</name>
  <files>scripts/check-booking-architecture.js</files>
  <read_first>
    <file>scripts/check-booking-architecture.js</file>
    <file>js/roller-checkout.js</file>
  </read_first>
  <action>
1. **Remove** assertions that required **`loc.rollerCheckoutUrl`** substring inside `roller-checkout.js` (obsolete — Roller URLs live in ticket-panel resolver per Plan 05-01).

2. **Add** assertion: `rollerCheckout` source must **NOT** contain **`checkout_iframe.js`** — error message `'js/roller-checkout.js must not load Roller iframe CDN (BOOK-03)'`.

3. **Add** assertion: `rollerCheckout` source must **NOT** contain **`RollerCheckout.show`** or **`addEventListener('click'`** at capture phase tied to Roller — pragmatic check: forbid substring **`RollerCheckout`** entirely.

4. Keep **`ticketPanel` includes `window.TM.ready`** assertion.

  </action>
  <verify>
    <automated>node scripts/check-booking-architecture.js</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`node scripts/check-booking-architecture.js` exits 0.</criterion>
    <criterion>Script contains literal comment or message referencing `BOOK-03`.</criterion>
    <criterion>No remaining requirement that `roller-checkout.js` must reference `loc.rollerCheckoutUrl` (grep script source for `loc.rollerCheckoutUrl` — should appear **zero** times inside checks applied to `rollerCheckout` variable reads, or only in ticket-panel checks).</criterion>
  </acceptance_criteria>
  <done>Static booking gate matches iframe-free stub.</done>
</task>

</tasks>

<verification>
- `npm run check:booking` exits 0.
- Manual smoke: load homepage — DevTools Network must **not** request `checkout_iframe.js` on initial load + hero Book click path (BOOK-03 spot-check).
</verification>

<success_criteria>
- BOOK-03 satisfied for default path; optional iframe restoration requires explicit documentation elsewhere — not accidental code path.
</success_criteria>

<output>
Create `.planning/phases/05-booking-cta-flow/05-02-SUMMARY.md` noting Network panel verification outcome and whether `SiteScripts.astro` still references roller-checkout (expected OK).
</output>
