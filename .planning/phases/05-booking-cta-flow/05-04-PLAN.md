---
phase: 05-booking-cta-flow
plan: 04
type: execute
wave: 4
depends_on:
  - "05-03-PLAN.md"
files_modified:
  - package.json
  - tests/smoke/site.spec.js
  - docs/roller-booking-launch-checklist.md
autonomous: true
requirements:
  - BOOK-04
  - BOOK-05
must_haves:
  truths:
    - "`npm run verify:phase5` runs **`npm run check`**, **`npm run build:astro`**, **`npm run check:routes -- --dist`**, **`npm run check:astro-dist`**, **`npm run check:ticket-panel-parity`**, **`npm run check:ticket-panel-source-parity`**, and **`npm run test:smoke`** — Phase 5 gate."
    - "**`docs/roller-booking-launch-checklist.md`** enumerates BOOK-05 GTM/GA4 + native purchase verification steps when Venue Manager/playground exists."
    - "Smoke suite asserts **`?book=1`** on a representative **open** location page produces navigation to an **`https:`** URL (BOOK-04 regression harness)."
  artifacts:
    - path: "docs/roller-booking-launch-checklist.md"
      provides: "BOOK-05 launch readiness checklist"
    - path: "package.json"
      provides: "verify:phase5 script"
    - path: "tests/smoke/site.spec.js"
      provides: "Optional deep-link smoke coverage"
  key_links:
    - from: "tests/smoke/site.spec.js"
      to: "js/ticket-panel.js"
      via: "?book=1 redirect expectation"
      pattern: "book=1"
---

<objective>
Close Phase 5 verification loop: **BOOK-04** automated regression for **`?book=1`** on an open location page (expects eventual **`https:`** navigation after TM hydration); **BOOK-05** publish **`docs/roller-booking-launch-checklist.md`** for GTM/GA4 purchase validation when tooling access exists; wire **`verify:phase5`** extending Phase 4 parity gates.

Purpose: Executable cutover gate aligned with `.planning/ROADMAP.md` Phase 5 success criteria items 3 and 5.

Output: `package.json` script + docs + smoke addition — no booking resolver logic changes here.
</objective>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05-09 | Privacy | smoke test | mitigate | Assertions use public checkout URLs only — no credentials |
| T-05-10 | Flaky CI | Playwright | mitigate | Use `expect.poll` / generous timeout for external navigation |

</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@playwright.config.js
@tests/smoke/site.spec.js
@package.json
@philadelphia.html
@.planning/phases/05-booking-cta-flow/05-CONTEXT.md
@data/locations.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoke test — location page ?book=1 yields external https navigation</name>
  <files>tests/smoke/site.spec.js</files>
  <read_first>
    <file>tests/smoke/site.spec.js</file>
    <file>playwright.config.js</file>
    <file>js/ticket-panel.js</file>
    <file>data/locations.json</file>
  </read_first>
  <action>
Add **`test('open location ?book=1 navigates to https checkout', async ({ page }) => { ... })`**:

1. Pick slug **`philadelphia`** (open venue with guaranteed https checkout in `data/locations.json` — validate **either** `rollerCheckoutUrl` **or** `bookingUrl` starts with `https://`).

2. **`await page.goto('/philadelphia?book=1');`** — if Playwright webServer maps extensionless pages like existing tests, add **`page.route`** fallback mirroring `location selection persists canonical slug` test that rewrites `/philadelphia` → `/philadelphia.html` **only if** required by local server configuration.

3. After navigation settles, assert **`page.url()`** matches **`/^https:/`** (checkout domain reached). Use **`await expect.poll(() => page.url()).toMatch(/^https:/)`** with timeout **≥ 15000** ms to accommodate `setTimeout(..., 300)` + network.

4. Assert URL **does not** contain substring **`book=1`** after redirect completes.

If environment cannot reach external domains from CI, gate test with **`test.skip`** condition commented — **preferred:** keep enabled because Playwright resolves real HTTPS.

  </action>
  <verify>
    <automated>npm run test:smoke</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`grep -q 'book=1' tests/smoke/site.spec.js` succeeds after edit.</criterion>
    <criterion>`grep -q 'philadelphia' tests/smoke/site.spec.js` succeeds.</criterion>
    <criterion>`npm run test:smoke` exits 0 locally.</criterion>
  </acceptance_criteria>
  <done>BOOK-04 covered by automated smoke where network allows.</done>
</task>

<task type="auto">
  <name>Task 2: BOOK-05 launch checklist markdown</name>
  <files>docs/roller-booking-launch-checklist.md</files>
  <read_first>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/phases/05-booking-cta-flow/05-CONTEXT.md</file>
  </read_first>
  <action>
Create **`docs/roller-booking-launch-checklist.md`** with sections:

1. **Scope** — BOOK-05 / Phase 8 handoff reminder (measurement readiness).

2. **Prerequisites** — Roller Venue Manager or staging playground access; GTM container ID reference placeholder `{GTM-XXXX}`.

3. **Checkout QA** — Steps: complete test purchase on staging checkout URL from Phase 05 resolver output; verify thank-you page loads.

4. **dataLayer / GA4** — Bullets: confirm `purchase` or vendor-documented event fires **without PII** in ecommerce payload; cross-domain linker settings if applicable.

5. **Regression** — Link to `npm run verify:phase5` and smoke test names.

6. **Sign-off table** — Owner | Date | Pass/Fail placeholder columns.

Maximum **150 lines**.

  </action>
  <verify>
    <automated>test -f docs/roller-booking-launch-checklist.md</automated>
  </verify>
  <acceptance_criteria>
    <criterion>File contains substring `BOOK-05`.</criterion>
    <criterion>File contains substring `GTM`.</criterion>
    <criterion>File contains substring `verify:phase5`.</criterion>
    <criterion>`wc -l docs/roller-booking-launch-checklist.md` first column ≤ 150.</criterion>
  </acceptance_criteria>
  <done>BOOK-05 documentation artifact exists.</done>
</task>

<task type="auto">
  <name>Task 3: npm script verify:phase5</name>
  <files>package.json</files>
  <read_first>
    <file>package.json</file>
  </read_first>
  <action>
Add JSON script **`verify:phase5`** equal to exactly:

```
npm run check && npm run build:astro && npm run check:routes -- --dist && npm run check:astro-dist && npm run check:ticket-panel-parity && npm run check:ticket-panel-source-parity && npm run test:smoke
```

Use npm-run chaining consistent with **`verify:phase4`** pattern already in file.

  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.scripts['verify:phase5']) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`package.json` contains `"verify:phase5"` key inside `"scripts"`.</criterion>
    <criterion>The script value contains **`check:ticket-panel-parity`** substring.</criterion>
    <criterion>The script value contains **`test:smoke`** substring.</criterion>
  </acceptance_criteria>
  <done>Phase 5 verification entrypoint wired.</done>
</task>

</tasks>

<verification>
- Executor runs **`npm run verify:phase5`** before declaring plan complete — expected green on clean tree (may require network for smoke external navigation).
</verification>

<success_criteria>
- BOOK-04 and BOOK-05 artifacts traceable from repo docs and npm scripts.
</success_criteria>

<output>
Create `.planning/phases/05-booking-cta-flow/05-04-SUMMARY.md` capturing smoke timing observations and any CI networking caveats.
</output>
