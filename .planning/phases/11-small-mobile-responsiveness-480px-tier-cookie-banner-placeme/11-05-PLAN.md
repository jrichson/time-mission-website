---
phase: 11-small-mobile-responsiveness-480px-tier-cookie-banner-placeme
plan: 05
type: execute
wave: 3
depends_on: [11-01, 11-02, 11-03, 11-04]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [FND-02]

must_haves:
  truths:
    - "`npm run verify` (the canonical Astro build + dist validation chain) exits 0 end-to-end with all Phase 11 changes in place."
    - "`npm run check:tap-targets` exits 0 (independent quick-check)."
    - "ROADMAP.md Phase 11 entry is marked complete with date stamp and lists all 5 plans with statuses."
    - "STATE.md is updated: progress.completed_phases incremented, last_activity reflects Phase 11 closure, Recent trend block summarizes what shipped."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 11 marked [x] with completion date; Plans table updated; Wave summary added"
      contains: "[x] **Phase 11"
    - path: ".planning/STATE.md"
      provides: "completed_phases: 11; last_activity references Phase 11; Recent trend block updated"
      contains: "Phase 11"
  key_links:
    - from: ".planning/ROADMAP.md Phase 11 entry"
      to: "5 plan SUMMARYs in .planning/phases/11-.../"
      via: "Plans list with [x] markers and one-line objective per plan"
      pattern: "\\[x\\] 11-0[1-5]-PLAN.md"
    - from: ".planning/STATE.md last_activity"
      to: "verify command output"
      via: "verify chain exits 0 confirms all Phase 11 changes are runtime-stable"
      pattern: "npm run verify"
---

<objective>
Run the full verification chain to confirm plans 11-01 through 11-04 ship a launch-ready Astro build, then update `.planning/ROADMAP.md` and `.planning/STATE.md` to mark Phase 11 complete with the same fidelity as previous phase closures.

Purpose: Phase 11 is the last responsive-gap remediation before launch. ROADMAP/STATE bookkeeping is the gate that signals to the cutover sequence (`docs/cutover-checklist.md` step 1: `npm run verify:phase10` exits 0) that this work is done. STATE drives the next-action discoverability for human and AI operators.

Output: Verified `npm run verify` exit 0 result; modified ROADMAP.md and STATE.md reflecting Phase 11 closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-small-mobile-responsiveness-480px-tier-cookie-banner-placeme/11-CONTEXT.md
@docs/cutover-checklist.md

@package.json
@scripts/verify-site-output.mjs

<interfaces>
ROADMAP.md update points (lines 311-322 contain the Phase 11 placeholder):

Current state (line 311):
  ### Phase 11: small-mobile responsiveness ≤480px tier + cookie banner placement polish

Update target:
  - In the top-level Phases list (lines 14-25), add a new entry: `- [x] **Phase 11: small-mobile responsiveness ≤480px tier + cookie banner placement polish** (2026-05-05) - Brief outcome.`
  - In the Phase Details section (line 311+), update Plans line from "TBD" to actual plan list with [x] markers.
  - In the Progress table (lines 268-279), add a row: `| 11. small-mobile responsiveness ≤480px tier | 5/5 | Complete | 2026-05-05 |`

STATE.md update points (frontmatter + body):

Current frontmatter (lines 1-17):
  status: Cutover-ready
  last_activity: 2026-05-05 -- Phase 10 (Audit-Gap Closure & Cutover Readiness) closed; ...
  progress:
    completed_phases: 10
    total_plans: 47
    completed_plans: 47

Update target:
  status: Cutover-ready (no change — Phase 11 is a polish/fix layer, cutover gate is still verify:phase10)
  last_updated: <now>
  last_activity: 2026-05-05 -- Phase 11 (small-mobile responsiveness ≤480 + POLISH-01 cookie banner placement) closed; 5 plans shipped covering shared CSS + 7 partial ≤480 tiers + cookie banner .cm--box rewrite + Playwright 375×667 smoke
  progress:
    completed_phases: 11
    total_plans: 52
    completed_plans: 52

Body updates:
  - Current Position section: "Phase: 11" and "Plans: 52/52"
  - Performance Metrics velocity: "Total v1 plans completed: 40 (...) + 7 Phase 10 + 5 Phase 11 = 52"
  - Performance Metrics By Phase table: add row for Phase 11
  - Recent trend block: add Phase 11 paragraph above Phase 10 paragraph
  - Roadmap Evolution: add `2026-05-05 — Phase 11 closed: ...` entry under existing 2026-05-05 entries

Verification chain:
  - `npm run verify` runs `node scripts/verify-site-output.mjs` which executes `check`, `build:astro`, `check:astro-dist`, `test:smoke`, etc. (see package.json lines 40-58).
  - This chain MUST exit 0 with all Phase 11 changes (CSS + smoke test) in place.
  - `npm run check:tap-targets` is also confirmed independently (it's not in the default `check` chain — it's a separate guard that's been added in Phase 10).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run verify chain and capture results</name>
  <files>(none — read-only verification)</files>
  <read_first>
    - package.json (confirm verify and check:tap-targets script definitions)
    - scripts/verify-site-output.mjs (understand chain steps)
    - .planning/phases/11-small-mobile-responsiveness-480px-tier-cookie-banner-placeme/11-01-SUMMARY.md (confirm 11-01 completed)
    - .planning/phases/11-small-mobile-responsiveness-480px-tier-cookie-banner-placeme/11-02-SUMMARY.md (confirm 11-02 completed)
    - .planning/phases/11-small-mobile-responsiveness-480px-tier-cookie-banner-placeme/11-03-SUMMARY.md (confirm 11-03 completed)
    - .planning/phases/11-small-mobile-responsiveness-480px-tier-cookie-banner-placeme/11-04-SUMMARY.md (confirm 11-04 completed)
  </read_first>
  <behavior>
    After this task:
    - `npm run check:tap-targets` exits 0 (captured output in temp/log)
    - `npm run check` exits 0
    - `npm run verify` exits 0 (full pipeline: build + dist validators + smoke including new 375×667 tests)
    - `npm run verify:phase10` exits 0 (this is the cutover gate alias and must remain green)
  </behavior>
  <action>
    Run, in order, capturing both stdout/stderr and exit codes:

    1. `npm run check:tap-targets` — must exit 0. If non-zero, STOP and surface the failure to the orchestrator. Phase 11 cannot close while tap-targets fail.
    2. `npm run check` — must exit 0. Same stop-condition.
    3. `npm run build:astro` — must exit 0 (rebuild dist with all Phase 11 CSS).
    4. `npm run test:smoke` — must exit 0 (full smoke suite including new "small mobile (375x667)" describe).
    5. `npm run verify` — must exit 0 (canonical chain).
    6. `npm run verify:phase10` — must exit 0 (cutover gate alias; aliased to `npm run verify` per package.json line 58).

    If any step fails: do NOT proceed to ROADMAP/STATE updates. Surface the failure with full output to the orchestrator. The phase is not closeable until verify is green.

    On success: capture the timing and exit codes of each step in the SUMMARY. Note any new warnings (non-fatal) for the SUMMARY's Notes section.
  </action>
  <verify>
    <automated>npm run check:tap-targets &amp;&amp; npm run check &amp;&amp; npm run build:astro &amp;&amp; npm run test:smoke &amp;&amp; npm run verify</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check:tap-targets` exits 0
    - `npm run check` exits 0
    - `npm run build:astro` exits 0 with non-empty `dist/` directory
    - `npm run test:smoke` exits 0 with the "small mobile (375x667)" describe reported as PASSED in stdout
    - `npm run verify` exits 0
    - `npm run verify:phase10` exits 0
  </acceptance_criteria>
  <done>
    All six commands above exit 0. Captured outputs are in scratch and ready to be referenced from SUMMARY.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update ROADMAP.md and STATE.md to close Phase 11</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md (full file — confirm structure for adding Phase 11 entries: top Phases list, Phase Details, Progress table)
    - .planning/STATE.md (full file — confirm frontmatter shape and body sections)
    - .planning/phases/11-small-mobile-responsiveness-480px-tier-cookie-banner-placeme/11-CONTEXT.md (locked decisions reference for the closure description)
  </read_first>
  <behavior>
    After this task:
    - ROADMAP.md top Phases list contains a new line for Phase 11 marked `[x]` with date 2026-05-05.
    - ROADMAP.md Phase Details for Phase 11 (existing block at line 311+) has Plans line updated from "TBD" to the actual list of 5 plans with [x] markers, plus a Wave summary block.
    - ROADMAP.md Progress table contains a new row for Phase 11.
    - STATE.md frontmatter: `last_updated: <today ISO>`, `last_activity` updated, `completed_phases: 11`, `total_plans: 52`, `completed_plans: 52`.
    - STATE.md body: Current Position updated to Phase 11 / 52 plans; Performance Metrics By Phase table has a new row; Recent trend has a new bullet for Phase 11 closure; Roadmap Evolution has a new dated entry.
  </behavior>
  <action>
    First, READ the current ROADMAP.md (specifically lines 14-25 top Phases list, lines 268-279 Progress table, and lines 311-322 Phase 11 Phase Details) and STATE.md (full file).

    Then make the following surgical edits using the Edit tool (one Edit per logical change to keep diffs reviewable):

    ROADMAP.md edits:

    A. After line 24 (after the existing Phase 10 entry in the Phases list), add the Phase 11 entry:
    ```
    - [x] **Phase 11: small-mobile responsiveness ≤480px tier + cookie banner placement polish** (2026-05-05) - Closed launch-blocking responsive gap diagnosed in Phase 10 UAT test 8 (≤425px viewport): added @media (max-width: 480px) tier to 5 shared CSS files + 7 page-local partials; shipped POLISH-01 cookie banner bottom-left card placement (.cm--box override per docs/cutover-checklist.md spec); added Playwright smoke assertion at 375×667 viewport covering no horizontal scroll, footer-legal wrap, and ≥44×44 tap targets.
    ```

    B. Replace the Phase 11 Phase Details Plans block (lines 319-322 — the "TBD" placeholder):
    ```
    **Plans:** 5/5 plans complete

    Plans:
    - [x] 11-01-PLAN.md — ≤480 tier for 5 shared CSS files (nav, footer, faq, base, newsletter) (FND-02)
    - [x] 11-02-PLAN.md — ≤480 tier for 7 page partials (about, contact, faq, locations, birthdays, legal, houston) (FND-02)
    - [x] 11-03-PLAN.md — POLISH-01 cookie banner .cm--box rewrite in css/cookie-consent.css (FND-02)
    - [x] 11-04-PLAN.md — Playwright smoke assertion at 375×667 (no horizontal scroll, footer-legal wrap, .location-btn ≥ 44×44) (FND-02)
    - [x] 11-05-PLAN.md — Verify chain + ROADMAP/STATE bookkeeping

    Wave summary:
    - Wave 1 (parallel): 11-01 shared CSS ≤480 tier + 11-02 page partials ≤480 tier + 11-03 cookie banner POLISH-01 (independent files)
    - Wave 2: 11-04 Playwright 375×667 smoke (depends on Wave 1 CSS being in place)
    - Wave 3: 11-05 verify + ROADMAP/STATE bookkeeping (depends on Wave 2)

    **Locked decisions (CONTEXT.md):** D-A1 per-element ≤480 rules (no clamp); D-A2 visual parity preserved; D-A3 tap targets ≥44 (≥48 for .btn-tickets); D-A4 reuse existing CSS files; D-B1..D-B6 cookie banner POLISH-01 spec verbatim from docs/cutover-checklist.md.
    ```

    C. In the Progress table (lines 268-279), append after the Phase 10 row:
    ```
    | 11. small-mobile responsiveness ≤480px tier | 5/5 | Complete | 2026-05-05 |
    ```

    STATE.md edits:

    D. Frontmatter (lines 1-17):
    - Set `last_updated: "<today ISO timestamp, e.g., 2026-05-05T17:00:00.000Z>"`
    - Replace `last_activity` with: `2026-05-05 -- Phase 11 (small-mobile responsiveness ≤480 + POLISH-01 cookie banner placement) closed; 5 plans shipped covering 5 shared CSS files + 7 partial ≤480 tiers + cookie banner .cm--box rewrite + Playwright 375×667 smoke`
    - Update `progress.total_phases: 11`, `progress.completed_phases: 11`, `progress.total_plans: 52`, `progress.completed_plans: 52`. Keep `percent: 100` and `percent_v1_core: 100` (Phase 11 is a polish/fix layer not affecting v1 requirement coverage).

    E. Current Position section: change `Phase: 10` → `Phase: 11`, `Plans: **47/47**` → `Plans: **52/52**`. Update Last activity date if needed.

    F. Performance Metrics velocity bullet: change `Total v1 plans completed: 40 ...` to `Total v1 plans completed: 40 (roadmap 01-xx … 08-xx); Phase 9 summary; Phase 10: 7 plans; Phase 11: 5 plans (52 total)`.

    G. Performance Metrics By Phase table: add `| 11. small-mobile responsiveness ≤480 + POLISH-01 | 5 | Complete | — |`.

    H. Recent trend section: prepend a new bullet ABOVE the existing Phase 10 bullet:
    ```
    - **Phase 11 closed 2026-05-05**: 5 plans shipped closing the launch-blocking responsive gap from Phase 10 UAT test 8 (severity major, ≤425px viewports). Wave summary: (W1) 11-01 ≤480 tier across `nav.css` / `footer.css` / `faq.css` / `base.css` / `newsletter.css`; 11-02 ≤480 tier across 7 partials (`about`, `contact`, `faq`, `locations`, `birthdays` 2nd block, `legal`, `houston`); 11-03 POLISH-01 cookie banner .cm--box rewrite (rgba(20,20,24,0.92) surface, orange 18% border, 220ms cubic-bezier slide-up, mobile <640 full-width minus 16px gutter above iOS home indicator). (W2) 11-04 Playwright `small mobile (375x667)` describe with no-horizontal-scroll on 4 pages + .footer-legal wrap + .location-btn / .btn-tickets ≥ 44×44 tap targets. (W3) 11-05 verify chain green + ROADMAP/STATE bookkeeping. Tap-target floor preserved throughout.
    ```

    I. Roadmap Evolution section: add a new dated entry under the existing 2026-05-05 entries:
    ```
    - 2026-05-05 — Phase 11 closed: ≤480 small-mobile tier shipped across 5 shared CSS + 7 partials; POLISH-01 cookie banner placement implemented in css/cookie-consent.css; Playwright 375×667 smoke locked in. Cutover gate `npm run verify:phase10` remains green; v1 requirement coverage unchanged at 43/43.
    ```

    DO NOT remove existing entries from any section. DO NOT change the v1 requirement coverage table (Phase 11 references FND-02 which is already mapped to Phase 4; this is a polish layer that extends FND-02 without renumbering).
  </action>
  <verify>
    <automated>grep "\[x\] \*\*Phase 11" .planning/ROADMAP.md &amp;&amp; grep "completed_phases: 11" .planning/STATE.md &amp;&amp; grep "Phase 11 closed 2026-05-05" .planning/STATE.md &amp;&amp; grep "11-01-PLAN.md — ≤480 tier" .planning/ROADMAP.md &amp;&amp; npm run verify</automated>
  </verify>
  <acceptance_criteria>
    - `grep "\[x\] \*\*Phase 11" .planning/ROADMAP.md` returns 1 line in the Phases list
    - `grep -c "11-0[1-5]-PLAN.md" .planning/ROADMAP.md` returns ≥ 5 (one per plan in the new Plans block)
    - `grep "11. small-mobile" .planning/ROADMAP.md` returns ≥ 1 line in the Progress table
    - `grep "completed_phases: 11" .planning/STATE.md` returns 1 line (frontmatter)
    - `grep "completed_plans: 52" .planning/STATE.md` returns 1 line (frontmatter)
    - `grep "Phase 11 closed 2026-05-05" .planning/STATE.md` returns 1 line (Recent trend block)
    - `grep "POLISH-01" .planning/STATE.md` returns ≥ 1 line
    - `npm run verify` exits 0 (final post-bookkeeping confirmation)
  </acceptance_criteria>
  <done>
    ROADMAP.md and STATE.md reflect Phase 11 closure; all grep acceptance criteria pass; final `npm run verify` still exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — docs/bookkeeping only) | This plan only updates planning docs (ROADMAP.md, STATE.md). No code, no CSS, no JS, no dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09 | Tampering | .planning/ROADMAP.md, .planning/STATE.md | accept | Documentation/bookkeeping changes only. No production code or runtime behavior modified. ASVS L1 baseline satisfied. |
</threat_model>

<verification>
- All Task 1 commands exit 0 (verify chain green).
- All Task 2 grep acceptance criteria match.
- `npm run verify` exits 0 after the bookkeeping edits (sanity check that doc-only edits did not break anything).
- `git diff --stat` shows only the expected files modified (5 CSS files from 11-01, 7 partials from 11-02, css/cookie-consent.css from 11-03, tests/smoke/site.spec.js from 11-04, .planning/ROADMAP.md + .planning/STATE.md from 11-05).
</verification>

<success_criteria>
- `npm run check:tap-targets` exits 0
- `npm run check` exits 0
- `npm run build:astro` exits 0
- `npm run test:smoke` exits 0 (including new small-mobile describe)
- `npm run verify` exits 0
- `npm run verify:phase10` exits 0
- ROADMAP.md Phase 11 entry marked `[x]` with 2026-05-05 date and full Plans list
- STATE.md frontmatter shows completed_phases: 11, completed_plans: 52
- STATE.md Recent trend section starts with the new Phase 11 closure bullet
</success_criteria>

<output>
After completion, create `.planning/phases/11-small-mobile-responsiveness-480px-tier-cookie-banner-placeme/11-05-SUMMARY.md` with: timing and exit codes for all 6 verify commands, ROADMAP/STATE diff summary, confirmation of all grep acceptance criteria, and a cross-link to docs/cutover-checklist.md noting Phase 11 closure.
</output>
