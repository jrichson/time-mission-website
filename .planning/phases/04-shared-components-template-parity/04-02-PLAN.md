---
phase: 04-shared-components-template-parity
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01-PLAN.md
files_modified:
  - src/components/SiteNav.astro
  - src/components/MobileMenu.astro
  - src/components/LocationOverlay.astro
  - src/components/SiteFooter.astro
  - src/components/Faq.astro
  - src/components/Breadcrumbs.astro
  - src/layouts/SiteLayout.astro
autonomous: true
requirements:
  - FND-02
  - COMP-01
  - COMP-02
must_haves:
  truths:
    - "SiteLayout fills the `nav` slot with SiteNav + MobileMenu + LocationOverlay (in that DOM order, matching legacy index.html lines 2929–3045) and the `footer` slot with SiteFooter."
    - "SiteNav, MobileMenu, LocationOverlay preserve every selector consumed by js/nav.js: #nav, #mobileMenu, .mobile-menu-links a, .nav-menu-btn, .ticker-bar, .nav-logo, #locationBtn, #locationText, #locationDropdown, #locationInfo, #locationMap, .location-dropdown-close with aria-label starting `Close`, plus `[data-city=\"<City>\"]` per location link."
    - "SiteNav primary links and SiteFooter columns render from `src/data/site/navigation.json` and `src/data/site/footer.json` (D-08); no hand-coded literals duplicate Phase 3 catalogs."
    - "LocationOverlay iterates `allLocations` from `src/data/locations.ts` and emits `<a href={'/'+slug} data-city={name}>` for every location, with `class=\"location-coming-soon\"` plus a `<span class=\"coming-soon-tag\">Coming Soon</span>` for `status === 'coming-soon'` rows; legacy region grouping (United States vs Europe) preserved via `region` field."
    - "Faq.astro renders `.faq-question[role=\"button\"][aria-expanded=\"false\"]` so smoke test `tests/smoke/site.spec.js` lines 53–58 keep passing on FAQ pages."
  artifacts:
    - path: "src/components/SiteNav.astro"
      provides: "Top nav (#nav) with hamburger button, logo, primary links, location button, Book Now CTA"
    - path: "src/components/MobileMenu.astro"
      provides: "Full-screen mobile menu (#mobileMenu) with social icons + Book Now CTA"
    - path: "src/components/LocationOverlay.astro"
      provides: "Full-screen location dropdown (#locationDropdown) iterating allLocations"
    - path: "src/components/SiteFooter.astro"
      provides: ".site-footer rendered from src/data/site/footer.json columns"
    - path: "src/components/Faq.astro"
      provides: "FAQ accordion with role=button + aria-expanded markup, props-driven items array"
    - path: "src/components/Breadcrumbs.astro"
      provides: "Breadcrumb markup (Phase 7 will wire JSON-LD; Phase 4 ships visible markup only)"
  key_links:
    - from: "src/components/SiteNav.astro"
      to: "src/data/site/navigation.json"
      via: "static import + .map() over primary[]"
      pattern: "navigation.json"
    - from: "src/components/SiteFooter.astro"
      to: "src/data/site/footer.json"
      via: "static import + .map() over columns[]"
      pattern: "footer.json"
    - from: "src/components/LocationOverlay.astro"
      to: "src/data/locations.ts (allLocations)"
      via: "static import + .filter() by region + status"
      pattern: "allLocations"
    - from: "src/layouts/SiteLayout.astro"
      to: "src/components/SiteNav.astro + SiteFooter.astro"
      via: "named slot fills (slot=\"nav\", slot=\"footer\")"
      pattern: "slot=\"nav\""
---

<objective>
Layer the data-driven shared chrome onto Plan 04-01's foundation: SiteNav, MobileMenu, LocationOverlay, SiteFooter, plus reusable Faq and Breadcrumbs components. Wire SiteLayout so its `nav` slot defaults to the nav-cluster and its `footer` slot defaults to SiteFooter — pages from Plans 04-03/04-04 then get full chrome by importing only `SiteLayout`. All durable copy/links flow from `src/data/site/*.json` and `src/data/locations.ts` per D-08.

Purpose: Satisfy COMP-01 (shared nav/footer/picker/FAQ/breadcrumb components) and COMP-02 (preserve every selector + ARIA hook js/nav.js depends on) without redesigning anything.

Output: Six new `.astro` components plus a SiteLayout edit that defaults `nav` and `footer` slot contents.
</objective>

<threat_model>
Static marketing site — locations and chrome data are author-controlled JSON validated by `check-site-data.js` and `check-location-contracts.js`. Risk surface limited to drift and selector breakage.

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-04-06 | Tampering (Pitfall 4) | LocationOverlay.astro | mitigate | `data-city={location.name}` and `<a href={'/' + location.slug}>` come straight from `allLocations`; never from display name shortcut; keeps `js/nav.js` line 117–139 selectors aligned |
| T-04-07 | Tampering (XSS via JSON) | SiteNav.astro / SiteFooter.astro / Faq.astro | mitigate | Use Astro default `{value}` interpolation; NEVER `set:html`. Phase 3 `check-site-data.js` already rejects `<` / `>` in FAQ/site JSON |
| T-04-08 | Tampering (Pitfall 2) | SiteNav.astro / LocationOverlay.astro | mitigate | No scoped `<style>` blocks for any selector that exists in `css/nav.css` (e.g. `.nav`, `.mobile-menu`, `.location-dropdown-close`, `.btn-tickets`); rely on existing global CSS |
| T-04-09 | Tampering (Pitfall 3) | SiteLayout.astro | accept | No new HTML comment markers introduced in this plan; existing `<!-- Ticket Popup Panel -->` (Plan 04-01) unchanged |
| T-04-10 | Information Disclosure | SiteFooter.astro | accept | No PII in footer; social URLs are public marketing handles already in legacy footer |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/04-shared-components-template-parity/04-CONTEXT.md
@.planning/phases/04-shared-components-template-parity/04-RESEARCH.md
@.planning/phases/04-shared-components-template-parity/04-UI-SPEC.md
@.planning/research/PITFALLS.md
@js/nav.js
@js/locations.js
@js/a11y.js
@css/nav.css
@css/footer.css
@css/faq.css
@index.html
@philadelphia.html
@faq.html
@src/data/locations.ts
@src/data/site/navigation.json
@src/data/site/footer.json
@src/data/site/faqs.json
@src/data/routes.json
@src/components/SiteHead.astro
@src/components/TicketPanel.astro
@src/components/SiteScripts.astro
@src/layouts/SiteLayout.astro

<interfaces>
<!-- Phase 3 data shapes the executor consumes. No exploration needed. -->

From src/data/locations.ts:
```typescript
export interface LocationRecord {
  id: string;
  slug: string;
  name: string;          // e.g. "Philadelphia", "Houston (Marq'E)"
  shortName: string;
  region: string;        // "United States" | "Europe"
  status: 'open' | 'coming-soon';
  navLabel: string;      // e.g. "PA - Philadelphia"
  // ...
}
export const allLocations: LocationRecord[];
```

From src/data/site/navigation.json (already populated by Phase 3):
```json
{
  "primary": [ { "label": "About", "href": "/about" }, ... ],
  "groups":  [ { "label": "Birthdays", "href": "/groups/birthdays" }, ... ]
}
```

From src/data/site/footer.json (already populated):
```json
{
  "columns": [ { "title": "EXPERIENCE", "links": [ { "label": "About", "href": "/about" }, ... ] }, ... ]
}
```

From src/data/site/faqs.json (already populated):
```json
{
  "sections": [ { "id": "general", "heading": "General", "items": [ { "q": "...", "a": "..." }, ... ] } ]
}
```

Legacy nav cluster (literal, from index.html lines 2929–3045):
- `<div class="mobile-menu" id="mobileMenu">…</div>` — mobile-menu-links li/a, mobile-menu-socials, mobile-menu-cta with `<a class="btn-tickets">Book Now</a>`.
- `<div class="ticker-bar">…</div>` — eight `.ticker-item` spans.
- `<nav class="nav" id="nav">` — `.nav-left` (button.nav-menu-btn + a.nav-logo), `.nav-center` (ul.nav-links), `.nav-right` (button#locationBtn span#locationText + a.btn-tickets).
- `<div class="location-dropdown" id="locationDropdown">` — `.location-dropdown-header` (a.location-dropdown-logo + button.location-dropdown-close `aria-label="Close"`), `.location-dropdown-title`, `.location-overlay-body` (left `.location-group` per region with `<a href="/{slug}" data-city="{Name}">{Region} - {Name}</a>`, right `#locationInfo` with `#locationMap` and `.location-info-*` empty-state markup).

js/nav.js consumed selectors (verified, do NOT rename):
- `#nav`, `#mobileMenu`, `.mobile-menu-links a`, `.nav-menu-btn`, `.ticker-bar`, `.nav-logo`, `.location-dropdown-logo`
- `#locationBtn`, `#locationText`, `#locationDropdown`, `.location-dropdown-close`
- `[data-city="<City Name>"]` selector per location link
- `#locationInfo` + `.location-info-empty`, `.location-info-details`, `.location-info-name`, `.location-info-address`, `.location-info-phone`, `.location-info-hours`, `.location-info-actions`, `.location-info-book`
- `#locationMap`
- Ticker `.menu-hidden` toggle class

js/a11y.js + check-accessibility-baseline.js requirements:
- LocationOverlay close button MUST be `<button class="location-dropdown-close" aria-label="Close">` — substring match `location-dropdown-close" aria-label="Close` (line 41 of check script).

Legacy FAQ markup (from faq.html — verify accordion shape with the file before authoring):
- `<button class="faq-question" role="button" aria-expanded="false">…</button>` followed by sibling `<div class="faq-answer">…</div>` per Q/A.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SiteNav.astro + MobileMenu.astro + LocationOverlay.astro</name>
  <files>src/components/SiteNav.astro, src/components/MobileMenu.astro, src/components/LocationOverlay.astro</files>
  <read_first>
    <file>index.html</file>
    <file>js/nav.js</file>
    <file>js/a11y.js</file>
    <file>css/nav.css</file>
    <file>src/data/site/navigation.json</file>
    <file>src/data/locations.ts</file>
    <file>scripts/check-accessibility-baseline.js</file>
  </read_first>
  <action>Create three components. NO scoped `<style>` — rely on existing `css/nav.css?v=17` (loaded by SiteHead) for all rules. NO inline event handlers — `js/nav.js` IIFE binds them.

**File A — `src/components/SiteNav.astro`** (mirrors index.html lines 2913–2992):

Frontmatter:
```typescript
import nav from '../data/site/navigation.json';
const tickerMessages = [
  'NEW LOCATIONS COMING SOON!',
  'NEW LOCATIONS COMING SOON!',
  'NEW LOCATIONS COMING SOON!',
  'NEW LOCATIONS COMING SOON!',
  'NEW LOCATIONS COMING SOON!',
  'NEW LOCATIONS COMING SOON!',
  'NEW LOCATIONS COMING SOON!',
  'NEW LOCATIONS COMING SOON!'
];
```

Template (in this exact DOM order):
1. `<!-- Announcement Ticker -->` literal HTML comment.
2. `<div class="ticker-bar"><div class="ticker-track">{tickerMessages.map(...)}</div></div>` — 8 `<span class="ticker-item">{m}</span>` rows.
3. `<!-- Navigation -->` literal HTML comment.
4. `<nav class="nav" id="nav">` containing:
   - `<div class="nav-left">` with `<button class="nav-menu-btn" aria-label="Open menu"><div class="hamburger"><span></span><span></span><span></span></div></button>` and `<a href="/" class="nav-logo"><img src="/assets/logo/TM_Logo_White.svg" alt="Time Mission" /></a>`.
   - `<div class="nav-center">` with `<ul class="nav-links">` rendering FOUR primary anchors hard-coded as in index.html: `#about`, `#experiences`, `#groups`, `#faq` (these are intra-page anchors used on the homepage; per-page nav variations come later — see note below).
   - `<div class="nav-right">` with `<button class="location-btn" id="locationBtn" aria-label="Change location">` (location pin SVG + `<span id="locationText">Select Location</span>` + chevron SVG) and `<a href="#" class="btn-tickets">Book Now</a>`.

Note on `.nav-center` links: legacy homepage uses anchors (`#about` etc.); legacy non-homepage pages (e.g. about.html) usually do NOT have these anchors. For Phase 4 parity ship the homepage variant verbatim (these match index.html); per-page nav variations are page-level concerns handled in Plans 04-03/04-04 if needed (rare — most legacy pages also use the same `#about` etc. anchors against the homepage). Do NOT data-drive nav-center from `navigation.json` in this task — `navigation.json` `primary` array is for the MOBILE menu and footer use cases, not the desktop nav-center which is hard-coded across legacy pages.

**File B — `src/components/MobileMenu.astro`** (mirrors index.html lines 2929–2953):

Frontmatter:
```typescript
import nav from '../data/site/navigation.json';
```

Template (exact DOM order):
1. `<!-- Full-Screen Mobile Menu -->` literal comment.
2. `<div class="mobile-menu" id="mobileMenu">` containing:
   - `<ul class="mobile-menu-links">` rendering one `<li><a href={item.href}>{item.label}</a></li>` per entry in `nav.primary` (data-driven). Order: as it appears in `navigation.json` `primary` array (currently About / Missions / Groups / FAQ / Gift Cards / Locations / Contact — keep that order).
   - `<div class="mobile-menu-socials">` with three Instagram / TikTok / Facebook anchors VERBATIM from legacy (target=_blank rel=noopener, aria-label, full SVG bodies).
   - `<div class="mobile-menu-cta"><a href="#" class="btn-tickets">Book Now</a></div>`.

**File C — `src/components/LocationOverlay.astro`** (mirrors index.html lines 2994–3052):

Frontmatter:
```typescript
import { allLocations } from '../data/locations';
const usLocations = allLocations.filter((l) => l.region === 'United States');
const euLocations = allLocations.filter((l) => l.region === 'Europe');
```

Template (exact DOM order):
1. `<!-- Full-screen Location Overlay -->` literal comment.
2. `<div class="location-dropdown" id="locationDropdown">` containing:
   - `<div class="location-dropdown-header">` with `<a href="/" class="location-dropdown-logo"><img src="/assets/logo/TM_Logo_White.svg" alt="Time Mission" /></a>` and `<button class="location-dropdown-close" aria-label="Close">&times;</button>` — attribute order EXACTLY `class` then `aria-label` so check-accessibility-baseline.js line 41 substring matches.
   - `<div class="location-dropdown-title">Select Your Location</div>`.
   - `<div class="location-overlay-body">` containing:
     - `<div class="location-overlay-left">` with two region groups:
       - `<div class="location-group"><div class="location-group-title">United States</div>{usLocations.map(loc => …)}</div>`
       - `<div class="location-group"><div class="location-group-title">Europe</div>{euLocations.map(loc => …)}</div>`
       - Each `<a>` MUST match this exact template — `<a href={'/' + loc.slug} data-city={loc.name} class={loc.status === 'coming-soon' ? 'location-coming-soon' : undefined}>{loc.navLabel}{loc.status === 'coming-soon' && <span class="coming-soon-tag">Coming Soon</span>}</a>`. The legacy markup uses ` <span class="coming-soon-tag">Coming Soon</span>` (note leading space) only on `.location-coming-soon` rows. Astro will OMIT the `class` attribute when value is `undefined`.
     - `<div class="location-overlay-divider"></div>`.
     - `<div class="location-overlay-right" id="locationInfo">` with `<div class="location-map" id="locationMap"></div>` and `<div class="location-info-content">` empty-state markup VERBATIM from legacy lines 3025–3043.

Critical guards (Pitfall 4):
- `data-city={loc.name}` — use `name`, NOT `shortName` or `slug`. `js/nav.js` lines 117–139 expect display names (e.g. `"Philadelphia"`, `"Houston (Marq'E)"`). Validate with `console.log` during dev: `dist/index.html` MUST contain `data-city="Philadelphia"` and `data-city="Houston (Marq'E)"`.
- `href={'/' + loc.slug}` — leading slash, no extension, no trailing slash, never `.html` (ROUTE-01).
- For `loc.region === 'United States'` use the literal string `"United States"` (not `"USA"`).</action>
  <verify>
    <automated>npx astro check 2>&amp;1 | tail -5 &amp;&amp; grep -q 'id="nav"' src/components/SiteNav.astro &amp;&amp; grep -q 'id="locationBtn"' src/components/SiteNav.astro &amp;&amp; grep -q 'id="mobileMenu"' src/components/MobileMenu.astro &amp;&amp; grep -q 'id="locationDropdown"' src/components/LocationOverlay.astro &amp;&amp; grep -q 'id="locationInfo"' src/components/LocationOverlay.astro &amp;&amp; grep -q 'id="locationMap"' src/components/LocationOverlay.astro &amp;&amp; grep -q 'class="location-dropdown-close" aria-label="Close"' src/components/LocationOverlay.astro &amp;&amp; grep -q 'data-city={loc.name}' src/components/LocationOverlay.astro &amp;&amp; echo NAV_CLUSTER_OK</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`src/components/SiteNav.astro` contains `id="nav"`, `id="locationBtn"`, `id="locationText"`, `class="nav-menu-btn"`, `class="ticker-bar"`, `class="nav-logo"`, `class="btn-tickets"`.</criterion>
    <criterion>`src/components/MobileMenu.astro` contains `id="mobileMenu"`, `class="mobile-menu-links"`, `class="mobile-menu-socials"`, `class="mobile-menu-cta"`, and imports `navigation.json`.</criterion>
    <criterion>`src/components/MobileMenu.astro` renders one `<li><a>` per `nav.primary` entry; `grep -c 'mobile-menu-links' src/components/MobileMenu.astro` returns ≥ 1 and the `<ul>` is data-driven (contains `nav.primary.map`).</criterion>
    <criterion>`src/components/LocationOverlay.astro` contains literal substring `class="location-dropdown-close" aria-label="Close"` (exact attribute order — required by check-accessibility-baseline.js).</criterion>
    <criterion>`src/components/LocationOverlay.astro` imports `allLocations` from `../data/locations` and uses `data-city={loc.name}` (NOT `loc.shortName` or `loc.slug`).</criterion>
    <criterion>`src/components/LocationOverlay.astro` filters `region === 'United States'` and `region === 'Europe'` and emits two `<div class="location-group">` blocks.</criterion>
    <criterion>None of the three files contains `set:html` (`grep -E "set:html" src/components/SiteNav.astro src/components/MobileMenu.astro src/components/LocationOverlay.astro` returns no matches).</criterion>
    <criterion>None contains a scoped `<style>` block targeting `.nav`, `.mobile-menu`, or `.location-dropdown-*` class (`grep -E "<style[^>]*>" src/components/SiteNav.astro src/components/MobileMenu.astro src/components/LocationOverlay.astro` returns no matches).</criterion>
    <criterion>`npx astro check` exits 0.</criterion>
  </acceptance_criteria>
  <done>Nav cluster components exist with preserved selectors and data-driven mobile menu + location overlay.</done>
</task>

<task type="auto">
  <name>Task 2: SiteFooter.astro from footer.json</name>
  <files>src/components/SiteFooter.astro</files>
  <read_first>
    <file>index.html</file>
    <file>css/footer.css</file>
    <file>src/data/site/footer.json</file>
    <file>scripts/check-site-data.js</file>
  </read_first>
  <action>Create `src/components/SiteFooter.astro`. Frontmatter:

```typescript
import footer from '../data/site/footer.json';
const year = new Date().getFullYear();
```

Template (exact DOM order):
1. `<!-- Footer -->` literal HTML comment.
2. `<footer class="site-footer">` containing:
   - `<div class="footer-inner">` (matches legacy css/footer.css inner wrapper) with:
     - `<div class="footer-columns">` rendering one `<div class="footer-column">` per entry in `footer.columns`, each containing `<h4>{col.title}</h4>` and `<ul>{col.links.map(...)}</ul>` where each `<li>` is `<li><a href={link.href}>{link.label}</a></li>`.
     - `<div class="footer-bottom">` with:
       - `<a href="/" class="footer-logo"><img src="/assets/logo/TM_Logo_White.svg" alt="Time Mission" /></a>`
       - `<p class="footer-copy">&copy; {year} Time Mission. All rights reserved.</p>`
       - `<div class="footer-legal-links"><a href="/privacy">Privacy</a><a href="/terms">Terms</a><a href="/cookies">Cookies</a></div>`

Rules:
- All `href` values are clean paths (already validated by `check-site-data.js`); never insert `.html`.
- No `set:html`. Use `{value}` interpolation.
- No new selectors that override `css/footer.css`. If legacy markup uses `.site-footer` and `.footer-column`, use those exact class names.
- Cross-check legacy footer in `index.html` AFTER reading the file: if legacy uses different class wrappers (e.g. `.footer-row` instead of `.footer-columns`), MATCH the legacy class names, not invent new ones. Failing that, the executor MUST adopt whatever class name CSS in `css/footer.css` already targets — read `css/footer.css` first.</action>
  <verify>
    <automated>npx astro check 2>&amp;1 | tail -5 &amp;&amp; grep -q "import footer" src/components/SiteFooter.astro &amp;&amp; grep -q "class=\"site-footer\"" src/components/SiteFooter.astro &amp;&amp; grep -q "footer.columns.map" src/components/SiteFooter.astro &amp;&amp; echo FOOTER_OK</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`src/components/SiteFooter.astro` imports `../data/site/footer.json`.</criterion>
    <criterion>The file contains `class="site-footer"` and renders one `<div class="footer-column">` per entry via `.map()`.</criterion>
    <criterion>The file contains an `<h4>` rendering `{col.title}` and an `<ul>` rendering `{col.links.map(link => <li><a href={link.href}>{link.label}</a></li>)}`.</criterion>
    <criterion>The file's footer-bottom legal links point to `/privacy`, `/terms`, `/cookies` (no `.html` extensions).</criterion>
    <criterion>`grep -E "set:html" src/components/SiteFooter.astro` returns no matches.</criterion>
    <criterion>`npx astro check` exits 0.</criterion>
  </acceptance_criteria>
  <done>Footer renders from validated JSON; all hrefs are clean routes.</done>
</task>

<task type="auto">
  <name>Task 3: Faq.astro + Breadcrumbs.astro + SiteLayout slot defaults</name>
  <files>src/components/Faq.astro, src/components/Breadcrumbs.astro, src/layouts/SiteLayout.astro</files>
  <read_first>
    <file>faq.html</file>
    <file>css/faq.css</file>
    <file>js/a11y.js</file>
    <file>tests/smoke/site.spec.js</file>
    <file>src/data/site/faqs.json</file>
    <file>src/layouts/SiteLayout.astro</file>
  </read_first>
  <action>Create two new components and edit SiteLayout.

**File A — `src/components/Faq.astro`** (matches faq.html accordion structure + smoke test contract):

Frontmatter:
```typescript
interface FaqItem { q: string; a: string; }
interface Props {
  items: FaqItem[];
  heading?: string;        // optional section heading rendered above
  startExpanded?: number;  // optional 0-based index of an item to open by default; otherwise none open
}
const { items, heading, startExpanded } = Astro.props;
```

Template:
- If `heading` truthy: `<h3 class="faq-heading">{heading}</h3>` first.
- `<div class="faq-list">` then for each `(item, i)`:
  - `<div class="faq-item">`
    - `<button class="faq-question" role="button" aria-expanded={i === startExpanded ? 'true' : 'false'}>{item.q}</button>`
    - `<div class="faq-answer">{item.a}</div>`
  - `</div>`

Critical:
- `<button class="faq-question" role="button" aria-expanded="false">` MUST be the literal output for items where `i !== startExpanded`. The smoke test (`tests/smoke/site.spec.js` line 53–55) asserts `role=button` AND `aria-expanded=false`. Default `startExpanded` to `-1` (no item open) when undefined.
- Use Astro `{item.a}` text interpolation only — `check-site-data.js` already rejects `<` / `>` in FAQ JSON, but defense-in-depth: do NOT use `set:html`.
- The `<button>` element with `role="button"` is intentional even though `<button>` already has button role — the legacy markup uses `<button role="button">` and the smoke test asserts the `role` attribute is present.

**File B — `src/components/Breadcrumbs.astro`** (visible markup only, JSON-LD is Phase 7 scope):

Frontmatter:
```typescript
interface Crumb { label: string; href?: string; }   // last crumb has no href (current page)
interface Props { items: Crumb[]; }
const { items } = Astro.props;
```

Template:
- `<nav class="breadcrumbs" aria-label="Breadcrumb"><ol>`
- For each `(crumb, i)` if `crumb.href` truthy: `<li><a href={crumb.href}>{crumb.label}</a></li>`. Else: `<li aria-current="page">{crumb.label}</li>`.
- `</ol></nav>`

Rules: no `set:html`. Phase 7 will add `<script type="application/ld+json">` for BreadcrumbList — DO NOT add JSON-LD here.

**File C — `src/layouts/SiteLayout.astro` edits** (additive — preserve all Plan 04-01 contracts):

Edit the existing `src/layouts/SiteLayout.astro` to:
1. Add to frontmatter imports:
   ```typescript
   import SiteNav from '../components/SiteNav.astro';
   import MobileMenu from '../components/MobileMenu.astro';
   import LocationOverlay from '../components/LocationOverlay.astro';
   import SiteFooter from '../components/SiteFooter.astro';
   ```
2. In the body, replace `<slot name="nav" />` with the named-slot fallback pattern Astro supports:
   ```astro
   <slot name="nav">
     <MobileMenu />
     <SiteNav />
     <LocationOverlay />
   </slot>
   ```
3. Replace `<slot name="footer" />` with:
   ```astro
   <slot name="footer">
     <SiteFooter />
   </slot>
   ```
4. Do NOT touch the order of `<TicketPanel />` and `<SiteScripts />` — they remain the last two body elements.
5. The `pre-nav` slot remains an unfilled `<slot name="pre-nav" />` (used by index.astro for the game popup overlay; legacy index.html line 2872–2913).

DOM order in the rendered body (verify by inspection):
`<slot name="pre-nav" />` → `<MobileMenu />` → `<SiteNav />` → `<LocationOverlay />` → default slot → `<SiteFooter />` → `<TicketPanel />` → `<SiteScripts />`.

This matches legacy index.html order: game popup → mobile menu → ticker → nav → location overlay → main content → footer → ticket panel → scripts.</action>
  <verify>
    <automated>npx astro check 2>&amp;1 | tail -5 &amp;&amp; grep -q 'role="button"' src/components/Faq.astro &amp;&amp; grep -q 'aria-expanded={' src/components/Faq.astro &amp;&amp; grep -q 'aria-label="Breadcrumb"' src/components/Breadcrumbs.astro &amp;&amp; grep -q "import SiteNav" src/layouts/SiteLayout.astro &amp;&amp; grep -q "import SiteFooter" src/layouts/SiteLayout.astro &amp;&amp; grep -q "<MobileMenu" src/layouts/SiteLayout.astro &amp;&amp; npm run build:astro 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`src/components/Faq.astro` declares `interface FaqItem` and `interface Props` and uses `{item.a}` interpolation (no `set:html`).</criterion>
    <criterion>`src/components/Faq.astro` emits `class="faq-question" role="button" aria-expanded="false"` for items where `i !== startExpanded`.</criterion>
    <criterion>`src/components/Breadcrumbs.astro` emits `<nav class="breadcrumbs" aria-label="Breadcrumb">` and uses `aria-current="page"` for the final crumb.</criterion>
    <criterion>`src/layouts/SiteLayout.astro` imports `SiteNav`, `MobileMenu`, `LocationOverlay`, and `SiteFooter` from `../components/`.</criterion>
    <criterion>`src/layouts/SiteLayout.astro` `nav` slot fallback contains, in source order: `<MobileMenu`, `<SiteNav`, `<LocationOverlay` (verify: `awk` line numbers of those three tokens are strictly ascending).</criterion>
    <criterion>`src/layouts/SiteLayout.astro` `footer` slot fallback contains `<SiteFooter />`.</criterion>
    <criterion>`src/layouts/SiteLayout.astro` still contains exactly one `<TicketPanel` and one `<SiteScripts` (`grep -c '<TicketPanel\\|<SiteScripts' src/layouts/SiteLayout.astro` returns `2`).</criterion>
    <criterion>`npm run build:astro` exits 0.</criterion>
    <criterion>`npx astro check` exits 0.</criterion>
  </acceptance_criteria>
  <done>FAQ + Breadcrumb components and the layout-with-default-chrome contract are live; pages can adopt full chrome by importing only SiteLayout.</done>
</task>

</tasks>

<verification>
After all three tasks:
- `npx astro check` exits 0.
- `npm run build:astro` exits 0.
- `npm run check` still exits 0 (no legacy HTML touched).
- The seven listed files exist (six new components + edited SiteLayout).
- All preserved selectors from `<interfaces>` block exist verbatim in the rendered output (Plan 04-05 will add `check-ticket-panel-parity.js` and `check-component-usage.js` to enforce this dist-side; this plan validates source-side).
</verification>

<success_criteria>
- COMP-01 satisfied: nav, footer, location picker, FAQ, breadcrumb components exist as Astro components.
- COMP-02 contract preserved: every selector consumed by `js/nav.js`, `js/a11y.js`, and the smoke tests is present in component source.
- D-08 satisfied: navigation.json + footer.json + locations.ts feed the chrome; no duplicated literals.
- SiteLayout fallbacks let Plans 04-03/04-04 adopt full chrome with zero per-page boilerplate.
</success_criteria>

<output>
Create `.planning/phases/04-shared-components-template-parity/04-02-SUMMARY.md` with:
- Files created and the legacy line ranges they mirror.
- Confirmation that every preserved selector (table in `<interfaces>`) is present in source.
- Any deviation from legacy markup AND why (expected: zero).
- Open questions handed to Plans 04-03/04-04 (e.g. whether per-page nav-center variants need to be wired now).
</output>
