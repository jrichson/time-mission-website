---
phase: 04-shared-components-template-parity
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02-PLAN.md
files_modified:
  - scripts/sync-static-to-public.mjs
  - src/pages/about.astro
  - src/pages/faq.astro
  - src/pages/contact.astro
  - src/pages/privacy.astro
autonomous: true
requirements:
  - FND-02
  - COMP-01
  - COMP-02
  - COMP-04
must_haves:
  truths:
    - "scripts/sync-static-to-public.mjs skips legacy HTML copy for any route whose outputFile is rendered by Astro from src/pages, preventing public/ → dist/ overrides during build (Pitfall 5)."
    - "Four representative templates render through SiteLayout: about (marketing), faq (FAQ pattern), contact (contact pattern), privacy (policy/utility) — covering 4 of the 8 ROADMAP archetypes."
    - "Each Astro page emits the legacy title, canonical URL, og:url, and main body content of its corresponding legacy *.html (visible copy preserved per FND-02)."
    - "The contact page preserves the smoke contract: `<form class=\"contact-form\" method=\"post\" action=\"...\">` matching `/contact-thank-you|formspree|netlify` (tests/smoke/site.spec.js lines 64–66)."
    - "The faq page renders Faq.astro with at least 8 Q&A items derived from src/data/site/faqs.json (DATA-03 round-trip)."
  artifacts:
    - path: "scripts/sync-static-to-public.mjs"
      provides: "Updated sync that excludes migrated routes from legacy HTML copy"
    - path: "src/pages/about.astro"
      provides: "Marketing parity template (archetype #2)"
    - path: "src/pages/faq.astro"
      provides: "FAQ pattern (archetype #7a)"
    - path: "src/pages/contact.astro"
      provides: "Contact pattern (archetype #7b) preserving smoke-test form contract"
    - path: "src/pages/privacy.astro"
      provides: "Policy/utility/legal parity (archetype #8)"
  key_links:
    - from: "src/pages/about.astro"
      to: "src/layouts/SiteLayout.astro"
      via: "import + canonicalPath='/about'"
      pattern: "SiteLayout"
    - from: "src/pages/faq.astro"
      to: "src/components/Faq.astro + src/data/site/faqs.json"
      via: "import + .map() over sections"
      pattern: "faqs.json"
---

<objective>
Migrate four of the eight Phase 4 representative templates from legacy `*.html` to `src/pages/*.astro`, all using the Plan 04-02 SiteLayout. Update `scripts/sync-static-to-public.mjs` so it excludes Astro-rendered routes from the legacy HTML copy step (Pitfall 5 mitigation), preventing build conflicts during the migration window. Plan 04-04 owns the other 5 templates in parallel (it does NOT touch sync).

Purpose: Half of the COMP-04 representative set with the simpler / smaller pages first to prove the layout under realistic body content. Both this plan and 04-04 run in Wave 3 because their `files_modified` sets do not overlap.

Output: One sync script edit, four Astro pages. No legacy HTML deletion (kept at repo root for now — Phase 7/8 archives them).
</objective>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-04-11 | Tampering (Pitfall 5) | sync-static-to-public.mjs | mitigate | Skip-list filter excludes migrated outputFiles; sync no longer writes `public/about.html` etc., so Astro's `dist/about.html` is the sole producer |
| T-04-12 | Tampering (Pitfall 6) | All four .astro pages | mitigate | Use absolute `/assets/...`, `/css/...`, `/...` paths — no relative `../assets/...` |
| T-04-13 | Tampering (Pitfall 2) | All four .astro pages | mitigate | Page-local CSS migrated from legacy `<style>` blocks goes into `<style is:global>` in the `page-style` slot — never scoped, never `import './style.css'` |
| T-04-14 | Tampering (Pitfall 3) | All four .astro pages | mitigate | If a legacy `<style>` block contains the substring `<!-- Ticket Popup Panel -->` or other tooling markers, the comment must be preserved as HTML, not converted |
| T-04-15 | Tampering (XSS) | faq.astro | accept | Faq.astro uses `{value}` interpolation; faqs.json has been validated by `check-site-data.js` (no `<` / `>`) |
| T-04-16 | Tampering (form spoofing) | contact.astro | accept | The `<form>` tag's method/action are author-controlled and validated by smoke test regex; Phase 6 owns final form provider integration |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/04-shared-components-template-parity/04-CONTEXT.md
@.planning/phases/04-shared-components-template-parity/04-RESEARCH.md
@.planning/research/PITFALLS.md
@scripts/sync-static-to-public.mjs
@src/data/routes.json
@src/layouts/SiteLayout.astro
@src/components/Faq.astro
@src/components/Breadcrumbs.astro
@src/data/site/faqs.json
@about.html
@faq.html
@contact.html
@privacy.html
@tests/smoke/site.spec.js
@scripts/check-astro-dist-manifest.js

<interfaces>
<!-- Layout signature consumers MUST match -->
From src/layouts/SiteLayout.astro (Plan 04-01 + 04-02):
```typescript
interface Props {
  title: string;
  description: string;
  canonicalPath: string;
  ogImage?: string;
  twitterImage?: string;
  bodyDataLocation?: string;   // location slug, only on location pages — leave undefined for non-location pages
  bodyClass?: string;
}
```
Named slots: `head-extra`, `page-style`, `json-ld`, `pre-nav`, `nav` (default fills from chrome), `footer` (default fills SiteFooter), default (page main content).

From src/components/Faq.astro:
```typescript
interface FaqItem { q: string; a: string; }
interface Props { items: FaqItem[]; heading?: string; startExpanded?: number; }
```

Routes (from src/data/routes.json) — outputFile values for THIS plan's pages:
- `about` → `outputFile: "about.html"`, canonicalPath `/about`
- `faq` → `outputFile: "faq.html"`, canonicalPath `/faq`
- `contact` → `outputFile: "contact.html"`, canonicalPath `/contact`
- `privacy` → `outputFile: "privacy.html"`, canonicalPath `/privacy`

Plan 04-04 will own these additional outputFiles (this plan's sync skip-list MUST include them too):
- `home` → `index.html` (canonicalPath `/`)
- `locations` → `locations.html`
- `groups-corporate` → `groups/corporate.html`
- `philadelphia` → `philadelphia.html`
- `houston` → `houston.html`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skip migrated routes in sync-static-to-public.mjs</name>
  <files>scripts/sync-static-to-public.mjs</files>
  <read_first>
    <file>scripts/sync-static-to-public.mjs</file>
    <file>src/data/routes.json</file>
    <file>.planning/phases/04-shared-components-template-parity/04-RESEARCH.md</file>
  </read_first>
  <action>Edit `scripts/sync-static-to-public.mjs` to skip legacy HTML copy for routes that are migrated to Astro pages. Concrete change inside the `for (const route of routes)` loop (currently around lines 76–91):

1. Near the top of the file (after `const routes = routesJson.routes || [];`), add a constant:

```javascript
// Routes whose HTML is now produced by src/pages/*.astro under Phase 4.
// Each entry is the route's outputFile relative path (matches routes.json `outputFile`).
// When a route is in this set, sync MUST NOT copy a legacy HTML stub into public/ —
// Astro will emit it directly into dist/. Entries are added per migrated archetype.
const ASTRO_RENDERED_OUTPUT_FILES = new Set([
  'index.html',
  'about.html',
  'faq.html',
  'contact.html',
  'privacy.html',
  'locations.html',
  'groups/corporate.html',
  'philadelphia.html',
  'houston.html',
]);
```

The list includes BOTH this plan's four pages AND Plan 04-04's five pages so the sync filter is final after this commit and 04-04 can run in parallel without re-editing this file. The `index.html` slot is included because Plan 04-04 will replace the existing `src/pages/index.astro` placeholder with the real homepage.

2. Inside the `for (const route of routes)` loop, immediately after the `if (route.canonicalPath === '/') continue;` line, add:

```javascript
const outRel = route.outputFile.replace(/^\//, '');
if (ASTRO_RENDERED_OUTPUT_FILES.has(outRel)) continue;
```

Reason for hoisting `outRel` once: the existing code below also computes `route.outputFile.replace(/^\//, '')` for `destRel`. Replace the second computation by reusing `outRel`:

```javascript
const destRel = outRel;
```

3. Update the success log line at the bottom to mention skipped routes:

```javascript
const skippedHtml = routes.filter(r => ASTRO_RENDERED_OUTPUT_FILES.has(r.outputFile.replace(/^\//, ''))).length;
console.log(
  `Synced root static assets and ${copiedHtml} registered HTML routes from routes.json to public/ (skipped ${skippedHtml} Astro-rendered routes).`,
);
```

Rules:
- Do NOT delete legacy `*.html` from repo root in this task.
- Do NOT touch any route NOT in the skip-list — they continue to be copied as before.
- The `'/'` check stays separate (the homepage was already excluded). The skip-list `index.html` entry is defensive in case someone removes that early-return later.
- After editing, `npm run build:astro` MUST still succeed even if 04-04's pages do not yet exist (the skip just prevents copying — the missing Astro page would mean the route's dist file is missing, but that's a Plan 04-04 concern; this plan's four pages MUST result in their dist files existing).</action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('scripts/sync-static-to-public.mjs','utf8');for(const f of ['about.html','faq.html','contact.html','privacy.html','locations.html','groups/corporate.html','philadelphia.html','houston.html','index.html']){if(!s.includes(\"'\"+f+\"'\"))throw new Error('missing skip entry: '+f)};if(!s.includes('ASTRO_RENDERED_OUTPUT_FILES'))throw new Error('missing constant');console.log('sync skip-list OK');" &amp;&amp; npm run build:astro 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`scripts/sync-static-to-public.mjs` declares `const ASTRO_RENDERED_OUTPUT_FILES = new Set([...])` containing all 9 outputFile strings: `index.html`, `about.html`, `faq.html`, `contact.html`, `privacy.html`, `locations.html`, `groups/corporate.html`, `philadelphia.html`, `houston.html`.</criterion>
    <criterion>The route loop contains a `continue` guard that skips when `ASTRO_RENDERED_OUTPUT_FILES.has(outRel)`.</criterion>
    <criterion>`npm run build:astro` exits 0 (Astro can build with the new skip-list — note: at this point 04-04's pages may not yet exist, so check:astro-dist may fail for those — that's expected and a Wave 3 concurrency artifact, NOT a Plan 04-03 failure).</criterion>
    <criterion>After build: `dist/about.html` and `dist/faq.html` and `dist/contact.html` and `dist/privacy.html` are PRESENT (because Tasks 2+3 in this plan create the corresponding Astro pages).</criterion>
    <criterion>`public/about.html`, `public/faq.html`, `public/contact.html`, `public/privacy.html` are NOT created by sync (verify after `npm run build:astro`: `test ! -f public/about.html`).</criterion>
  </acceptance_criteria>
  <done>Sync no longer fights Astro for migrated routes; both this plan and 04-04 can run without conflict.</done>
</task>

<task type="auto">
  <name>Task 2: about.astro and privacy.astro (smaller marketing/legal templates)</name>
  <files>src/pages/about.astro, src/pages/privacy.astro</files>
  <read_first>
    <file>about.html</file>
    <file>privacy.html</file>
    <file>src/layouts/SiteLayout.astro</file>
    <file>src/data/routes.json</file>
  </read_first>
  <action>Create two Astro pages, each importing `SiteLayout` and migrating the body of the corresponding legacy HTML.

**File A — `src/pages/about.astro`**

Frontmatter:
```typescript
import SiteLayout from '../layouts/SiteLayout.astro';
```

Template body — `<SiteLayout>` invocation must use these exact prop values pulled from `about.html` `<head>`:
- `title`: literal `<title>` text from `about.html` (read the file first; do NOT invent)
- `description`: literal value of the existing `<meta name="description" content="...">`
- `canonicalPath`: `"/about"`
- `ogImage`: if `about.html` has a non-default `og:image`, pass it; otherwise omit (default in SiteHead handles it)

Body fill rules:
1. Default slot: copy the contents of `<body>...</body>` from `about.html` VERBATIM, MINUS:
   - The legacy `<div class="ticker-bar">`, `<div class="mobile-menu" id="mobileMenu">`, `<nav class="nav" id="nav">`, `<div class="location-dropdown" id="locationDropdown">` — these are now provided by SiteLayout's default `nav` slot.
   - The legacy `<footer>` block — provided by SiteLayout default `footer` slot.
   - The legacy `<!-- Ticket Popup Panel -->` block — provided by SiteLayout's `<TicketPanel />`.
   - The legacy five `<script src="js/...">` lines — provided by SiteLayout's `<SiteScripts />`.
   - Any legacy game popup overlay — if present, move it into the `pre-nav` slot fill.
2. Page-local CSS: extract the legacy `<style>...</style>` block(s) from `<head>` and place them inside a `<Fragment slot="page-style"><style is:global>...</style></Fragment>` block. The `is:global` directive is REQUIRED (Pitfall 2) because the legacy CSS targets unprefixed selectors. Do NOT reformat or "modernize" the CSS.
3. Page-specific JSON-LD: if `about.html` `<head>` contains a `<script type="application/ld+json">`, copy it verbatim into a `<Fragment slot="json-ld"><script type="application/ld+json" set:html={...} /></Fragment>` block. EXCEPTION to the no-`set:html` rule: JSON-LD strings are author-controlled JSON literals, not data interpolation; preserving the exact `>` and `"` characters is required for schema validity. Use a `const ld = \`{...JSON...}\`;` template literal in frontmatter and emit `<script type="application/ld+json" set:html={ld} />`.
4. Replace any relative asset path (`assets/...`, `css/...`, `js/...`) inside the body with absolute (`/assets/...`, `/css/...`, `/js/...`) — Pitfall 6.
5. Update any internal anchor `href="foo.html"` in body content to clean route `href="/foo"` (already validated by check-route-contract.js sources scan).

**File B — `src/pages/privacy.astro`**

Same pattern, but for `privacy.html`. Prop values:
- `title`: from privacy.html `<title>`
- `description`: from privacy.html meta description
- `canonicalPath`: `"/privacy"`

Same body migration rules (1–5 above).

Self-check after creating both files:
- `dist/about.html` and `dist/privacy.html` exist after `npm run build:astro`.
- Each contains `<!-- Ticket Popup Panel -->` and `id="ticketPanel"` (because SiteLayout includes `<TicketPanel />`).
- Each loads `/js/nav.js?v=4`, `/js/locations.js?v=9`, `/js/roller-checkout.js?v=1`, `/js/ticket-panel.js?v=4`, `/js/a11y.js`.
- Each contains the legacy hero/section markup unchanged (e.g. about page hero copy; privacy section headings).</action>
  <verify>
    <automated>npm run build:astro 2>&amp;1 | tail -10 &amp;&amp; test -f dist/about.html &amp;&amp; test -f dist/privacy.html &amp;&amp; grep -q '<!-- Ticket Popup Panel -->' dist/about.html &amp;&amp; grep -q 'id="ticketPanel"' dist/about.html &amp;&amp; grep -q '/js/ticket-panel.js?v=4' dist/about.html &amp;&amp; grep -q 'rel="canonical" href="https://timemission.com/about"' dist/about.html &amp;&amp; grep -q 'rel="canonical" href="https://timemission.com/privacy"' dist/privacy.html &amp;&amp; echo PAGES_AB_OK</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`src/pages/about.astro` imports `SiteLayout` and passes `canonicalPath="/about"`.</criterion>
    <criterion>`src/pages/privacy.astro` imports `SiteLayout` and passes `canonicalPath="/privacy"`.</criterion>
    <criterion>`npm run build:astro` exits 0.</criterion>
    <criterion>`dist/about.html` exists and contains `<title>` text taken from `about.html` (verify by extracting both titles and comparing — they must be equal).</criterion>
    <criterion>`dist/about.html` contains the literal substring `<!-- Ticket Popup Panel -->`.</criterion>
    <criterion>`dist/about.html` contains `<link rel="canonical" href="https://timemission.com/about">`.</criterion>
    <criterion>`dist/about.html` contains all five legacy script `src` URLs exactly: `/js/nav.js?v=4`, `/js/locations.js?v=9`, `/js/roller-checkout.js?v=1`, `/js/ticket-panel.js?v=4`, `/js/a11y.js`.</criterion>
    <criterion>`dist/privacy.html` contains `<link rel="canonical" href="https://timemission.com/privacy">`.</criterion>
    <criterion>Neither `dist/about.html` nor `dist/privacy.html` contains a `/_astro/` script URL (Pitfall 1 sentinel — `grep -q "/_astro/.*\\.js"` must FAIL on each file).</criterion>
    <criterion>`npm run check` continues to exit 0 (legacy HTMLs at repo root are intact).</criterion>
  </acceptance_criteria>
  <done>About and Privacy templates render through SiteLayout with full chrome and parity head/body.</done>
</task>

<task type="auto">
  <name>Task 3: faq.astro and contact.astro (FAQ + contact pattern)</name>
  <files>src/pages/faq.astro, src/pages/contact.astro</files>
  <read_first>
    <file>faq.html</file>
    <file>contact.html</file>
    <file>src/components/Faq.astro</file>
    <file>src/data/site/faqs.json</file>
    <file>tests/smoke/site.spec.js</file>
    <file>src/layouts/SiteLayout.astro</file>
  </read_first>
  <action>Create two Astro pages.

**File A — `src/pages/faq.astro`**

Frontmatter:
```typescript
import SiteLayout from '../layouts/SiteLayout.astro';
import Faq from '../components/Faq.astro';
import faqs from '../data/site/faqs.json';
```

Template:
```astro
<SiteLayout
  title={/* from faq.html */}
  description={/* from faq.html meta description */}
  canonicalPath="/faq"
>
  <main class="faq-main">
    {faqs.sections.map((section) => (
      <section class="faq-section" id={section.id}>
        <Faq heading={section.heading} items={section.items} />
      </section>
    ))}
  </main>

  <Fragment slot="page-style">
    <style is:global>
      /* Legacy <style> block from faq.html migrated verbatim. */
    </style>
  </Fragment>
</SiteLayout>
```

Migration rules (same as Task 2 except replace the body's accordion list with the `Faq.astro` rendered output):
- Title and description copied from `faq.html` verbatim.
- Migrate page-local `<style>` block from `faq.html` `<head>` into the `page-style` slot, wrapped in `<style is:global>`.
- The legacy faq accordion DOM (each `<button class="faq-question" role="button" aria-expanded="false">…</button>`) is now rendered by `Faq.astro`. The smoke test assertion (line 53–58) tests `.faq-question` first item — Faq.astro emits this contract.
- Surround the `<Faq>` calls with whatever section/header markup the legacy page uses (e.g. `<section class="faq-hero">`, hero copy block, etc.) so the page chrome above the accordion is preserved.
- Do NOT data-migrate FAQ Q/A text — `faqs.json` already contains it (Phase 3 Plan 03-03). If `faq.html` has additional Q/As not in `faqs.json`, file a follow-up note in 04-03-SUMMARY rather than editing JSON in this plan.

**File B — `src/pages/contact.astro`**

Frontmatter:
```typescript
import SiteLayout from '../layouts/SiteLayout.astro';
```

Template:
- `canonicalPath="/contact"`.
- Default slot: copy `contact.html` `<body>` minus chrome, ticket panel, scripts (same rules as Task 2).
- The page MUST contain a `<form class="contact-form" method="post" action="...">` element. The `action` attribute MUST match the smoke test regex `/\/contact-thank-you|formspree|netlify/i`. If `contact.html` currently uses an action that matches that regex, copy it verbatim. If not, set `action="/contact-thank-you"` (the canonical clean route registered as `outputFile: "contact-thank-you.html"`).
- All form input names + labels copied verbatim.
- Page-local CSS migrated to `page-style` slot via `<style is:global>`.
- Migrate any legacy `<script type="application/ld+json">` to `json-ld` slot.

Critical contract checks for contact.astro (these mirror tests/smoke/site.spec.js lines 61–67):
- `<form class="contact-form" ...>` selector must exist.
- `method="post"` (case-insensitive — legacy may use lowercase `post`).
- `action` matches `/\/contact-thank-you|formspree|netlify/i`.</action>
  <verify>
    <automated>npm run build:astro 2>&amp;1 | tail -10 &amp;&amp; test -f dist/faq.html &amp;&amp; test -f dist/contact.html &amp;&amp; grep -q 'class="faq-question" role="button" aria-expanded="false"' dist/faq.html &amp;&amp; grep -qE 'class="contact-form"[^>]*method="post"' dist/contact.html &amp;&amp; grep -qE 'action="[^"]*(contact-thank-you|formspree|netlify)' dist/contact.html &amp;&amp; grep -q 'rel="canonical" href="https://timemission.com/faq"' dist/faq.html &amp;&amp; grep -q 'rel="canonical" href="https://timemission.com/contact"' dist/contact.html &amp;&amp; echo PAGES_FC_OK</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`src/pages/faq.astro` imports `Faq` from `../components/Faq.astro` and `faqs` from `../data/site/faqs.json`.</criterion>
    <criterion>`src/pages/faq.astro` renders one `<section>` per `faqs.sections` entry via `.map()`.</criterion>
    <criterion>`dist/faq.html` contains AT LEAST 8 occurrences of `class="faq-question"` (the count of items in faqs.json).</criterion>
    <criterion>`dist/faq.html` first `.faq-question` matches `class="faq-question" role="button" aria-expanded="false"` (smoke test line 53–55 contract).</criterion>
    <criterion>`dist/contact.html` contains `<form class="contact-form" method="post" action="...">` where action matches `/\/contact-thank-you|formspree|netlify/i`.</criterion>
    <criterion>`dist/contact.html` and `dist/faq.html` each contain `<!-- Ticket Popup Panel -->` and the five legacy script tag URLs.</criterion>
    <criterion>Neither file contains a `/_astro/` URL.</criterion>
    <criterion>`npm run check:astro-dist` exits 0 (faq.html and contact.html and about.html and privacy.html are not on the manifest's MUST list yet — that's Plan 04-05 — but their absence does NOT cause check failure either).</criterion>
    <criterion>`npm run build:astro &amp;&amp; npm run check:routes -- --dist` exits 0 (every route's outputFile must exist; this plan provides 4 of the 9; Plan 04-04 provides the rest; if 04-04 has not yet run, this single criterion may not pass — record as expected Wave 3 race condition and re-run after 04-04 merges).</criterion>
  </acceptance_criteria>
  <done>FAQ + contact templates render through SiteLayout, preserve smoke contract, and consume Phase 3 data for FAQ content.</done>
</task>

</tasks>

<verification>
After all three tasks:
- `npm run build:astro` exits 0.
- `npm run check` exits 0 (legacy HTMLs intact at repo root, no source-side regressions).
- `dist/about.html`, `dist/faq.html`, `dist/contact.html`, `dist/privacy.html` all exist and pass per-page checks above.
- `public/about.html` etc. are NOT created during sync (skip-list active).

Wave 3 race condition note: when this plan and 04-04 are running in parallel, intermediate states may have some `dist/*.html` files missing because the corresponding `src/pages/*.astro` is being authored. `npm run check:routes -- --dist` may fail until BOTH plans complete. That is acceptable; full `verify:phase4` runs in Plan 04-05 Wave 4.
</verification>

<success_criteria>
- 4 of 8 representative templates ship through Astro: about, faq, contact, privacy.
- COMP-04 satisfied for archetypes: marketing, FAQ, contact, policy/utility.
- D-08 round-trip proven: faq.astro consumes faqs.json end-to-end (DATA-03 → COMP-04).
- Sync-static-to-public.mjs no longer collides with Astro's emission for any of the 9 migrated routes (this plan's 4 + Plan 04-04's 5).
- FND-02 visual parity: title, description, canonical, body content, page-local CSS preserved.
- Smoke test contract for `tests/smoke/site.spec.js` lines 50–67 (FAQ accordion + contact form) preserved in dist output.
</success_criteria>

<output>
Create `.planning/phases/04-shared-components-template-parity/04-03-SUMMARY.md` with:
- Files created (paths + which legacy HTML they migrated).
- Confirmation that the sync skip-list contains all 9 outputFiles.
- Diff highlights between legacy and Astro output for one page (e.g. `diff <(curl http://localhost:4173/about.html) <(curl http://localhost:4173/about)` if applicable, or simply key strings preserved).
- Any FAQ Q/As that exist in `faq.html` but NOT in `faqs.json` (handed forward as a Phase 3 follow-up).
- Any deviation from legacy markup AND why.
</output>
