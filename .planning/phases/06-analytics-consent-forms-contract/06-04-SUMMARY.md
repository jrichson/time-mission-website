# Plan 06-04 — Summary

**Done:** `ticket-panel.js`: `booking_click`, `checkout_start`, `ticket_panel_open/close`, dropdown `location_select`. `locations.js` `TM.select` → `location_select`. `nav.js` overlay picks → `location_select` with `cta_id: nav_location_overlay`.
