---
phase: 06-analytics-consent-forms-contract
plan: 05
wave: 4
dependencies: ['06-01-PLAN.md', '06-02-PLAN.md', '06-03-PLAN.md', '06-04-PLAN.md']
requires_todo_write: false
---

# Plan 05 — Docs, launch checklist, `verify:phase6`, ROADMAP

## Objective

Meet **ANLY-06**: extend **`docs/roller-booking-launch-checklist.md`** with **cross-domain / iframe / PII** analytics steps; add **`docs/gtm-operator-runbook.md`** (container wiring, Preview, Consent Mode test); add **`npm run verify:phase6`**; update **ROADMAP** Phase 6 completion checklist; produce **`06-*-SUMMARY.md`** after execution waves.

## Preconditions

- Plans 01–04 implemented or **stubbed** as specified.

## Tasks

### Task 5.1 — `docs/gtm-operator-runbook.md`

**Goal:** Operator steps: publish container, **Consent Mode** test in Tag Assistant, **GA4** DebugView, **staging** vs **production** IDs, **no PII** reminder.

**Files:**

- `docs/gtm-operator-runbook.md`

**verify:**

```bash
test -f docs/gtm-operator-runbook.md
```

### Task 5.2 — Extend `docs/roller-booking-launch-checklist.md`

**Goal:** Add section **Analytics & consent** covering **iframe** measurement limitations, **cross-domain** linker (reference ROLLER docs), **purchase** event expectations.

**Files:**

- `docs/roller-booking-launch-checklist.md`

**verify:**

```bash
rg -n "Analytics|consent|cross-domain" docs/roller-booking-launch-checklist.md
```

### Task 5.3 — `verify:phase6` in `package.json`

**Goal:** Script runs: `npm run check` (includes `check:analytics`), `npm run build:astro`, existing dist parity checks as for phase 5 **if applicable**, `npm run test:smoke`.

**read_first:**

- `package.json` (`verify:phase5` pattern)

**Files:**

- `package.json`

**verify:**

```bash
npm run verify:phase6
```

### Task 5.4 — ROADMAP + phase summaries

**Goal:** Mark Phase 6 plans in `ROADMAP.md`; after execution, add **`06-01-SUMMARY.md` … `06-05-SUMMARY.md`** and **`06-VERIFICATION.md`**.

**Files:**

- `.planning/ROADMAP.md`
- `.planning/phases/06-analytics-consent-forms-contract/`

**verify:**

```bash
gsd-tools plan check 6 || true
```

## Wave completion

- [ ] Commit: `docs(06): gtm runbook verify phase6 roadmap`

## Requirements addressed

- **ANLY-06**, project verification gate
