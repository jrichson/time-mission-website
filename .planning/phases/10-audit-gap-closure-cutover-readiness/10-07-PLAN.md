---
phase: 10
plan: 07
type: execute
wave: 6
depends_on: [10-02, 10-03, 10-04, 10-05, 10-06]
files_modified:
  - docs/cutover-checklist.md
  - package.json
  - .planning/ROADMAP.md
  - .planning/STATE.md
addresses: [P2-6, P1-7, P1-10, P1-11, P1-17, P2-8, P2-9, P2-10]
requirements_addressed: [VER-05, VER-06]
autonomous: true
must_haves:
  truths:
    - "docs/cutover-checklist.md exists and contains a row for each of the 7 host items: P1-7 (Cloudflare case-insensitive), P1-10 (ROLLER/Experience Factory brand), P1-11 (FB+TikTok pixel domain), P1-17 (tickets.timemission.com DNS+CSP), P2-8 (share-image.jpg compression), P2-9 (brochure.pdf compression), P2-10 (api-1 CORS narrowing)"
    - "Each host item row has Owner, Verification Step, and a checkbox"
    - "docs/cutover-checklist.md includes the Code Changes section listing every check:* validator added in Phase 10 (check:img-alt-axe, check:hreflang-cluster, check:tap-targets) plus the existing verify chain"
    - "docs/cutover-checklist.md documents P1-12 hero video locked decision (decorative, aria-hidden) AND P2-6 brand compliance manual review item"
    - "package.json has a verify:phase10 script that runs the full verify chain plus the three Phase 10 validators"
    - "ROADMAP.md Phase 10 section is updated to reflect 7 effective plans, status complete, and which findings are closed by which plan"
    - "STATE.md is updated to mark Phase 10 complete"
  artifacts:
    - path: "docs/cutover-checklist.md"
      provides: "Pre-cutover gate documenting host dependencies + automated checks"
      contains: "P1-17"
    - path: "package.json"
      provides: "verify:phase10 script alias"
      contains: "verify:phase10"
  key_links:
    - from: "docs/cutover-checklist.md"
      to: "docs/rollback-runbook.md"
      via: "cross-link"
      pattern: "rollback-runbook"
    - from: "package.json verify:phase10"
      to: "all Phase 10 check:* scripts"
      via: "npm run chain"
      pattern: "check:img-alt-axe|check:hreflang-cluster|check:tap-targets"
---

<objective>
Create the pre-cutover gate documents and verify-chain alias for Phase 10 close. Build `docs/cutover-checklist.md` capturing every host/external dependency (P1-7, P1-10, P1-11, P1-17, P2-8, P2-9, P2-10) with owner + verification steps. Add the manual brand compliance review (P2-6) as a documented item against `assets/mockup-reference/`. Wire `verify:phase10` script into `package.json`. Update `ROADMAP.md` and `STATE.md` to reflect Phase 10 closure.

Purpose: Closes audit P2-6 (brand compliance manual review) and converts the 7 host/external items from the audit into trackable cutover-runbook entries. The verify alias gives operators one command to run before launch.

Output: One canonical cutover doc + one npm verify alias + roadmap/state bookkeeping. No code changes beyond documentation and `package.json` scripts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10-audit-gap-closure-cutover-readiness/10-CONTEXT.md
@.planning/phases/10-audit-gap-closure-cutover-readiness/10-RESEARCH.md
@docs/rollback-runbook.md
@docs/cloudflare-preview-validation.md
@docs/roller-booking-launch-checklist.md
@_headers
@package.json

<interfaces>
<!-- Locked decision D-04 (CONTEXT.md item 5): -->
<!--   P1-17 tickets.timemission.com CSP — HOST ACTION ITEM (DevOps); -->
<!--   Code unchanged in Phase 10; record in cutover checklist only. -->

<!-- The 7 host items from CONTEXT.md "External / host dependencies" table: -->
<!--   P1-7  (Web Dev)            Cloudflare case-insensitive routing -->
<!--   P1-10 (Product + Brand)    ROLLER / Experience Factory brand strategy -->
<!--   P1-11 (GTM Admin)          FB & TikTok pixel domain=.timemission.com fix -->
<!--   P1-17 (DevOps)             tickets.timemission.com DNS + CSP allowlist -->
<!--   P2-8  (Designer)           share-image.jpg 586KB → ~150KB compress -->
<!--   P2-9  (Designer + WebDev)  brochure.pdf 11.7MB → ~2-3MB compress -->
<!--   P2-10 (DevOps)             api-1.timemission.com CORS narrow if PII-bound -->

<!-- Phase 10 plans 10-02/10-04/10-06 added these check:* scripts: -->
<!--   check:img-alt-axe         (10-02) -->
<!--   check:hreflang-cluster    (10-04) -->
<!--   check:tap-targets         (10-06) -->

<!-- Existing verify alias pattern (from package.json): -->
<!--   verify:phase8: "npm run verify" -->
<!--   verify:phase7: "npm run verify" -->
<!-- For verify:phase10, the chain MUST include the three new validators explicitly so the -->
<!-- alias is meaningful for cutover (even if verify-site-output.mjs already runs them). -->

<!-- ROADMAP.md Phase 10 currently lists 9 suggested plans (lines 290-298). -->
<!-- After Phase 10 close: collapse to 7 effective plans (10-01 through 10-07) -->
<!-- with Done checkboxes and SUMMARY links. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create docs/cutover-checklist.md with all host items + automated checks + manual reviews</name>
  <files>docs/cutover-checklist.md</files>

  <read_first>
    - .planning/phases/10-audit-gap-closure-cutover-readiness/10-CONTEXT.md "External / host dependencies" table (the 7 owner items)
    - .planning/phases/10-audit-gap-closure-cutover-readiness/10-RESEARCH.md "Cutover Runbook Outline" lines 456-503 (the structure to follow)
    - docs/rollback-runbook.md (cross-link target — the cutover doc must reference it)
    - docs/cloudflare-preview-validation.md (cross-link target for P1-7)
    - docs/roller-booking-launch-checklist.md (cross-link target for P1-10/P1-11)
    - assets/mockup-reference/ (verify directory exists for P2-6 brand compliance review)
  </read_first>

  <action>
    Create `docs/cutover-checklist.md`. Use Markdown. The document MUST contain these sections in order, with verbatim header text:

    ## Section 1 — Frontmatter

    Top of doc:
    ```markdown
    # Time Mission Astro Migration — Cutover Checklist

    Status: in progress
    Origin: Phase 10 plan 10-07 (Audit-Gap Closure & Cutover Readiness)
    Cross-links:
    - [docs/rollback-runbook.md](./rollback-runbook.md)
    - [docs/cloudflare-preview-validation.md](./cloudflare-preview-validation.md)
    - [docs/roller-booking-launch-checklist.md](./roller-booking-launch-checklist.md)

    > Audit findings closed by Phase 10 code changes are documented in the SUMMARY files
    > under `.planning/phases/10-audit-gap-closure-cutover-readiness/`. This document covers
    > items that ship outside the Astro repo: host config, GTM container changes, DNS,
    > asset compression, and brand sign-off.
    ```

    ## Section 2 — Code Changes (auto-verified)

    Include a table listing every Phase 10 validator + the gate it enforces:
    ```markdown
    ## Code Changes (auto-verified by `npm run verify:phase10`)

    | Check | Gate | Closed Finding(s) |
    |-------|------|-------------------|
    | `npm run check:schema-output` | All open-location LocalBusiness JSON-LD declares `name="Time Mission [City]"`; Antwerp emits `alternateName` | P0-5, P0-8 (already shipped via quick 260504-lsk) |
    | `npm run check:img-alt-axe` | Zero critical/serious axe image-alt violations across 21 dist HTML files | P0-4 |
    | `npm run check:hreflang-cluster` | Antwerp dist emits `<html lang="nl-BE">`; US pages emit `<html lang="en">`; no cross-city `<link rel="alternate" hreflang>` (locked D-02) | P1-9 |
    | `npm run check:tap-targets` | Nav buttons declare `min-height >= 44px`; booking CTAs declare `min-height >= 48px` (CSS-source heuristic) | P1-1 |
    | `npm run check:a11y` (extended) | Every Astro page has SSR skip-link + `<main id="main">` | P1-3, P0-4 partial |
    | `npm run check:astro-dist` | All 21 Astro pages render (incl. 6 legal pages from plan 10-03 + antwerp from plan 10-04) | P1-16 |
    | Mobile Playwright project | `tap` on location-link keeps overlay open + reveals info panel | P0-7a |
    | Manual visual confirmation | Hero video on `index.html` is `aria-hidden="true" tabindex="-1"` (locked D-01: decorative) | P1-12 |
    | Manual GTM DebugView | `web_vitals` event with LCP/CLS/INP visible in dataLayer on at least homepage + one location page after consent grant | P2-1 |
    | Manual visual smoke | Cookie banner appears on `dist/antwerp.html`, absent on `dist/philadelphia.html` (locked D-03 EU-only) | P2-4 |
    ```

    ## Section 3 — Host Dependencies

    Table with all 7 items — copy values verbatim from CONTEXT.md "External / host dependencies":
    ```markdown
    ## Host Dependencies (human-verified, NOT automated)

    | Finding | Owner | Action | Verification Step | Status |
    |---------|-------|--------|-------------------|--------|
    | P1-7 | Web Dev | Cloudflare case-insensitive routing | Follow `docs/cloudflare-preview-validation.md` — test `/FAQ`, `/Philadelphia` (mixed case) → expect 200 | [ ] not started |
    | P1-10 | Product + Brand | ROLLER / Experience Factory brand strategy decision | Confirm whether "Experience Factory" surfaces anywhere on-site post-cutover (e.g., legacy schema fields, copy). Document decision. | [ ] not started |
    | P1-11 | GTM Admin | FB & TikTok pixel `domain=.timemission.com` | Fix domain attribute in GTM container (NOT in repo); verify in GTM Preview before publish | [ ] not started |
    | P1-17 | DevOps | `tickets.timemission.com` DNS + CSP allowlist | (a) Deploy subdomain DNS+TLS, OR (b) migrate Philadelphia + West Nyack + Lincoln booking URLs from `tickets.timemission.com` to `ecom.roller.app` first, then remove from `_headers` CSP `frame-src`/`connect-src`. CSP currently retains the entry — booking would break if removed prematurely. | [ ] not started |
    | P2-8 | Designer | Re-export `share-image.jpg` (586 KB → ~150 KB) | Compressed file delivered; replace in `assets/`; rebuild dist | [ ] not started |
    | P2-9 | Designer + Web Dev | Compress `brochure.pdf` (11.7 MB → ~2–3 MB) | Compressed PDF delivered; replace in `assets/` | [ ] not started |
    | P2-10 | DevOps | `api-1.timemission.com` open CORS | If PII-bound, narrow `Access-Control-Allow-Origin` to `https://timemission.com`; if not, document acceptance | [ ] not started |
    ```

    ## Section 4 — Manual Reviews (P2-6 brand compliance + P1-12 hero verification)

    ```markdown
    ## Manual Reviews

    ### P2-6 — Brand Compliance Review

    **Owner:** Designer
    **Method:** Open representative Astro pages in browser (`dist/index.html`, `dist/philadelphia.html`, `dist/antwerp.html`, `dist/groups/birthdays.html`, `dist/terms.html`, `dist/contact.html`) and compare against `assets/mockup-reference/` screenshots and brand-dna folder.

    **Pass criteria:**
    - [ ] Color palette matches brand tokens (orange accent, dark surfaces; see `css/variables.css`)
    - [ ] Typography matches brand (Bebas Neue display, DM Sans body, Orbitron tech)
    - [ ] Imagery matches mockup reference (no missing photos, no broken assets)
    - [ ] Hero treatment matches reference (video on index/philadelphia; image elsewhere)
    - [ ] Spacing rhythm matches reference (no obvious gap regressions)

    **Resume:** Designer signs off OR files specific defect items in `STATE.md` blockers.

    ### P1-12 — Hero Video Decorative (locked decision D-01)

    **Owner:** Web Dev (verification only — decision is locked to "decorative")

    **Verification:** `grep 'aria-hidden="true" tabindex="-1"' src/partials/index-main.frag.txt | grep "heroVideo"` returns the `<video id="heroVideo">` line. Confirm via `dist/index.html` after build.

    - [ ] Confirmed: hero video shipped with `aria-hidden="true" tabindex="-1"`
    ```

    ## Section 5 — Final Pre-Cutover Sequence

    ```markdown
    ## Final Pre-Cutover Sequence

    1. Run `npm run verify:phase10` — must exit 0.
    2. All Host Dependencies above either Done or explicitly Deferred (with rationale logged in `STATE.md`).
    3. P2-6 brand compliance review signed off (or specific defects logged + resolved).
    4. Run `docs/cloudflare-preview-validation.md` checklist on a real Cloudflare Pages preview deployment.
    5. Confirm GTM DebugView shows `web_vitals` events on homepage + one location page after consent grant.
    6. Confirm cookie banner appears on Antwerp; absent on Philadelphia (locked D-03 EU-only).
    7. Final rollback sanity: confirm `docs/rollback-runbook.md` is current and the `pre-astro-migration-baseline` git tag exists.
    8. Cutover.
    ```

    Save and commit the document.
  </action>

  <verify>
    <automated>test -f docs/cutover-checklist.md &amp;&amp; for tag in P1-7 P1-10 P1-11 P1-17 P2-8 P2-9 P2-10 P2-6 P1-12 verify:phase10 rollback-runbook.md cloudflare-preview-validation.md; do grep -q "$tag" docs/cutover-checklist.md || { echo "MISSING tag: $tag"; exit 1; }; done; echo OK</automated>
  </verify>

  <acceptance_criteria>
    - `docs/cutover-checklist.md` exists
    - Document contains a row or section for ALL 7 host items: `P1-7`, `P1-10`, `P1-11`, `P1-17`, `P2-8`, `P2-9`, `P2-10`
    - Each host item row has Owner, Action, Verification Step, and a checkbox `- [ ]`
    - Document contains the Code Changes section listing `check:img-alt-axe`, `check:hreflang-cluster`, `check:tap-targets`, `check:schema-output`, mobile Playwright project
    - Document contains a `## Manual Reviews` section with `### P2-6 — Brand Compliance Review` (referencing `assets/mockup-reference/`) AND `### P1-12 — Hero Video Decorative` (referencing locked D-01)
    - Document cross-links to `docs/rollback-runbook.md`, `docs/cloudflare-preview-validation.md`, `docs/roller-booking-launch-checklist.md`
    - Document contains the literal text `verify:phase10` (the npm script alias from Task 2)
    - Document contains the locked decision identifiers `D-01`, `D-02`, `D-03` referenced where applicable
  </acceptance_criteria>

  <done>
    Cutover checklist exists with all 7 host owners, all auto-verified checks listed, all manual reviews documented, and all cross-links present.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add verify:phase10 script to package.json</name>
  <files>package.json</files>

  <read_first>
    - package.json (current `verify` chain — note: `verify-site-output.mjs` already runs `check:img-alt-axe`, `check:hreflang-cluster` per plans 10-02/10-04 wiring; `check:tap-targets` may have been added to `check` chain or `verify` chain in plan 10-06 — read package.json to see where it landed)
    - existing `verify:phase8` script for the alias pattern: `"verify:phase8": "npm run verify"`
  </read_first>

  <action>
    Edit `package.json`. Add a new script entry `"verify:phase10"` after the existing `verify:phase7` line. The script must run the full verify chain. Two acceptable forms (pick whichever matches the project's pattern by comparing to verify:phase4/5/6/7/8):

    **Form A (alias-only — preferred if all Phase 10 validators are already in verify-site-output.mjs):**
    ```json
    "verify:phase10": "npm run verify"
    ```

    **Form B (explicit — use only if any Phase 10 validator is NOT in the default verify chain):**
    ```json
    "verify:phase10": "npm run verify && npm run check:img-alt-axe && npm run check:hreflang-cluster && npm run check:tap-targets"
    ```

    **Decision rule:**
    - Read `scripts/verify-site-output.mjs` `steps` array. If all three (`check:img-alt-axe`, `check:hreflang-cluster`, `check:tap-targets`) appear there OR in the `check` chain in `package.json`, use Form A.
    - If any is missing, use Form B (or fix the underlying chain — but Form B is safer for cutover documentation since the alias is self-documenting).

    Run `npm run verify:phase10` end-to-end after editing to confirm it exits 0.
  </action>

  <verify>
    <automated>node -e 'const p=require("./package.json"); if(!p.scripts["verify:phase10"]){console.error("FAIL: missing verify:phase10");process.exit(1);} console.log("PASS: " + p.scripts["verify:phase10"]);' &amp;&amp; npm run verify:phase10</automated>
  </verify>

  <acceptance_criteria>
    - `package.json` `scripts.verify:phase10` is defined and is a non-empty string
    - The script runs (at minimum) `npm run verify` — verify by reading the value
    - `npm run verify:phase10` exits 0 end-to-end (this is the gate command for cutover)
    - The script ordering in `package.json` places `verify:phase10` adjacent to other `verify:phase*` aliases (alphabetical by phase number)
  </acceptance_criteria>

  <done>
    `verify:phase10` is the canonical pre-cutover gate command; runs full verify chain; passes against current dist.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update ROADMAP.md and STATE.md to reflect Phase 10 closure</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>

  <read_first>
    - .planning/ROADMAP.md current Phase 10 section (lines 280-311) — currently lists 9 suggested plans
    - .planning/STATE.md current frontmatter (`phase_10_plans: 0`, `phase_10_status: not_planned`)
    - Each Phase 10 plan summary file (10-01-SUMMARY.md through 10-07-SUMMARY.md) — verify they exist (they will, since plan-execute creates them)
    - Existing pattern for completed phases (e.g., Phase 8/9 listings in ROADMAP.md)
  </read_first>

  <action>
    1. **Edit `.planning/ROADMAP.md` Phase 10 section.**

       a) In the top-of-file phase list (around line 15-23), change the Phase 10 entry to `[x]` Complete with date `2026-05-04` (or the actual completion date when this task runs):
       ```markdown
       - [x] **Phase 10: Audit-Gap Closure & Cutover Readiness** (2026-05-XX) - Every P0/P1 audit finding shipped, deferred with rationale, or recorded as a host action in `docs/cutover-checklist.md`.
       ```

       b) In the Phase 10 detail section (around line 280-311), update:
       - **Plans:** change `0/9` to `7/7` and status to `Complete`
       - **Plan list:** replace the suggested 9-plan list with the actual 7 plans that shipped:
         ```markdown
         Plans:
         - [x] 10-01-PLAN.md — SSR landmarks + skip-link + decorative SVG/video aria-hidden sweep (P1-3, P0-4 partial, P1-12)
         - [x] 10-02-PLAN.md — axe `dist` scan (`check:img-alt-axe`) + Playwright mobile project + P0-7a fix (P0-4, P0-7a)
         - [x] 10-03-PLAN.md — 6 legal page Astro migrations with shared `legal-inline.raw.css.txt` partial (P1-16)
         - [x] 10-04-PLAN.md — Per-route `lang` prop + `antwerp.astro` migration + `check-hreflang-cluster` (P1-9)
         - [x] 10-05-PLAN.md — Hero medium audit + `<picture>`/srcset for confirmed `<img>` heroes + `web-vitals@5` RUM beacon (P1-18, P2-1)
         - [x] 10-06-PLAN.md — Cookie banner (`vanilla-cookieconsent@3`, EU-routed) + `check-tap-targets` validator (P2-4, P1-1)
         - [x] 10-07-PLAN.md — `docs/cutover-checklist.md` + `verify:phase10` + brand compliance pass (P2-6 + 7 host items)
         ```
       - **Wave summary:** replace the original 7-wave list with the actual delivered waves:
         ```markdown
         Wave summary:
         - Wave 1 (parallel): 10-01 SSR landmarks, 10-02 axe + mobile project
         - Wave 2: 10-03 legal pages
         - Wave 3: 10-04 lang attribute + antwerp.astro
         - Wave 4: 10-05 hero srcset + RUM
         - Wave 5: 10-06 cookie banner + tap-targets
         - Wave 6: 10-07 cutover checklist + verify:phase10
         ```
       - **Note about closed gaps:** add a note that the original suggested 10-01 (Antwerp schema rename) and 10-02 (legacy redirects) were closed pre-planning by quick-tasks 260504-lsk and 260504-mly.

       c) In the Coverage table (around line 269), update Phase 10 row to `7/7 | Complete | 2026-05-XX`.

    2. **Edit `.planning/STATE.md`.**

       a) Update frontmatter:
       ```yaml
       milestone: v1.0
       last_updated: "<ISO timestamp>"
       last_activity: "2026-05-XX — Phase 10 (Audit-Gap Closure & Cutover Readiness) closed: 7 plans shipped, 7 host items recorded in docs/cutover-checklist.md, verify:phase10 alias wired."
       progress:
         total_phases: 10
         completed_phases: 10
         total_plans: 47   # was 40 + 7 Phase 10 plans
         completed_plans: 47
         phase_10_plans: 7
         phase_10_status: complete
         percent_v1_core: 100
       ```

       b) Add a Phase 10 entry to the "By Phase" table:
       ```markdown
       | 10. Audit-Gap Closure & Cutover Readiness | 7 | Complete | — |
       ```

       c) Update the "Recent trend" section to summarize Phase 10 outcomes (validators added, banner shipped, antwerp migrated, host items captured).

       d) Update "Pending todos":
       - REMOVE "Phase 10 planning: Run `/gsd-plan-phase 10`..."
       - REMOVE "Brand decision blocking 10-04: P1-12 hero video..." (locked decision D-01 resolved this)
       - ADD "Cutover: `npm run verify:phase10` must pass before launch" with cross-link to `docs/cutover-checklist.md`
       - Existing "Human: Rehearse `docs/cloudflare-preview-validation.md`..." stays
       - Existing "Human: Confirm ROLLER/GTM validation..." stays
       - ADD entries for P1-17, P2-8, P2-9, P2-10 host action items (one bullet each)

       e) Update "Session continuity": "Phase 10 closed. Cutover gate is `npm run verify:phase10` + `docs/cutover-checklist.md` host items + `docs/rollback-runbook.md` rehearsal."

    3. **Commit both file edits in a single docs commit** (per existing repo pattern of grouping ROADMAP+STATE updates).
  </action>

  <verify>
    <automated>grep -q "phase_10_status: complete" .planning/STATE.md &amp;&amp; grep -q "10-07-PLAN.md" .planning/ROADMAP.md &amp;&amp; grep -q "verify:phase10" .planning/STATE.md</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/ROADMAP.md` Phase 10 entry in the top phase list shows `[x]` Complete
    - `.planning/ROADMAP.md` Phase 10 detail section lists exactly 7 plans (10-01 through 10-07) all marked `[x]`
    - `.planning/ROADMAP.md` Phase 10 wave summary references the actual 6 execution waves
    - `.planning/ROADMAP.md` Coverage table Phase 10 row shows `7/7 Complete`
    - `.planning/STATE.md` frontmatter `phase_10_plans: 7`, `phase_10_status: complete`, `total_plans: 47`, `completed_plans: 47`
    - `.planning/STATE.md` "Pending todos" no longer mentions "Phase 10 planning" or "Brand decision blocking 10-04"
    - `.planning/STATE.md` "Pending todos" includes the cutover-readiness reminder pointing to `docs/cutover-checklist.md`
    - `.planning/STATE.md` "By Phase" table has a Phase 10 row marked Complete
    - Both files end with a single trailing newline (no editor garbage)
  </acceptance_criteria>

  <done>
    Roadmap and state files reflect Phase 10 closure with accurate plan list, wave summary, and updated todos pointing operators to the cutover checklist + verify alias.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → operator (cutover doc) | Document content is informational guidance for human operators |
| repo → CI (verify:phase10) | npm script alias chains existing validators |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07-01 | Information Disclosure | `docs/cutover-checklist.md` | accept | Document is checked into a public repo; references public host items (Cloudflare, GTM, DNS owners). No secrets, credentials, or PII. Owner names refer to roles, not individuals. |
| T-10-07-02 | Tampering | `verify:phase10` script | accept | npm scripts are committed to repo; reviewed via PR; cannot be exploited at runtime. The alias only chains existing validators. |
| T-10-07-03 | Repudiation | Phase closure bookkeeping | accept | git history provides authoritative record of who closed Phase 10; ROADMAP/STATE updates are auditable via `git log`. |

No new attack surface; informational changes only. Per `<security_threat_model>` guidance: "for plans with negligible security surface (10-04 lang attribute, 10-07 docs), use a brief threat_model noting 'no new attack surface; informational changes only.'"
</threat_model>

<verification>
After all tasks complete, run from project root:

```bash
test -f docs/cutover-checklist.md
grep -q "P1-17" docs/cutover-checklist.md
grep -q "P2-6" docs/cutover-checklist.md
grep -q "verify:phase10" docs/cutover-checklist.md
node -e 'const p=require("./package.json"); console.log(p.scripts["verify:phase10"])'  # expect: non-empty
npm run verify:phase10                                                                  # expect: exit 0
grep -q "phase_10_status: complete" .planning/STATE.md
grep -q "10-07-PLAN.md" .planning/ROADMAP.md
```
</verification>

<success_criteria>
1. `docs/cutover-checklist.md` exists with all 7 host items + Code Changes section + Manual Reviews section.
2. P2-6 brand compliance review documented as a manual checklist item against `assets/mockup-reference/`.
3. `package.json` exposes `verify:phase10` as the canonical pre-cutover gate command.
4. `npm run verify:phase10` exits 0 against current state.
5. `ROADMAP.md` accurately reflects 7 shipped plans and Phase 10 closed status.
6. `STATE.md` frontmatter and todos updated; obsolete entries removed.
</success_criteria>

<output>
After completion, create `.planning/phases/10-audit-gap-closure-cutover-readiness/10-07-SUMMARY.md` documenting:
- docs/cutover-checklist.md created (size, sections)
- verify:phase10 form chosen (alias-only or explicit chain) + rationale
- ROADMAP.md / STATE.md changes (line counts, key field updates)
- Confirmation that `npm run verify:phase10` exits 0
</output>
