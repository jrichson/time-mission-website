---
phase: 10
plan: 05
type: execute
wave: 4
depends_on: [10-04]
files_modified:
  - docs/hero-medium-audit.md
  - src/partials/philadelphia-main.frag.txt
  - src/partials/houston-main.frag.txt
  - src/partials/antwerp-main.frag.txt
  - package.json
  - js/web-vitals-rum.js
  - src/components/SiteScripts.astro
  - _headers
autonomous: true
addresses: [P1-18, P2-1]
requirements_addressed: [ANLY-01, ANLY-03, SEO-01]
must_haves:
  truths:
    - "docs/hero-medium-audit.md exists listing every Astro-rendered location partial and stating whether its hero is <img>, <video>, or <picture>"
    - "Every location partial CONFIRMED to use a single <img> hero is converted to <picture> with desktop + mobile <source> entries (per UI-SPEC Surface 4)"
    - "Location partials whose hero is already <video> or <picture> are left UNTOUCHED (visual parity rule)"
    - "web-vitals@5.2.0 is installed in package.json (exact pin)"
    - "js/web-vitals-rum.js exists as an IIFE that imports onLCP, onCLS, onINP from web-vitals/attribution and pushes web_vitals events to window.dataLayer"
    - "js/web-vitals-rum.js is consent-gated: it only initialises measurement when window.__TM_CONSENT_STATE__.analytics_storage === 'granted'"
    - "src/components/SiteScripts.astro loads js/web-vitals-rum.js after GTM init"
    - "_headers CSP review documented: no change to connect-src needed because the script only writes to dataLayer (same-origin)"
  artifacts:
    - path: "docs/hero-medium-audit.md"
      provides: "Audit log of hero medium per location partial"
      contains: "philadelphia"
    - path: "js/web-vitals-rum.js"
      provides: "RUM beacon pushing LCP/CLS/INP to dataLayer"
      contains: "onLCP"
  key_links:
    - from: "js/web-vitals-rum.js"
      to: "window.dataLayer"
      via: "dataLayer.push({event: 'web_vitals', ...})"
      pattern: "web_vitals"
    - from: "src/components/SiteScripts.astro"
      to: "js/web-vitals-rum.js"
      via: "<script src> tag"
      pattern: "web-vitals-rum.js"
---

<objective>
Audit each Astro-rendered location partial to identify which use `<img>` vs `<video>` vs `<picture>` heroes (REQUIRED first task per UI-SPEC line 142-143 and CONTEXT.md item 3). For partials confirmed to use a single `<img>` hero, replace with `<picture>` + `srcset` per UI-SPEC Surface 4. Add a Real User Monitoring beacon (`web-vitals@5.2.0`) that pushes LCP/CLS/INP to `window.dataLayer` for GTM consumption, gated by Phase 6 consent state.

Purpose: Closes audit P1-18 (per-location hero srcset) without breaking visual parity on `<video>` heroes; closes P2-1 (RUM beacon) integrated with existing Phase 6 Consent Mode v2 architecture.

Output: Audit doc + targeted srcset replacements only on confirmed `<img>` heroes + web-vitals beacon wired through SiteScripts and respecting consent state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-audit-gap-closure-cutover-readiness/10-CONTEXT.md
@.planning/phases/10-audit-gap-closure-cutover-readiness/10-UI-SPEC.md
@.planning/phases/10-audit-gap-closure-cutover-readiness/10-RESEARCH.md
@src/partials/philadelphia-main.frag.txt
@src/partials/houston-main.frag.txt
@src/components/SiteScripts.astro
@src/components/SiteHead.astro
@_headers

<interfaces>
<!-- Confirmed by file read 2026-05-04: -->
<!--   philadelphia-main.frag.txt line 4: hero is <video id="heroVideo"> (NOT <img>) -->
<!--   houston-main.frag.txt: needs verification at hero region (line ~4-25) -->

<!-- UI-SPEC Surface 4 srcset template (verbatim copy when needed): -->
<!-- <picture> -->
<!--   <source srcset="/assets/photos/venue/hero-1920w.jpg 1x, /assets/photos/venue/hero-3840w.jpg 2x" -->
<!--           media="(min-width: 1024px)"> -->
<!--   <source srcset="/assets/photos/venue/hero-768w.jpg 1x, /assets/photos/venue/hero-1536w.jpg 2x" -->
<!--           media="(max-width: 1023px)"> -->
<!--   <img src="/assets/photos/venue/hero-1920w.jpg" -->
<!--        alt="Time Mission [City]" width="1920" height="1080" -->
<!--        loading="eager" fetchpriority="high"> -->
<!-- </picture> -->

<!-- Phase 6 consent contract (from research): -->
<!--   window.__TM_CONSENT_STATE__ is set by SiteHead.astro -->
<!--   For US pages: { analytics_storage: 'granted', ad_storage: 'granted' } by default -->
<!--   For EU pages: defaults to 'denied'; banner (plan 10-06) updates this on user accept -->
<!--   web-vitals beacon must check analytics_storage === 'granted' before measuring -->

<!-- _headers CSP currently allows GTM and google-analytics.com in connect-src. -->
<!-- web-vitals beacon writes ONLY to window.dataLayer (same-origin JS); no fetch -->
<!-- to external endpoints; therefore NO CSP change is required. Document the review -->
<!-- decision in plan summary. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Audit hero medium per Astro-rendered location partial + write docs/hero-medium-audit.md</name>
  <files>docs/hero-medium-audit.md</files>

  <read_first>
    - src/partials/philadelphia-main.frag.txt (already verified: <video id="heroVideo"> at line 4)
    - src/partials/houston-main.frag.txt (verify hero region — line 1-30)
    - src/partials/antwerp-main.frag.txt (created in plan 10-04 — verify what hero medium it uses)
    - .planning/phases/10-audit-gap-closure-cutover-readiness/10-CONTEXT.md item 3 (UI-SPEC notes — hero <img> audit MUST run first)
    - .planning/phases/10-audit-gap-closure-cutover-readiness/10-UI-SPEC.md "Surface 4: Hero <picture> srcset" lines 514-580
    - src/data/site/astro-rendered-output-files.json (the canonical list of Astro-rendered pages — only audit those that are location pages)
  </read_first>

  <action>
    1. **Identify Astro-rendered location pages.** Read `src/data/site/astro-rendered-output-files.json`. From the `outputFiles` array, enumerate location-page entries. Currently expected (after plan 10-04): `philadelphia.html`, `houston.html`, `antwerp.html`. Do NOT include non-location pages (index, about, faq, contact, locations index, groups/*, legal pages).

    2. **For each location page, locate its hero region** in the corresponding partial:
       - `src/partials/philadelphia-main.frag.txt`
       - `src/partials/houston-main.frag.txt`
       - `src/partials/antwerp-main.frag.txt` (created in plan 10-04)

       Read each partial's first ~50 lines (where the hero typically lives — UI-SPEC Surface 4 + RESEARCH.md confirm hero is the first major element after the `<section class="hero">` opener).

    3. **Categorize each partial's hero** into one of:
       - **VIDEO** — uses `<video>` element (no srcset work needed; visual parity prohibits replacing video with image)
       - **PICTURE_EXISTING** — already uses `<picture>` with `<source>` entries (no srcset work needed; already optimized)
       - **IMG_SINGLE** — uses a single `<img>` with `src` and no `<picture>` wrapper (CANDIDATE for srcset conversion in Task 2)

    4. **Write `docs/hero-medium-audit.md`** with the following structure (verbatim header):
       ```markdown
       # Hero Medium Audit — Phase 10 Plan 10-05

       Status: complete
       Date: <today>
       Source: src/partials/{location}-main.frag.txt

       ## Astro-rendered location pages

       | Location | Partial file | Hero medium | Line(s) | Action |
       |----------|--------------|-------------|---------|--------|
       | Philadelphia | philadelphia-main.frag.txt | VIDEO (<video id="heroVideo">) | 4 | none — visual parity protects video |
       | Houston | houston-main.frag.txt | <FILL IN> | <FILL IN> | <FILL IN> |
       | Antwerp | antwerp-main.frag.txt | <FILL IN> | <FILL IN> | <FILL IN> |

       ## Conclusion

       <bullet list of partials that need <picture> srcset conversion in Task 2; if none, state explicitly>

       ## Asset availability

       For each IMG_SINGLE candidate, list:
       - Existing 1x source: <filename + size>
       - Existing 2x source: <filename + size, OR "missing — defer per UI-SPEC line 575-577">
       ```

    5. **Determine asset availability.** For each IMG_SINGLE candidate, run:
       ```bash
       ls -la assets/photos/venue/ assets/photos/{location}/ 2>/dev/null | grep -i hero
       ```
       Document whether 2x resolution assets exist. If absent, note this in the audit per UI-SPEC line 575-577 (executor must either use 1x for both or defer 2x).

    6. **Commit the audit doc** before any conversion work in Task 2 begins.
  </action>

  <verify>
    <automated>test -f docs/hero-medium-audit.md &amp;&amp; grep -q "Philadelphia" docs/hero-medium-audit.md &amp;&amp; grep -q "Houston" docs/hero-medium-audit.md &amp;&amp; grep -q "Antwerp" docs/hero-medium-audit.md &amp;&amp; grep -qE "VIDEO|PICTURE_EXISTING|IMG_SINGLE" docs/hero-medium-audit.md</automated>
  </verify>

  <acceptance_criteria>
    - `docs/hero-medium-audit.md` exists
    - Document lists ALL Astro-rendered location partials (currently: philadelphia, houston, antwerp)
    - Each location row has one of the three exact category labels: `VIDEO`, `PICTURE_EXISTING`, or `IMG_SINGLE`
    - Each row cites the line number(s) in the partial
    - Document has a `## Conclusion` section listing IMG_SINGLE candidates (or stating none exist)
    - Document has an `## Asset availability` section with file paths/sizes for any IMG_SINGLE candidates' existing 1x and 2x resolution images
    - Philadelphia row reports `VIDEO` (verified during research)
  </acceptance_criteria>

  <done>
    Audit doc exists, classifies every location partial's hero medium, and identifies the Task 2 candidate set. If zero IMG_SINGLE candidates exist, Task 2 becomes a documentation-only confirmation task.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace <img> heroes with <picture> srcset on CONFIRMED IMG_SINGLE partials only</name>
  <files>src/partials/{partial}-main.frag.txt for each IMG_SINGLE entry from Task 1's audit</files>

  <read_first>
    - docs/hero-medium-audit.md (Task 1 output — defines which partials are IMG_SINGLE candidates)
    - .planning/phases/10-audit-gap-closure-cutover-readiness/10-UI-SPEC.md "Surface 4: Hero <picture> srcset" lines 514-580 (verbatim template + breakpoints)
    - The current `<img>` markup in each candidate partial (so the alt text and current src path are preserved)
    - assets/photos/ directory listings to verify 2x assets exist or are missing
  </read_first>

  <action>
    **If Task 1's audit reports ZERO IMG_SINGLE candidates:** Skip the conversion work and document this in the SUMMARY. The task is complete with no code change. State explicitly: "Per docs/hero-medium-audit.md, all Astro-rendered location partials use <video> or <picture> heroes; no srcset conversion needed in Phase 10."

    **If Task 1's audit reports one or more IMG_SINGLE candidates:** For each candidate partial:

    1. Locate the existing single `<img>` hero markup. Note the current `src`, `alt`, and any other attributes.

    2. Replace the entire `<img>` element with the UI-SPEC verbatim `<picture>` template (UI-SPEC lines 528-552), substituting `[City]` with the actual city name (matches UI-SPEC Copywriting Contract: "Time Mission Philadelphia", "Time Mission Houston", "Time Mission Antwerp"):

       ```html
       <picture>
           <source
               srcset="/assets/photos/venue/hero-1920w.jpg 1x, /assets/photos/venue/hero-3840w.jpg 2x"
               media="(min-width: 1024px)"
           >
           <source
               srcset="/assets/photos/venue/hero-768w.jpg 1x, /assets/photos/venue/hero-1536w.jpg 2x"
               media="(max-width: 1023px)"
           >
           <img
               src="/assets/photos/venue/hero-1920w.jpg"
               alt="Time Mission [City]"
               width="1920"
               height="1080"
               loading="eager"
               fetchpriority="high"
           >
       </picture>
       ```

    3. **Adapt the file paths** to the actual asset paths used in each partial. The `/assets/photos/venue/hero-1920w.jpg` placeholder must be substituted with the actual location-specific path. If the existing image is at `/assets/photos/{slug}/hero.jpg`, the new sources should reference `/assets/photos/{slug}/hero-1920w.jpg` (or equivalent naming).

    4. **Asset gap fallback:** If the audit confirmed 2x assets do NOT exist, follow UI-SPEC line 575-577: use the 1x asset for both 1x and 2x in the `srcset` (acceptable interim) AND add a comment in the partial: `<!-- TODO: Phase 10 P2-8 — Designer to deliver 2x hero assets; currently 1x served for both densities -->`. Document the deferral in the SUMMARY.

    5. **Aspect ratio + LCP:** Always include `width="1920" height="1080"` and `loading="eager" fetchpriority="high"` per UI-SPEC line 565-566.

    6. **Format scope:** JPEG ONLY in this phase per UI-SPEC line 559. Do not introduce WebP or AVIF sources.

    7. After all conversions, run:
       ```bash
       npm run build:astro
       ```
       Open the affected location pages in a browser and confirm visual parity (the same hero photo shows at the same size; no broken image icon).
  </action>

  <verify>
    <automated>npm run build:astro &amp;&amp; for slug in philadelphia houston antwerp; do if grep -q "IMG_SINGLE.*${slug}" docs/hero-medium-audit.md 2>/dev/null; then grep -q '&lt;picture&gt;' "src/partials/${slug}-main.frag.txt" || { echo "FAIL: ${slug} should have &lt;picture&gt; per audit"; exit 1; }; fi; done; echo OK</automated>
  </verify>

  <acceptance_criteria>
    - For every partial classified `IMG_SINGLE` in `docs/hero-medium-audit.md`, that partial now contains `<picture>` and at least two `<source>` elements with `media` and `srcset` attributes
    - For every partial classified `VIDEO` or `PICTURE_EXISTING`, the partial is UNCHANGED (verify via git diff that only IMG_SINGLE files changed)
    - Every replaced `<img>` retains its descriptive `alt` text matching UI-SPEC Copywriting Contract pattern: `"Time Mission {City}"`
    - Every replaced `<img>` has `width="1920" height="1080" loading="eager" fetchpriority="high"`
    - No `.webp` or `.avif` source URLs introduced (JPEG only this phase)
    - `npm run build:astro` succeeds
    - For each IMG_SINGLE conversion, opening `dist/{slug}.html` in a browser shows the hero photo at expected size (no broken image, no layout shift)
    - If 2x assets are missing, the partial contains a `TODO: Phase 10 P2-8` comment marking the deferral
    - `npm run check:seo-output` exits 0 (img alt still present in pictures)
    - `npm run check:img-alt-axe` exits 0 (axe scan still passes)
  </acceptance_criteria>

  <done>
    All IMG_SINGLE hero candidates converted to UI-SPEC `<picture>` template with correct breakpoints and LCP attributes; VIDEO/PICTURE_EXISTING partials untouched. If zero candidates, the SUMMARY documents that fact explicitly.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Install web-vitals@5.2.0 + create js/web-vitals-rum.js IIFE + wire SiteScripts.astro + CSP review</name>
  <files>package.json, package-lock.json, js/web-vitals-rum.js, src/components/SiteScripts.astro, _headers</files>

  <read_first>
    - package.json (current devDependencies — note: this is a static site; web-vitals must work at runtime in browser, so it should be a regular dependency NOT devDependency — verify project convention by reading current dependency list)
    - .planning/phases/10-audit-gap-closure-cutover-readiness/10-RESEARCH.md "Gap 10 — P2-1: RUM Beacon (web-vitals)" lines 235-269 (the dataLayer push pattern + CSP analysis)
    - src/components/SiteScripts.astro (current state — find where GTM init occurs and where to insert the new script tag)
    - src/components/SiteHead.astro (verify Phase 6 sets `window.__TM_CONSENT_STATE__` and `window.__TM_TAGGING_CONFIG__`)
    - js/nav.js, js/locations.js, js/ticket-panel.js (existing IIFE patterns — follow same conventions per CONVENTIONS.md: IIFE, `'use strict'`, try/catch around storage access)
    - _headers (CSP rules — confirm connect-src, script-src lines)
  </read_first>

  <action>
    1. **Install web-vitals@5.2.0 (exact pin)** as a regular `dependencies` entry (not devDep — runtime browser script):
       ```bash
       npm install --save web-vitals@5.2.0
       ```
       After install, verify `package.json` `dependencies` (creating the block if it does not exist) contains `"web-vitals": "5.2.0"` (exact, no caret). If npm rewrote to caret, manually edit and re-run `npm install`.

       NOTE on bundling: `web-vitals` ships ESM. For a static `js/web-vitals-rum.js` IIFE to use it, two approaches exist:
       - **(a)** Use the prebuilt UMD/IIFE file: `node_modules/web-vitals/dist/web-vitals.iife.js` — copy this file to `public/js/web-vitals.iife.js` (or load it directly).
       - **(b)** Use `<script type="module">` to import from CDN — adds CSP work and external dep.

       **Decision:** Approach (a). Create `js/web-vitals-rum.js` as a vanilla IIFE that uses the global `webVitals` object exposed by the bundled IIFE. The IIFE bundle exposes `onLCP`, `onCLS`, `onINP` on `window.webVitals` (per `web-vitals` v5 IIFE distribution).

       Add an npm "postinstall" script (or update `sync-static-to-public.mjs`) so `node_modules/web-vitals/dist/web-vitals.iife.js` is copied to `public/js/web-vitals.iife.js` during build. **Verify which mechanism the project uses to ship vendor JS** by reading `sync-static-to-public.mjs` first; integrate via the established pattern, not a new mechanism.

    2. **Create `js/web-vitals-rum.js`** as an IIFE following the project's `js/*.js` convention (CONVENTIONS.md: 4-space indent in browser IIFEs, `'use strict'`, single-quote strings, missing-element tolerance, try/catch around storage access). The file content (verbatim — substitute nothing):
       ```javascript
       /**
        * Phase 10 P2-1 — Web Vitals RUM beacon
        * Measures LCP / CLS / INP via the web-vitals attribution build and pushes
        * normalized non-PII events to window.dataLayer for GTM consumption.
        *
        * Consent gate: only initializes when window.__TM_CONSENT_STATE__.analytics_storage
        * === 'granted'. Set by SiteHead.astro Phase 6 init or by cookie banner accept.
        */
       (function () {
           'use strict';
           if (typeof window === 'undefined') return;

           function consentGranted() {
               try {
                   var s = window.__TM_CONSENT_STATE__;
                   return s && s.analytics_storage === 'granted';
               } catch (e) {
                   return false;
               }
           }

           function sendToDataLayer(metric) {
               try {
                   window.dataLayer = window.dataLayer || [];
                   window.dataLayer.push({
                       event: 'web_vitals',
                       web_vitals_name: metric.name,
                       web_vitals_delta: Math.round(metric.delta),
                       web_vitals_value: Math.round(metric.value),
                       web_vitals_rating: metric.rating,
                       web_vitals_id: metric.id,
                   });
               } catch (e) {
                   // Fail silently — RUM should never break the page
               }
           }

           function initialize() {
               var wv = window.webVitals;
               if (!wv || typeof wv.onLCP !== 'function') return;
               wv.onLCP(sendToDataLayer);
               wv.onCLS(sendToDataLayer);
               wv.onINP(sendToDataLayer);
           }

           if (consentGranted()) {
               initialize();
           } else {
               // Listen for consent updates (Phase 6 + cookie banner from plan 10-06)
               window.addEventListener('tm:consent-updated', function () {
                   if (consentGranted()) initialize();
               });
           }
       })();
       ```

       This pattern matches CONVENTIONS.md (IIFE, 4-space indent, single quotes, fail-soft with try/catch, predicate function name `consentGranted`).

    3. **Wire into `src/components/SiteScripts.astro`.** Read the file. Find where existing `<script>` tags are emitted (typically after GTM init). Add (after the existing scripts, before any closing tags):
       ```html
       <script src="/js/web-vitals.iife.js" defer></script>
       <script src="/js/web-vitals-rum.js?v=1" defer></script>
       ```

       Order matters: the IIFE bundle must load before `web-vitals-rum.js` since the latter reads `window.webVitals`. Both scripts use `defer` so they don't block page rendering. Use a versioned query string on `web-vitals-rum.js?v=1` consistent with `js/locations.js?v=8` pattern in the codebase.

    4. **CSP review.** Read `_headers`. The script-src line should include `'self'` (allowing the new same-origin scripts). The connect-src does NOT need an update because the beacon writes only to `window.dataLayer` (in-page, no fetch). DO NOT modify `_headers` unless `script-src` does not include `'self'`. Document the review decision in the SUMMARY: "CSP unchanged — web-vitals script is same-origin and only writes to dataLayer (no fetch to external endpoints)."

    5. **Verify ship path.** After `npm run build:astro`:
       - Confirm `dist/js/web-vitals.iife.js` exists (vendor bundle copied into public during build)
       - Confirm `dist/js/web-vitals-rum.js` exists (custom IIFE copied)
       - Confirm `dist/index.html` contains `<script src="/js/web-vitals.iife.js" defer></script>` AND `<script src="/js/web-vitals-rum.js?v=1" defer></script>`
       - Open `dist/index.html` in a browser; in DevTools console, check `window.dataLayer` after page load — if test environment grants analytics consent, you should see at least one `{event: 'web_vitals', web_vitals_name: 'LCP', ...}` push. (May require user interaction for INP.)
  </action>

  <verify>
    <automated>node -e 'const p=require("./package.json"); const v=(p.dependencies&amp;&amp;p.dependencies["web-vitals"])||(p.devDependencies&amp;&amp;p.devDependencies["web-vitals"]); if(v!=="5.2.0"){console.error("FAIL: web-vitals must be exactly 5.2.0, got "+v);process.exit(1);} console.log("PASS");' &amp;&amp; npm run build:astro &amp;&amp; test -f dist/js/web-vitals-rum.js &amp;&amp; test -f dist/js/web-vitals.iife.js &amp;&amp; grep -q "web-vitals-rum.js" dist/index.html</automated>
  </verify>

  <acceptance_criteria>
    - `package.json` contains `"web-vitals": "5.2.0"` (exact pin, in either dependencies or devDependencies — production-runtime preferred)
    - `package-lock.json` resolved version is `5.2.0`
    - `js/web-vitals-rum.js` exists and is an IIFE (`(function () { 'use strict'; ... })();`)
    - `js/web-vitals-rum.js` contains literal `window.__TM_CONSENT_STATE__` (consent gate)
    - `js/web-vitals-rum.js` contains `event: 'web_vitals'` in the dataLayer push
    - `js/web-vitals-rum.js` listens for `tm:consent-updated` to re-evaluate consent (so cookie banner accept in plan 10-06 can activate it)
    - `src/components/SiteScripts.astro` contains literal `<script src="/js/web-vitals.iife.js" defer></script>` AND `<script src="/js/web-vitals-rum.js?v=1" defer></script>` (in that order)
    - After `npm run build:astro`: `dist/js/web-vitals.iife.js` and `dist/js/web-vitals-rum.js` both exist
    - `dist/index.html` contains both script tags
    - `_headers` is unchanged (no CSP modification needed) OR if modified, the diff is documented as "added 'self' to script-src" with clear rationale
    - `npm run verify` end-to-end exits 0
    - `npm run check:img-alt-axe` exits 0 (no new axe violations introduced by srcset or RUM)
  </acceptance_criteria>

  <done>
    `web-vitals@5.2.0` installed exact-pinned; runtime IIFE measures LCP/CLS/INP, gates by consent state, pushes to dataLayer; SiteScripts loads it after GTM; CSP review recorded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → window.dataLayer | RUM beacon writes performance metrics into the same-origin dataLayer; consumed by GTM |
| npm registry → repo | New runtime dependency `web-vitals@5.2.0`; vendor IIFE shipped to /public |
| user-agent → static hero images | New `<picture>` srcset references additional asset URLs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05-01 | Tampering | `web-vitals@5.2.0` supply chain | mitigate | Exact version pin (no caret/tilde); lockfile commit; copy IIFE bundle from node_modules at build time; no CDN load (would expose to runtime supply-chain attacks). |
| T-10-05-02 | Information Disclosure | dataLayer push payload | mitigate | Only metric name, rounded delta/value (no decimals), rating, and metric id (UUID generated by web-vitals — not user-identifiable). NO URL with query params, NO user identifiers. Verify in `js/web-vitals-rum.js` that no `location.href` or query string is included in the push. |
| T-10-05-03 | Denial of Service | RUM beacon failures | mitigate | All measurement code wrapped in try/catch; library load failure short-circuits via `if (!wv || typeof wv.onLCP !== 'function') return`; failure cannot break page. |
| T-10-05-04 | Information Disclosure | Hero `<picture>` srcset | accept | Image URLs are public marketing assets; no PII; same-origin; no new attack surface. |
| T-10-05-05 | Tampering | Consent gate bypass | mitigate | `consentGranted()` reads `window.__TM_CONSENT_STATE__.analytics_storage`; this is set by Phase 6 SiteHead BEFORE GTM loads. Race condition mitigated by `tm:consent-updated` listener for late grants. EU defaults are `denied`; banner accept (plan 10-06) flips to granted. |
| T-10-05-06 | Tampering | CSP allowlist | accept | `script-src 'self'` already permits same-origin scripts. No external CDN load. No `connect-src` change because no fetch occurs (dataLayer is in-page). |

No HIGH-severity threats. Supply chain mitigated by exact-pin + bundle-from-node_modules; PII risk mitigated by payload allowlist; consent gate is a documented boundary.
</threat_model>

<verification>
After all tasks complete, run from project root:

```bash
test -f docs/hero-medium-audit.md
node -e 'const p=require("./package.json"); const v=(p.dependencies&&p.dependencies["web-vitals"])||(p.devDependencies&&p.devDependencies["web-vitals"]); console.log(v)'   # expect: 5.2.0
npm run build:astro
test -f dist/js/web-vitals.iife.js
test -f dist/js/web-vitals-rum.js
grep -q "web-vitals-rum.js" dist/index.html
grep -q "web-vitals-rum.js" dist/philadelphia.html
npm run check:img-alt-axe
npm run check:hreflang-cluster
npm run verify
```

Manual (cutover-checklist tracks): Open dist/index.html in Chrome with DevTools open; confirm `dataLayer` receives a `web_vitals` push within ~5 seconds of load (LCP usually fires immediately).
</verification>

<success_criteria>
1. `docs/hero-medium-audit.md` documents every Astro-rendered location partial's hero medium with line citations.
2. Only IMG_SINGLE partials get `<picture>` conversion; VIDEO/PICTURE_EXISTING partials untouched (visual parity preserved).
3. `web-vitals@5.2.0` installed (exact pin); IIFE bundle shipped to `public/js/web-vitals.iife.js`.
4. `js/web-vitals-rum.js` is an IIFE that consent-gates on `analytics_storage === 'granted'`, pushes `web_vitals` events to `dataLayer`, listens for `tm:consent-updated`.
5. `SiteScripts.astro` loads both scripts (vendor first, custom second) with `defer`.
6. CSP review documented; no `_headers` change needed.
7. Full `npm run verify` chain passes.
</success_criteria>

<output>
After completion, create `.planning/phases/10-audit-gap-closure-cutover-readiness/10-05-SUMMARY.md` documenting:
- Hero medium audit findings (count of VIDEO / PICTURE_EXISTING / IMG_SINGLE)
- Files converted in Task 2 (or "0 conversions, all heroes already video or picture")
- web-vitals install version (exact pin confirmed)
- CSP review decision and rationale
</output>
