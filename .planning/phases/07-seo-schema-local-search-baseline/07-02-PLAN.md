---
phase: 07-seo-schema-local-search-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01-PLAN.md
files_modified:
  - src/components/SiteHead.astro
  - src/layouts/SiteLayout.astro
  - src/pages/index.astro
  - src/pages/about.astro
  - src/pages/faq.astro
  - src/pages/contact.astro
  - src/pages/contact-thank-you.astro
  - src/pages/locations.astro
  - src/pages/privacy.astro
  - src/pages/groups/corporate.astro
  - src/pages/philadelphia.astro
  - src/pages/houston.astro
  - scripts/check-seo-output.js
  - package.json
autonomous: true
requirements:
  - SEO-01
must_haves:
  truths:
    - "Every Astro-rendered page calls getSeoForRoute(canonicalPath) and renders title/description/canonical/OG/Twitter/robots from the catalog (no inline literals for SEO meta in *.astro frontmatter)."
    - "SiteHead emits <meta name=\"robots\" content=\"...\"> driven by seo-robots.json — drift impossible (D-02)."
    - "verify:phase4 still passes after migration (Phase 4 representative templates retain visual + script parity)."
    - "Built-output meta parity is enforced by scripts/check-seo-output.js (SEO-01 rendered shape)."
  artifacts:
    - path: "src/components/SiteHead.astro"
      provides: "Catalog-driven head with robots meta and absolute OG/Twitter image URLs"
    - path: "scripts/check-seo-output.js"
      provides: "dist/*.html parser asserting title/description/canonical/og/twitter/robots match resolved catalog (SEO-01 integration)"
  key_links:
    - from: "src/pages/*.astro"
      to: "src/lib/seo/catalog.ts"
      via: "getSeoForRoute(canonicalPath) call in frontmatter"
      pattern: "Single resolver"
---

<objective>
Refactor `SiteHead.astro` to consume `getSeoForRoute(canonicalPath)`, render `<meta name="robots">`, and emit absolute OG/Twitter URLs; migrate every existing `src/pages/*.astro` to drop inline `title`/`description`/`ogImage` literals in favor of the resolver; add `scripts/check-seo-output.js` to enforce parity in `dist/*.html` (D-01, D-02, D-03; SEO-01).
</objective>

<threat_model>
| Threat | Severity | Mitigation in this plan |
|--------|----------|-------------------------|
| GTM/Consent Mode bootstrap order regresses while editing `SiteHead.astro` (Phase 6 contract — see 06-CONTEXT D-13) | High | Make the refactor purely additive at the top of frontmatter; keep the existing `<script>` blocks for `dataLayer`, `gtag('consent','default',...)`, GTM loader **byte-identical** in source order; verify with `verify:phase6` before commit |
| Wrong canonicalPath passed by a page surfaces wrong meta (cloaking risk) | High | `getSeoForRoute` throws on unknown paths; `check-seo-output.js` cross-checks rendered `<link rel="canonical">` against the path the page declared |
| OG/Twitter `og:image` becomes relative in built HTML (Pitfall 5) | Medium | Helper builds `new URL` absolute upfront; integration check asserts `og:image` value matches `^https://timemission\.com/assets/` |
| Pages stop emitting `<meta name="robots">` for `/contact-thank-you` (regresses Phase 6 noindex on thank-you path) | Medium | `/contact-thank-you` resolves to `noindex,follow` from `seo-robots.json`; integration check pins the value |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-seo-schema-local-search-baseline/07-CONTEXT.md
@.planning/phases/07-seo-schema-local-search-baseline/07-RESEARCH.md
@.planning/phases/07-seo-schema-local-search-baseline/07-VALIDATION.md
@.planning/phases/07-seo-schema-local-search-baseline/07-01-PLAN.md
@.planning/phases/06-analytics-consent-forms-contract/06-CONTEXT.md
@.planning/phases/04-shared-components-template-parity/04-CONTEXT.md
@AGENTS.md
@CLAUDE.md
@src/components/SiteHead.astro
@src/layouts/SiteLayout.astro
@src/pages/index.astro
@src/pages/philadelphia.astro
@src/pages/houston.astro
@src/pages/groups/corporate.astro
@src/pages/about.astro
@src/pages/faq.astro
@src/pages/contact.astro
@src/pages/contact-thank-you.astro
@src/pages/locations.astro
@src/pages/privacy.astro
@src/data/site/seo-routes.json
@src/data/site/seo-robots.json
@src/lib/seo/catalog.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: GitNexus impact analysis on SiteHead and SiteLayout</name>
  <files>(read-only)</files>
  <read_first>
    <file>AGENTS.md</file>
    <file>src/components/SiteHead.astro</file>
    <file>src/layouts/SiteLayout.astro</file>
  </read_first>
  <action>Run `gitnexus_impact({target: "SiteHead", direction: "upstream"})` and `gitnexus_impact({target: "SiteLayout", direction: "upstream"})`. Record the d=1 page list and risk level. **STOP and surface to user if HIGH or CRITICAL** before edits (CLAUDE.md mandatory rule). Confirm the d=1 list matches the page list in this plan's `files_modified` so executor knows every consumer it must touch.</action>
  <verify>
    <automated>node -e "process.exit(0)"</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Execution log records both impact analyses with d=1 callers enumerated.</criterion>
    <criterion>Any HIGH/CRITICAL warning is acknowledged with mitigation note before continuing.</criterion>
  </acceptance_criteria>
  <done>Blast radius confirmed before refactor.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor SiteHead.astro to accept canonicalPath alone (catalog-driven)</name>
  <files>src/components/SiteHead.astro</files>
  <read_first>
    <file>src/components/SiteHead.astro</file>
    <file>src/lib/seo/catalog.ts</file>
    <file>src/data/site/seo-routes.json</file>
  </read_first>
  <action>Edit `src/components/SiteHead.astro` so the head is **catalog-driven** while preserving the existing GTM/Consent Mode block byte-identically.

**New props (Phase 7):**
```ts
interface Props {
    canonicalPath: string;
    // Optional overrides — kept for emergency parity escape hatch only; default to catalog values.
    title?: string;
    description?: string;
    ogImage?: string;
    twitterImage?: string;
    robots?: string;
}
```

**Frontmatter additions (top of frontmatter, BEFORE the existing `gtmContainerId`/`baseUrl` block):**
```ts
import { getSeoForRoute } from '../lib/seo/catalog';
const { canonicalPath, title: titleOverride, description: descOverride, ogImage: ogOverride, twitterImage: twOverride, robots: robotsOverride } = Astro.props;
const seo = getSeoForRoute(canonicalPath);
const title = titleOverride ?? seo.title;
const description = descOverride ?? seo.description;
const canonicalUrl = seo.canonicalUrl;
const og = ogOverride ?? seo.ogImage;
const tw = twOverride ?? seo.twitterImage;
const robotsValue = robotsOverride ?? seo.robots;
```

Remove the previous `const { title, description, canonicalPath, ogImage, twitterImage } = Astro.props;` and `const og = ogImage ?? ...`/`const tw = twitterImage ?? og;` lines (they are now derived).

**Add ONE new meta tag** immediately after `<link rel="canonical" href={canonicalUrl} />`:
```astro
<meta name="robots" content={robotsValue} />
```

**Do NOT edit** the existing `<script is:inline>window.dataLayer = ... gtag('consent','default',...)</script>` block, the GTM loader, or the noscript iframe — leave them byte-identical. Do NOT change the order of the `<link rel="preconnect">`, font preloads, or stylesheet links.

The `gtmContainerId`/`baseUrl` constants and the `<title>`/OG/Twitter meta tags can stay in their current positions — only their **values** are now derived from `seo`.</action>
  <verify>
    <automated>npx astro check</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg -n "import \\{ getSeoForRoute \\}" src/components/SiteHead.astro` matches once.</criterion>
    <criterion>`rg -n 'meta name="robots"' src/components/SiteHead.astro` matches at least once.</criterion>
    <criterion>`rg -n "gtag\\('consent', 'default'" src/components/SiteHead.astro` matches exactly once (Phase 6 contract preserved).</criterion>
    <criterion>`rg -n "googletagmanager.com/gtm.js" src/components/SiteHead.astro` matches once (Phase 6 GTM loader preserved).</criterion>
    <criterion>`npx astro check` exits 0.</criterion>
  </acceptance_criteria>
  <done>SiteHead is catalog-driven, robots meta is rendered, Phase 6 analytics contract is intact.</done>
</task>

<task type="auto">
  <name>Task 3: Update SiteLayout.astro Props to forward canonicalPath</name>
  <files>src/layouts/SiteLayout.astro</files>
  <read_first>
    <file>src/layouts/SiteLayout.astro</file>
  </read_first>
  <action>Edit `src/layouts/SiteLayout.astro` Props interface so `title` and `description` become **optional** (defaulted by `getSeoForRoute`); add an optional `robots?: string` prop and forward it to `SiteHead`. Final shape:

```ts
interface Props {
    canonicalPath: string;
    title?: string;
    description?: string;
    ogImage?: string;
    twitterImage?: string;
    robots?: string;
    bodyDataLocation?: string;
    bodyClass?: string;
}
```

Update destructure and `<SiteHead ... />` invocation accordingly. Pages that omit `title`/`description` will rely entirely on the catalog. Do not change body markup, slot layout, or the `TicketPanel`/`SiteScripts` order (Phase 4 parity contract).</action>
  <verify>
    <automated>npx astro check</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`title?:` (with `?`) appears in `src/layouts/SiteLayout.astro` Props interface.</criterion>
    <criterion>`<SiteHead` invocation forwards `robots={robots}` to `SiteHead`.</criterion>
    <criterion>`npx astro check` exits 0.</criterion>
  </acceptance_criteria>
  <done>Layout no longer requires per-page meta strings.</done>
</task>

<task type="auto">
  <name>Task 4: Migrate Astro pages to drop inline meta literals</name>
  <files>src/pages/index.astro, src/pages/about.astro, src/pages/faq.astro, src/pages/contact.astro, src/pages/contact-thank-you.astro, src/pages/locations.astro, src/pages/privacy.astro, src/pages/groups/corporate.astro, src/pages/philadelphia.astro, src/pages/houston.astro</files>
  <read_first>
    <file>src/pages/index.astro</file>
    <file>src/pages/philadelphia.astro</file>
    <file>src/pages/houston.astro</file>
    <file>src/pages/contact-thank-you.astro</file>
    <file>src/data/site/seo-routes.json</file>
  </read_first>
  <action>For **every** Astro page in `files_modified` for this task, remove the `title=`, `description=`, `ogImage=`, and `twitterImage=` props from the `<SiteLayout ... >` invocation and keep only `canonicalPath` plus any non-SEO props (e.g. `bodyDataLocation`, `bodyClass`). Do NOT touch the `<Fragment slot="json-ld">` block (still inline this phase — Wave 3 owns JSON-LD).

**Concrete edits, page by page:**

- **`src/pages/index.astro`**: keep `canonicalPath="/"`; remove `title=...`, `description=...`. (Catalog provides both.)
- **`src/pages/about.astro`**: keep `canonicalPath="/about"`; remove `title=...`, `description=...`.
- **`src/pages/faq.astro`**: keep `canonicalPath="/faq"`; remove `title=...`, `description=...`.
- **`src/pages/contact.astro`**: keep `canonicalPath="/contact"`, `bodyClass="has-noise"`; remove `title=...`, `description=...`.
- **`src/pages/contact-thank-you.astro`**: this page currently renders its own `<head>` and inline `<meta name="robots" content="noindex, follow" />`. **Refactor this page to use `<SiteLayout>` if feasible**, OR — if the bespoke layout is required for its visual minimalism — ensure its `<SiteHead canonicalPath="/contact-thank-you" />` invocation delegates to the catalog and **delete** the hand-rendered `<meta name="robots" content="noindex, follow" />` (because `SiteHead` now emits it from `seo-robots.json`). Choose the lower-risk path; document choice in commit message.
- **`src/pages/locations.astro`**: keep `canonicalPath="/locations"`, `bodyClass="has-noise"`; remove `title=...`, `description=...`. Note: this file currently has a duplicated `bodyClass` line (parity bug) — keep one occurrence; do not introduce other unrelated cleanup.
- **`src/pages/privacy.astro`**: keep `canonicalPath="/privacy"`, `bodyClass="has-noise"`; remove `title=...`, `description=...`.
- **`src/pages/groups/corporate.astro`**: keep `canonicalPath="/groups/corporate"`, `bodyClass="has-noise"`; remove `title=...`, `description=...`, `ogImage=...`, `twitterImage=...`. (Catalog now holds the corporate `og:image` per Plan 07-01 Task 3.)
- **`src/pages/philadelphia.astro`**: keep `canonicalPath="/philadelphia"`, `bodyDataLocation={philly.slug}`; remove `title=...`, `description=...`.
- **`src/pages/houston.astro`**: keep `canonicalPath="/houston"`, `bodyDataLocation={houston.slug}`; remove `title=...`, `description=...`.

**Hard guard:** do NOT delete the `const ld = '{...}'` literals or their `<Fragment slot="json-ld">` blocks — those are Wave 3 territory. Wave 2 only touches meta literals.</action>
  <verify>
    <automated>npx astro check && rg -n "title=\"" src/pages | rg -v "noindex|sectionLabel|menuTitle|cardTitle" || true</automated>
  </verify>
  <acceptance_criteria>
    <criterion>For each migrated page, `rg -n "title=\\\"" src/pages/{page}.astro` returns zero matches inside the `<SiteLayout` opening tag (use multi-line read to confirm).</criterion>
    <criterion>For each migrated page, `<SiteLayout` invocation still includes literal `canonicalPath=`.</criterion>
    <criterion>`rg -n "Fragment slot=\"json-ld\"" src/pages | wc -l` is unchanged from before the edit (Wave 3 has not started — JSON-LD blocks remain).</criterion>
    <criterion>`npx astro check` exits 0.</criterion>
    <criterion>`npm run build:astro` exits 0.</criterion>
  </acceptance_criteria>
  <done>Every Astro page derives meta from the catalog; only `canonicalPath` (and non-SEO props) remain on `<SiteLayout>`.</done>
</task>

<task type="auto">
  <name>Task 5: Create scripts/check-seo-output.js (dist/*.html parity)</name>
  <files>scripts/check-seo-output.js, package.json</files>
  <read_first>
    <file>scripts/check-route-contract.js</file>
    <file>scripts/check-sitemap.js</file>
    <file>src/data/routes.json</file>
    <file>src/data/site/seo-routes.json</file>
    <file>src/data/site/seo-robots.json</file>
  </read_first>
  <action>Create `scripts/check-seo-output.js` (CommonJS, error-accumulation). The script MUST:

1. Read `src/data/routes.json`, `src/data/site/seo-routes.json`, `src/data/site/seo-robots.json`, and `src/data/site/seo-defaults.json`.
2. Inline a tiny copy of `resolveRobotsForRoute` matching `src/lib/seo/route-patterns.ts` (comment: "keep in sync with src/lib/seo/route-patterns.ts").
3. For **only** routes whose `outputFile` corresponds to an Astro-rendered page (the same set as `ASTRO_RENDERED_OUTPUT_FILES` in `scripts/sync-static-to-public.mjs` — duplicate the literal set; comment "keep in sync"), do:
   a. Read `dist/{outputFile}` from the most recent build (script must error with a friendly message if `dist/` is missing — direct user to run `npm run build:astro` first).
   b. Use simple regex extractors to capture: `<title>(.+?)</title>`, `<meta name="description" content="([^"]*)"`, `<link rel="canonical" href="([^"]+)"`, `<meta property="og:url" content="([^"]+)"`, `<meta property="og:image" content="([^"]+)"`, `<meta name="twitter:image" content="([^"]+)"`, `<meta name="robots" content="([^"]+)"`.
   c. Compare extracted values to the resolved catalog values for that canonicalPath (`title === entry.title`; `description === entry.description`; `canonical === baseUrl + path` with root being `https://timemission.com/`; `og:url === canonical`; `og:image === absolute(entry.ogImage)`; `twitter:image === absolute(entry.twitterImage ?? entry.ogImage)`; `robots === resolveRobotsForRoute(path, robotsTable)`).
   d. Accumulate mismatches with the file path and the field name.
4. Print `SEO output check passed for ${count} Astro-rendered routes.` on success.
5. Add npm script `"check:seo-output": "node scripts/check-seo-output.js"`. Do NOT add to root `npm run check` (this requires `dist/` and runs in `verify:phase7`).</action>
  <verify>
    <automated>npm run build:astro && npm run check:seo-output</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`scripts/check-seo-output.js` exists.</criterion>
    <criterion>`package.json` includes `"check:seo-output"`.</criterion>
    <criterion>`npm run check:seo-output` exits 0 after `npm run build:astro` against current Astro pages.</criterion>
    <criterion>Manual smoke (executor logs in journal): temporarily change one entry's `title` in `seo-routes.json`, rebuild, run `check:seo-output`, see it fail with the path + `title mismatch`. Revert.</criterion>
  </acceptance_criteria>
  <done>Built-output meta parity is now machine-checked (SEO-01 integration).</done>
</task>

<task type="auto">
  <name>Task 6: Verify Phase 4 + Phase 6 still pass after migration</name>
  <files>(verify-only)</files>
  <read_first>
    <file>package.json</file>
  </read_first>
  <action>Run `npm run verify:phase4` and `npm run verify:phase6` end-to-end. Both must exit 0. If `verify:phase6` fails because of GTM/consent regression, debug and fix `SiteHead.astro` before continuing (block on this — Phase 6 contract is non-negotiable per 06-CONTEXT D-13).</action>
  <verify>
    <automated>npm run verify:phase4 &amp;&amp; npm run verify:phase6</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`npm run verify:phase4` exits 0.</criterion>
    <criterion>`npm run verify:phase6` exits 0.</criterion>
  </acceptance_criteria>
  <done>Wave 2 has not regressed prior phase gates.</done>
</task>

<task type="auto">
  <name>Task 7: GitNexus pre-commit detect_changes</name>
  <files>(read-only)</files>
  <read_first>
    <file>AGENTS.md</file>
  </read_first>
  <action>Run `gitnexus_detect_changes()`. Confirm modifications are scoped to `src/components/SiteHead.astro`, `src/layouts/SiteLayout.astro`, the ten Astro pages listed in `files_modified`, `scripts/check-seo-output.js`, and `package.json`. Surface to user if any other files appear (e.g. `src/lib/schema/*` is Wave 3 scope; `src/pages/sitemap.xml.ts` is Wave 4 scope).</action>
  <verify>
    <automated>node -e "process.exit(0)"</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Execution log records `gitnexus_detect_changes()` output.</criterion>
  </acceptance_criteria>
  <done>Wave 2 commit stays in scope.</done>
</task>

</tasks>
