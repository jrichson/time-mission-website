---
phase: 07-seo-schema-local-search-baseline
plan: 05
type: execute
wave: 5
depends_on:
  - 07-01-PLAN.md
  - 07-02-PLAN.md
  - 07-03-PLAN.md
  - 07-04-PLAN.md
files_modified:
  - scripts/check-nap-parity.js
  - docs/geo-answer-first-review.md
  - package.json
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - SEO-05
  - SEO-06
must_haves:
  truths:
    - "For every Astro-rendered open location with localBusinessSchemaEligible === true, the EntertainmentBusiness JSON-LD in dist/{slug}.html exposes streetAddress, addressLocality, postalCode, telephone, email, mapUrl-equivalent, and openingHoursSpecification values byte-equal to the source in data/locations.json (D-09 primary)."
    - "scripts/check-nap-parity.js fails CI if any value diverges (SEO-05)."
    - "docs/geo-answer-first-review.md exists and lists per-page answer-first opportunities + AI-citation gaps (D-13, SEO-06)."
    - "npm run verify:phase7 chains npm run check, npm run build:astro, every Wave 2 to Wave 4 dist-output validator, and exits 0 — pattern matches verify:phase4 and verify:phase6."
    - ".planning/ROADMAP.md Phase 7 plan list and STATE.md progress reflect 5 plans completed."
  artifacts:
    - path: "scripts/check-nap-parity.js"
      provides: "Schema NAP === data/locations.json source assertion (SEO-05 D-09 primary)"
    - path: "docs/geo-answer-first-review.md"
      provides: "GEO/AI answer-first review (D-13)"
    - path: "package.json"
      provides: "verify:phase7 script"
---

<objective>
Close Phase 7 with NAP parity enforcement (D-09 primary guarantee), the D-13 GEO/AI answer-first review document, the executable `npm run verify:phase7` gate (chaining all Wave 2 to Wave 4 dist validators), and roadmap/state bookkeeping. After this plan, Phase 7 is shippable and Phase 8 inherits a known-good SEO contract.
</objective>

<threat_model>
| Threat | Severity | Mitigation in this plan |
|--------|----------|-------------------------|
| Schema NAP drifts from `data/locations.json` after a future location edit (Pitfall 3, SEO-05) | HIGH | `check-nap-parity.js` parses `dist/{slug}.html`, extracts the EntertainmentBusiness node, and asserts every NAP/hours field is byte-equal to the source row; runs in `verify:phase7` and per-wave |
| Phase 7 is "done" without a green gate, leaving Phase 8 cutover to discover gaps | HIGH | `verify:phase7` chains `check` (sources) + `build:astro` + every dist validator from Waves 2–4 + this plan's NAP check; matches existing `verify:phase{2,3,4,5,6}` pattern |
| GEO review is silently skipped or treated as scope creep (D-13) | Low | This plan creates the artifact unconditionally with a fixed minimal table — answers the SEO-06 review-doc requirement without expanding scope into mandatory copy rewrites |
| ROADMAP/STATE drift after execution makes future planners think Phase 7 is incomplete | Low | Tasks 4 and 5 update both files using the same pattern as `03-04-PLAN.md` Task 4 |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-seo-schema-local-search-baseline/07-CONTEXT.md
@.planning/phases/07-seo-schema-local-search-baseline/07-RESEARCH.md
@.planning/phases/07-seo-schema-local-search-baseline/07-VALIDATION.md
@.planning/phases/07-seo-schema-local-search-baseline/07-01-PLAN.md
@.planning/phases/07-seo-schema-local-search-baseline/07-02-PLAN.md
@.planning/phases/07-seo-schema-local-search-baseline/07-03-PLAN.md
@.planning/phases/07-seo-schema-local-search-baseline/07-04-PLAN.md
@.planning/research/PITFALLS.md
@AGENTS.md
@CLAUDE.md
@data/locations.json
@src/data/locations.ts
@src/lib/schema/localBusiness.ts
@scripts/check-location-routes-alignment.js
@scripts/check-route-contract.js
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: GitNexus impact analysis on package.json + locations</name>
  <files>(read-only)</files>
  <read_first>
    <file>AGENTS.md</file>
  </read_first>
  <action>Run `gitnexus_impact({target: "verify:phase6", direction: "upstream"})`, `gitnexus_impact({target: "data/locations.json", direction: "upstream"})`, and `gitnexus_query({query: "NAP parity address phone schema"})`. Record results. STOP if HIGH or CRITICAL.</action>
  <verify>
    <automated>node -e "process.exit(0)"</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Execution log records all calls.</criterion>
  </acceptance_criteria>
  <done>Wave 5 blast radius acknowledged.</done>
</task>

<task type="auto">
  <name>Task 2: Create scripts/check-nap-parity.js (SEO-05 D-09 primary)</name>
  <files>scripts/check-nap-parity.js, package.json</files>
  <read_first>
    <file>data/locations.json</file>
    <file>src/lib/schema/localBusiness.ts</file>
    <file>src/data/routes.json</file>
    <file>scripts/sync-static-to-public.mjs</file>
    <file>scripts/check-location-routes-alignment.js</file>
  </read_first>
  <action>Create `scripts/check-nap-parity.js` (CommonJS, error-accumulation). Script MUST:

1. Read `data/locations.json` and `src/data/routes.json`.
2. Define the Astro-rendered set of location pages by intersecting `ASTRO_RENDERED_OUTPUT_FILES` from `scripts/sync-static-to-public.mjs` with the `philadelphia.html` and `houston.html` entries (currently the only Astro-rendered location pages — duplicate the literal set with comment "keep in sync"). Include any future Astro-rendered location automatically by comparing `routes.json` `outputFile` to the rendered set.
3. For each Astro-rendered location:
   a. Read `dist/{outputFile}`.
   b. Extract every `<script type="application/ld+json"...>...</script>` block; JSON.parse each; find the node whose `@type === 'EntertainmentBusiness'` (if any).
   c. Look up the matching row in `data/locations.json` by canonicalPath ↔ slug.
   d. **For open + eligible locations** (e.g. philadelphia): assert the EntertainmentBusiness node EXISTS and:
      - `name` === source `name`
      - `url` === `https://timemission.com${canonicalPath}`
      - `address.streetAddress` === `[loc.address.line1, loc.address.line2].filter(Boolean).join(', ')`
      - `address.addressLocality` === source `address.city`
      - `address.postalCode` === source `address.zip`
      - `address.addressCountry` === `loc.countryCode ?? loc.address.country`
      - `address.addressRegion` === source `address.state` (when non-empty)
      - `telephone` === `loc.phoneE164 ?? loc.contact.phone`
      - `email` === source `contact.email`
      - `openingHoursSpecification` length === number of `loc.hours` entries with both `open` and `close` strings
      - For each emitted hour entry, `opens`/`closes` strings byte-equal the source `loc.hours[day].open`/`.close` AND `dayOfWeek` matches the documented `dayOfWeekMap`.
   e. **For coming-soon locations** (e.g. houston): assert the EntertainmentBusiness node DOES NOT EXIST in any JSON-LD block on the page (already covered by `check-schema-output.js`, but re-assert here to keep `check-nap-parity` self-contained).
4. Print `NAP parity check passed for ${count} Astro-rendered locations.` on success.
5. Add npm script `"check:nap-parity": "node scripts/check-nap-parity.js"`. Do NOT add to root `npm run check` (requires `dist/`).</action>
  <verify>
    <automated>npm run build:astro &amp;&amp; npm run check:nap-parity</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`scripts/check-nap-parity.js` exists.</criterion>
    <criterion>`package.json` includes `"check:nap-parity"`.</criterion>
    <criterion>`npm run check:nap-parity` exits 0 against current build.</criterion>
    <criterion>Manual smoke (executor logs in journal): temporarily change philadelphia phone in `data/locations.json` to a fake number, rebuild, run check — fails with `philadelphia: telephone mismatch ...`. Revert.</criterion>
  </acceptance_criteria>
  <done>NAP parity is enforced byte-equal between source and dist (SEO-05 D-09 primary).</done>
</task>

<task type="auto">
  <name>Task 3: Create docs/geo-answer-first-review.md (D-13)</name>
  <files>docs/geo-answer-first-review.md</files>
  <read_first>
    <file>.planning/phases/07-seo-schema-local-search-baseline/07-RESEARCH.md</file>
    <file>src/data/routes.json</file>
    <file>src/data/site/seo-routes.json</file>
    <file>src/data/site/faqs.json</file>
    <file>data/locations.json</file>
  </read_first>
  <action>Create `docs/geo-answer-first-review.md` per 07-RESEARCH § "GEO answer-first review document (D-13)". The doc is an internal review artifact — NOT a mandatory rewrite list (per D-13 explicit scope guard).

Required sections:

1. **Header:** "GEO/AI answer-first review (Phase 7 D-13)" + last-updated date + scope statement: "This document is a baseline review per D-13. It does not commit Phase 7 to content rewrites; it is input for a future content phase or marketing planning."

2. **Methodology:** brief explanation that the executor read each Astro-rendered page's first H1/H2 + lead paragraph to assess whether the page answers a likely user question in the first 50 words.

3. **Per-page review table** with columns:
   `Route | Page intent | Answer-first lead present? (Y/N/Partial) | Question-shaped headings? (Y/N) | FAQ schema source | Recommendation | Status`
   - One row per Astro-rendered route (`/`, `/about`, `/faq`, `/contact`, `/locations`, `/privacy`, `/groups/corporate`, `/philadelphia`, `/houston`).
   - One row per high-priority legacy-HTML route (open locations: `/mount-prospect`, `/west-nyack`, `/lincoln`, `/manassas`, `/antwerp`).
   - Recommendation values from the controlled list: `rewrite`, `add FAQ`, `no action`.
   - Status starts as `pending`.

4. **AI citation gaps:** short bulleted list of locations without `faqs[]` populated in `data/locations.json` (currently every location — none have validated faqs[]); recommendation = "consider populating `faqs[]` per location after Phase 7 cutover so per-location FAQPage emits per D-08".

5. **Out-of-scope statement (D-13):** "Phase 7 explicitly does not require any content rewrites listed above. Items marked `rewrite` or `add FAQ` are routed to backlog or a future Phase 8+ content review."

6. **Cross-reference:** link `docs/schema-coverage-matrix.md` (created in Plan 07-03 Task 9) for related Phase 8 input.</action>
  <verify>
    <automated>test -f docs/geo-answer-first-review.md &amp;&amp; rg -n "Phase 7 D-13" docs/geo-answer-first-review.md &amp;&amp; rg -n "philadelphia|houston|mount-prospect" docs/geo-answer-first-review.md</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`docs/geo-answer-first-review.md` exists.</criterion>
    <criterion>Document contains rows for every Astro-rendered route plus the listed legacy locations.</criterion>
    <criterion>Document contains explicit "out-of-scope" statement referencing D-13.</criterion>
    <criterion>Document contains cross-reference to `docs/schema-coverage-matrix.md`.</criterion>
  </acceptance_criteria>
  <done>D-13 GEO/AI review artifact exists without scope creep into mandatory rewrites.</done>
</task>

<task type="auto">
  <name>Task 4: Add npm run verify:phase7 gate (chains all dist validators)</name>
  <files>package.json</files>
  <read_first>
    <file>package.json</file>
  </read_first>
  <action>Add the following npm script to `package.json` `scripts`, immediately after `verify:phase6`:

```json
"verify:phase7": "npm run check && npm run build:astro && npm run check:routes -- --dist && npm run check:astro-dist && npm run check:ticket-panel-parity && npm run check:ticket-panel-source-parity && npm run check:seo-output && npm run check:schema-output && npm run check:sitemap-output && npm run check:robots-ai && npm run check:llms-txt && npm run check:nap-parity && npm run test:smoke"
```

Order rationale (matches `verify:phase4`/`verify:phase6` pattern + Wave 2–5 additions):
1. `npm run check` — source-side validators (includes Wave 1 `check:seo-catalog`, `check:seo-robots`).
2. `npm run build:astro` — produces `dist/` artifacts that downstream checks consume.
3. `check:routes -- --dist` — Phase 2 dist contract still holds.
4. `check:astro-dist` — Phase 1 dist manifest still holds.
5. `check:ticket-panel-parity` + `check:ticket-panel-source-parity` — Phase 4 component parity still holds.
6. `check:seo-output` — Wave 2 (rendered meta).
7. `check:schema-output` — Wave 3 (JSON-LD shape, open vs coming-soon).
8. `check:sitemap-output` — Wave 4 (dist sitemap parity).
9. `check:robots-ai` + `check:llms-txt` — Wave 4 (AI bot rules + llms.txt allowlist).
10. `check:nap-parity` — Wave 5 (schema NAP === source).
11. `test:smoke` — Phase 5/6 Playwright suite (last because slowest).

Do NOT modify any earlier `verify:phase*` script.</action>
  <verify>
    <automated>npm run verify:phase7</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg -n "verify:phase7" package.json` matches once.</criterion>
    <criterion>`verify:phase7` literal contains all 11 substep substrings listed above.</criterion>
    <criterion>`npm run verify:phase7` exits 0 after Waves 1–4 are executed and committed.</criterion>
    <criterion>Earlier `verify:phase{1..6}` scripts are unchanged (executor diffs `package.json` to confirm).</criterion>
  </acceptance_criteria>
  <done>Phase 7 has an executable end-to-end gate that consumes built output (per VALIDATION.md "Full suite command").</done>
</task>

<task type="auto">
  <name>Task 5: Update ROADMAP.md Phase 7 plan list and STATE.md progress</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    <file>.planning/ROADMAP.md</file>
    <file>.planning/STATE.md</file>
    <file>.planning/phases/03-validated-data-foundation/03-04-PLAN.md</file>
  </read_first>
  <action>**ROADMAP.md** — replace the Phase 7 section's `**Plans**: TBD` line with:

```
**Plans**: 5 plans
Plans:
- [ ] 07-01-PLAN.md — SEO catalog scaffolding (seo-routes/seo-robots/seo-organization JSON, src/lib/seo helpers, check-seo-catalog/check-seo-robots) (SEO-01)
- [ ] 07-02-PLAN.md — SiteHead refactor + page meta migration + check-seo-output (SEO-01)
- [ ] 07-03-PLAN.md — JSON-LD helpers (organization/breadcrumb/faqPage/localBusiness/graph) + page ld migration + check-schema-output + docs/schema-coverage-matrix.md (SEO-03, SEO-04)
- [ ] 07-04-PLAN.md — sitemap.xml.ts + llms.txt.ts endpoints + robots.txt AI rules + check-sitemap-output/check-robots-ai/check-llms-txt (SEO-02, SEO-06)
- [ ] 07-05-PLAN.md — check-nap-parity + docs/geo-answer-first-review.md + verify:phase7 gate + ROADMAP/STATE bookkeeping (SEO-05, SEO-06)
Wave summary:
- Wave 1: 07-01 (catalog scaffolding + validators)
- Wave 2: 07-02 (SiteHead refactor + meta migration on Astro pages)
- Wave 3: 07-03 (JSON-LD helpers + ld literal migration; depends on Wave 2 page edits)
- Wave 4: 07-04 (sitemap/robots/llms.txt + dist validators; only depends on Wave 1; parallel-safe with Wave 2/3)
- Wave 5: 07-05 (NAP parity + GEO doc + verify:phase7 gate; depends on Waves 1–4)
```

Also update the Progress table row for Phase 7 to read: `| 7. SEO, Schema & Local Search Baseline | 0/5 | Not started | - |`.

**STATE.md** — update the `progress:` block:
- `total_phases: 8` (unchanged)
- `total_plans: 30` (was 25; +5 for Phase 7)
- `completed_plans: 25` (until executors complete; this PLAN is just bookkeeping)
- `percent: 50` (calculated; executor recomputes)

Also update the `## Performance Metrics` Phase 7 row to `| 7. SEO, Schema & Local Search Baseline | 5 | Not started | N/A |` and bump the `Recent trend:` lines to mention Phase 7 plans landed.

Do NOT mark any plan as completed in this task — execution is owned by `/gsd-execute-phase`. This task only records that PLANS were *created*.</action>
  <verify>
    <automated>rg -n "07-05-PLAN.md" .planning/ROADMAP.md &amp;&amp; rg -n "0/5" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg -n "07-01-PLAN.md" .planning/ROADMAP.md` matches; same for 07-02 through 07-05.</criterion>
    <criterion>`.planning/ROADMAP.md` Phase 7 line is no longer `TBD`.</criterion>
    <criterion>`.planning/STATE.md` `total_plans` is at least 30.</criterion>
    <criterion>`.planning/STATE.md` Phase 7 row in `## Performance Metrics` shows 5 plans.</criterion>
  </acceptance_criteria>
  <done>Roadmap and state reflect the Phase 7 plan inventory.</done>
</task>

<task type="auto">
  <name>Task 6: GitNexus pre-commit detect_changes</name>
  <files>(read-only)</files>
  <read_first>
    <file>AGENTS.md</file>
  </read_first>
  <action>Run `gitnexus_detect_changes()`. Confirm modifications are scoped to `scripts/check-nap-parity.js`, `docs/geo-answer-first-review.md`, `package.json`, `.planning/ROADMAP.md`, `.planning/STATE.md`. Surface to user if any other file appears.</action>
  <verify>
    <automated>node -e "process.exit(0)"</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Execution log records `gitnexus_detect_changes()` output.</criterion>
  </acceptance_criteria>
  <done>Wave 5 commit stays in scope.</done>
</task>

</tasks>
