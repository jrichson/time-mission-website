---
phase: 03-validated-data-foundation
plan: 02
type: execute
wave: 1
depends_on:
  - 03-01-PLAN.md
files_modified:
  - src/data/site/navigation.json
  - src/data/site/footer.json
  - src/data/site/analytics-labels.json
  - scripts/check-site-data.js
  - package.json
autonomous: true
requirements:
  - DATA-03
must_haves:
  truths:
    - "Primary nav, footer columns, and analytics label constants are authored only in validated JSON under src/data/site."
    - "Every internal link target in those files matches a canonicalPath in src/data/routes.json or equals `/`."
  artifacts:
    - path: "src/data/site/navigation.json"
      provides: "Header/mobile nav items with clean hrefs and labels"
    - path: "src/data/site/footer.json"
      provides: "Footer link groups"
    - path: "src/data/site/analytics-labels.json"
      provides: "Stable event name / parameter key strings for future GTM"
    - path: "scripts/check-site-data.js"
      provides: "Structural + route cross-reference validation"
  key_links:
    - from: "src/data/site/navigation.json"
      to: "src/data/routes.json"
      via: "check-site-data.js resolves href → canonicalPath"
      pattern: "canonicalPath"
---

<objective>
Extract durable site chrome data (navigation, footer, analytics label vocabulary) into validated JSON modules consumed later by Astro components, with automated drift checks against the route registry (DATA-03).
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Phishing links in nav/footer JSON | Medium | Only allow `href` values that are `/...` clean paths or documented external allowlist (none in v1 — use `/` relative paths only) |
| PII in analytics labels | Low | Labels are constant strings only — no user content fields |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/03-validated-data-foundation/03-RESEARCH.md
@src/data/routes.json
@index.html
@scripts/check-route-contract.js
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author navigation.json from current homepage nav</name>
  <files>src/data/site/navigation.json</files>
  <read_first>
    <file>index.html</file>
    <file>src/data/routes.json</file>
  </read_first>
  <action>Create directory `src/data/site/` and file `navigation.json` with shape: `{ "primary": [ { "label": string, "href": string, "highlight": optional boolean } ], "groups": [ ... ] }` matching **logical** groupings from `index.html` nav (About, Experiences, Groups dropdown children, Locations, etc.). Every `href` MUST be a clean path starting with `/` and MUST exactly equal some `canonicalPath` from `src/data/routes.json` **or** be `/` or `/groups` with optional `#!` fragment allowed only as `{ "href": "/groups", "hash": "birthday" }` — if fragments are needed, use separate `"hash"` string property; do not put `#` in `href` for validation simplicity. Prefer no hash in v1: link to `/groups/birthdays` etc. where a dedicated route exists. Do not include external URLs in this file.</action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('src/data/site/navigation.json','utf8')); console.log('nav json ok');"</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`src/data/site/navigation.json` exists.</criterion>
    <criterion>At least 8 entries exist across `primary` and `groups` arrays combined.</criterion>
    <criterion>No `href` value contains the substring `.html`.</criterion>
  </acceptance_criteria>
  <done>Navigation data extracted with clean URLs only.</done>
</task>

<task type="auto">
  <name>Task 2: Author footer.json</name>
  <files>src/data/site/footer.json</files>
  <read_first>
    <file>index.html</file>
    <file>src/data/routes.json</file>
  </read_first>
  <action>Create `src/data/site/footer.json` with shape `{ "columns": [ { "title": string, "links": [ { "label": string, "href": string } ] } ] }` reflecting footer link columns from `index.html`. All `href` values: clean internal paths only, routable via `routes.json` or `/`.</action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('src/data/site/footer.json','utf8')); console.log('footer json ok');"</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`src/data/site/footer.json` contains key `"columns"` with array length ≥ 2.</criterion>
    <criterion>No `href` contains `.html`.</criterion>
  </acceptance_criteria>
  <done>Footer links are data-backed.</done>
</task>

<task type="auto">
  <name>Task 3: Analytics labels stub</name>
  <files>src/data/site/analytics-labels.json</files>
  <read_first>
    <file>.planning/REQUIREMENTS.md</file>
  </read_first>
  <action>Create `src/data/site/analytics-labels.json` with keys: `eventNames` (object mapping logical name → SCREAMING_SNAKE string), `parameters` (object mapping logical param → string key for dataLayer). Include at least: `cta_click`, `booking_click`, `gift_card_click`, `location_select`, `ticket_panel_open`, `ticket_panel_close`, `mission_card_click`, `contact_submit`. Values must be alphanumeric with underscores only.</action>
  <verify>
    <automated>node -e "const j=JSON.parse(require('fs').readFileSync('src/data/site/analytics-labels.json','utf8'));if(!j.eventNames.booking_click) throw 0; console.log('labels ok');"</automated>
  </verify>
  <acceptance_criteria>
    <criterion>File defines `eventNames.booking_click` and `parameters` object with ≥ 4 keys.</criterion>
  </acceptance_criteria>
  <done>Tracking vocabulary constants exist for Phase 6.</done>
</task>

<task type="auto">
  <name>Task 4: Implement check-site-data.js and npm script</name>
  <files>scripts/check-site-data.js, package.json</files>
  <read_first>
    <file>scripts/check-location-contracts.js</file>
    <file>src/data/routes.json</file>
  </read_first>
  <action>Create CommonJS `scripts/check-site-data.js` that: (1) Loads `src/data/site/navigation.json`, `footer.json`, `analytics-labels.json` (fail if missing). (2) Loads `src/data/routes.json` and builds a Set of all `canonicalPath` strings plus include `/` if absent. (3) Walks every `href` in nav and footer; each must start with `/` and must be in the Set **or** be a prefix match exception documented in code comments (none allowed). (4) Validate `analytics-labels.json` values match /^[A-Z0-9_]+$/. (5) On success print `Site data check passed.` Add to `package.json` script `"check:site-data": "node scripts/check-site-data.js"` and append `&& npm run check:site-data` to the `check` script **after** `check:routes`.</action>
  <verify>
    <automated>npm run check:site-data</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`package.json` contains `"check:site-data": "node scripts/check-site-data.js"`.</criterion>
    <criterion>`package.json` `check` script invokes `check:site-data` as the last check before closing quote.</criterion>
    <criterion>`npm run check:site-data` exits 0.</criterion>
  </acceptance_criteria>
  <done>Site chrome data is CI-validated against routes.</done>
</task>

</tasks>
