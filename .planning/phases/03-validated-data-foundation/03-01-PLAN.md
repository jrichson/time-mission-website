---
phase: 03-validated-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/locations.json
  - scripts/check-location-contracts.js
  - src/data/locations.ts
  - tsconfig.json
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-04
must_haves:
  truths:
    - "Every location row validates under expanded open vs coming-soon rules without weakening current prod behavior."
    - "Optional international-readiness keys are schema-validated when present."
    - "Astro/TypeScript can import the same location document the static site already publishes."
  artifacts:
    - path: "data/locations.json"
      provides: "Authoritative location facts including optional intl fields"
    - path: "scripts/check-location-contracts.js"
      provides: "DATA-02 enforcement (booking, map, gift, hours, contact, schema flags)"
    - path: "src/data/locations.ts"
      provides: "Astro-consumable re-export of locations (DATA-01)"
  key_links:
    - from: "src/data/locations.ts"
      to: "data/locations.json"
      via: "Single JSON source imported into src for Astro"
      pattern: "locations.json"
---

<objective>
Establish locations as a validated, Astro-importable data module while preserving Today’s runtime `data/locations.json` behavior and tightening open vs coming-soon rules (DATA-02). Add optional international-readiness fields on each location (DATA-04) validated only when present.
</objective>

<threat_model>
| Threat | Severity | Mitigation in this plan |
|--------|----------|-------------------------|
| Open redirect or `javascript:` URLs in booking/map fields | Medium | Reject location URLs that do not start with `https:` except allow `mailto:` only for `giftCardUrl` where already used |
| Schema/NAP drift for coming-soon marketed as open | Low | Validate `localBusinessSchemaEligible` when key present; default inference documented in script comments |
| Twin source of truth | Medium | `data/locations.json` remains authored file; `src/data/locations.ts` imports it — no hand-duplicated arrays |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-validated-data-foundation/03-RESEARCH.md
@.planning/phases/03-validated-data-foundation/03-VALIDATION.md
@.planning/phases/02-route-registry-clean-url-contract/02-CONTEXT.md
@data/locations.json
@scripts/check-location-contracts.js
@js/locations.js
@tsconfig.json
@src/pages/index.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DATA-04 optional fields to every location</name>
  <files>data/locations.json</files>
  <read_first>
    <file>data/locations.json</file>
    <file>.planning/phases/03-validated-data-foundation/03-RESEARCH.md</file>
  </read_first>
  <action>For each object in `data/locations.json` → `locations`, add the following keys with **explicit JSON `null` value** (not omission) so shape is uniform: `countryCode`, `locale`, `timeZone`, `currency`, `phoneE164`, `hreflang`. Use `null` everywhere in this task — do not invent real locale or currency values yet. Add optional boolean `localBusinessSchemaEligible` only where needed: set `true` for every `status: "open"` location and `false` for every `status: "coming-soon"` location. Do not remove or rename any existing keys.</action>
  <verify>
    <automated>node -e "const fs=require('fs');const d=JSON.parse(fs.readFileSync('data/locations.json','utf8'));const required=['countryCode','locale','timeZone','currency','phoneE164','hreflang'];for(const loc of d.locations){for(const k of required){if(!(k in loc)) throw new Error(loc.id+' missing '+k)} if(loc.status==='open'&&loc.localBusinessSchemaEligible!==true) throw new Error(loc.id+' open must be eligible'); if(loc.status==='coming-soon'&&loc.localBusinessSchemaEligible!==false) throw new Error(loc.id+' coming-soon must not be eligible')} console.log('locations intl keys ok');"</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`node -e` smoke script above exits 0.</criterion>
    <criterion>`rg '\"countryCode\"' data/locations.json` matches once per location (N lines ≥ location count).</criterion>
    <criterion>No existing string key names from the first location entry before edit are removed (spot-check `bookingUrl` still present for open locations).</criterion>
  </acceptance_criteria>
  <done>Optional international and schema-eligibility fields are present with null placeholders and correct booleans.</done>
</task>

<task type="auto">
  <name>Task 2: Expand check-location-contracts for DATA-02 and URL safety</name>
  <files>scripts/check-location-contracts.js</files>
  <read_first>
    <file>scripts/check-location-contracts.js</file>
    <file>data/locations.json</file>
  </read_first>
  <action>Extend `scripts/check-location-contracts.js` (same error-accumulation style): (1) When `countryCode`, `locale`, `timeZone`, `currency`, `phoneE164`, or `hreflang` is non-null, require types: string for all except `hreflang` which must be an array of objects with string keys `lang` and `url` (url must start with `https://` or be a clean internal path starting with `/` and not ending in `.html`). (2) Add `assertSafeUrl(field, value, opts)` rejecting `javascript:` and `data:`; allowed URL forms: `https:` URLs, `mailto:` (for email CTAs), and **internal** paths matching `^\/[a-z0-9\-\/]*$` (no query/hash in v1). (3) For `status === 'open'`: require non-empty `mapUrl` passing `assertSafeUrl`; require `contact.phone` and `contact.email` non-empty strings; require `hours` with all seven keys `mon`..`sun` each having string `label`; require `giftCardUrl` non-empty and passing `assertSafeUrl` (so `https:`, `mailto:`, or internal path per current Antwerp mailto pattern); keep existing `bookingUrl` non-empty rule; `rollerCheckoutUrl` if present must match `/^https:\\/\\//`. (4) For `status === 'coming-soon'`: fail if `bookingUrl` is a non-empty string (allow absent or `""`); do **not** require phone, email, hours, map, or gift card — only validate **when present**: `mapUrl`, `giftCardUrl`, `rollerCheckoutUrl` must pass `assertSafeUrl`/`https` rules. (5) If boolean `localBusinessSchemaEligible` exists: require `true` for open and `false` for coming-soon. Run existing id/slug/page file checks unchanged (`${slug}.html` at repo root).</action>
  <verify>
    <automated>npm run check:locations</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`scripts/check-location-contracts.js` contains the literal substring `assertSafeUrl`.</criterion>
    <criterion>`scripts/check-location-contracts.js` contains the literal substring `localBusinessSchemaEligible` OR documents eligibility via `status` checks only (if using implicit rules, file must comment "schema eligibility derived from status").</criterion>
    <criterion>`npm run check:locations` exits 0 after Task 1 data is valid.</criterion>
  </acceptance_criteria>
  <done>Location contract script enforces DATA-02-style completeness for open venues and safe URL schemes.</done>
</task>

<task type="auto">
  <name>Task 3: Astro-consumable locations module</name>
  <files>src/data/locations.ts, tsconfig.json</files>
  <read_first>
    <file>tsconfig.json</file>
    <file>data/locations.json</file>
    <file>src/env.d.ts</file>
  </read_first>
  <action>Create `src/data/locations.ts` that imports the JSON using a relative path `../../data/locations.json` and exports named exports: `locationsDocument` (full parsed object `{ locations }`) and `allLocations` (the array). If `tsconfig.json` lacks `"resolveJsonModule": true`, add it under `compilerOptions` while still extending `astro/tsconfigs/strict`. Add minimal TypeScript interfaces in the same file for `LocationRecord` covering existing keys plus optional intl keys (use `| null` for the six DATA-04 keys). Run `npx astro check` and fix any type errors.</action>
  <verify>
    <automated>npx astro check</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`src/data/locations.ts` exists and contains `export const allLocations`.</criterion>
    <criterion>`src/data/locations.ts` import path includes `../../data/locations.json`.</criterion>
    <criterion>`npx astro check` exits 0.</criterion>
  </acceptance_criteria>
  <done>Astro can import validated location data without a second JSON source.</done>
</task>

</tasks>
