---
phase: 03-validated-data-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01-PLAN.md
  - 03-03-PLAN.md
files_modified:
  - scripts/check-location-routes-alignment.js
  - package.json
  - .planning/ROADMAP.md
  - js/locations.js
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
must_haves:
  truths:
    - "Every location slug with a public page has a matching `/slug` route in routes.json."
    - "npm run verify:phase3 is the phase completion gate."
    - "FALLBACK in js/locations.js is either regenerated from data/locations.json or documented with a blocking check."
  artifacts:
    - path: "scripts/check-location-routes-alignment.js"
      provides: "Slug ↔ route registry alignment"
    - path: "package.json"
      provides: "verify:phase3 script"
---

<objective>
Close Phase 3 with cross-contract alignment between locations and the route registry, a single npm verification gate, roadmap tracking, and an explicit decision path for `js/locations.js` FALLBACK drift (Pitfall 3).
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Missing route for a live location page | High | Alignment script fails CI |
| Stale FALLBACK sends users to wrong checkout | High | Regenerate or add `check:fallback-sync` stub |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@data/locations.json
@src/data/routes.json
@package.json
@js/locations.js
@scripts/check-location-contracts.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Location slug ↔ routes alignment check</name>
  <files>scripts/check-location-routes-alignment.js, package.json</files>
  <read_first>
    <file>data/locations.json</file>
    <file>src/data/routes.json</file>
    <file>package.json</file>
  </read_first>
  <action>Create `scripts/check-location-routes-alignment.js` (CommonJS) that: (1) Reads all `locations` from `data/locations.json`. (2) For each location with `status` in `open | coming-soon`, require a route in `src/data/routes.json` whose `canonicalPath` is exactly `"/" + location.slug` (single leading slash, no trailing slash). (3) Print `Location route alignment passed.` on success. Add npm script `"check:location-routes": "node scripts/check-location-routes-alignment.js"`. Insert `&& npm run check:location-routes` into root `check` after `check:site-data` (or after `check:locations` if site-data not merged yet — order: after all other checks). **Concrete order:** `check:routes` then `check:site-data` then `check:location-routes`.</action>
  <verify>
    <automated>npm run check:location-routes</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`package.json` contains `"check:location-routes"`.</criterion>
    <criterion>`npm run check:location-routes` exits 0 on current repo data.</criterion>
    <criterion>`npm run check` includes `check:location-routes`.</criterion>
  </acceptance_criteria>
  <done>Routes and locations cannot silently diverge.</done>
</task>

<task type="auto">
  <name>Task 2: verify:phase3 gate</name>
  <files>package.json</files>
  <read_first>
    <file>package.json</file>
  </read_first>
  <action>Add script `"verify:phase3": "npm run check && npm run build:astro && npm run check:astro-dist && npm run check:routes -- --dist"` to `package.json` (exact chain — mirrors `verify:phase2` plus any new checks folded into `npm run check`).</action>
  <verify>
    <automated>npm run verify:phase3</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`package.json` contains literal `"verify:phase3"`.</criterion>
    <criterion>`npm run verify:phase3` exits 0 when executed after Plans 03-01–03-03 implementation.</criterion>
  </acceptance_criteria>
  <done>Phase 3 has an executable completion gate.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate FALLBACK or add sync check</name>
  <files>js/locations.js, scripts/check-fallback-locations-sync.js, package.json</files>
  <read_first>
    <file>js/locations.js</file>
    <file>data/locations.json</file>
  </read_first>
  <action>**Choose one path (document which in commit message):** (A) Add Node script `scripts/check-fallback-locations-sync.js` that parses `js/locations.js` for the `FALLBACK` object and compares every **slug** key’s `bookingUrl`, `giftCardUrl`, `mapUrl`, `shortName`, and nested address/contact/hours labels to `data/locations.json` for equality (normalize whitespace). Fail on mismatch. Wire as `npm run check:fallback` and add to `npm run check`. — OR — (B) Regenerate the `FALLBACK` object body from `data/locations.json` automatically (script writes `js/locations.js` partial) and document regeneration command in PLAN SUMMARY. Executor must pick A or B and complete it; A is preferred for lower risk.</action>
  <verify>
    <automated>npm run check:fallback</automated>
  </verify>
  <acceptance_criteria>
    <criterion>If path A: `scripts/check-fallback-locations-sync.js` exists and `package.json` includes `check:fallback` inside `check`.</criterion>
    <criterion>If path B: `package.json` includes `"generate:fallback"` or similar script and `npm run check` documents dependency in comment — still must run `npm run verify:phase3` successfully.</criterion>
    <criterion>`npm run verify:phase3` exits 0 after this task.</criterion>
  </acceptance_criteria>
  <done>FALLBACK drift is blocked or eliminated.</done>
</task>

<task type="auto">
  <name>Task 4: ROADMAP plan index</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    <file>.planning/ROADMAP.md</file>
  </read_first>
  <action>Update `.planning/ROADMAP.md` Phase 3 section: set **Plans:** to `4` and list checkboxes for `03-01-PLAN.md` through `03-04-PLAN.md` with one-line descriptions. Add wave summary (Wave 1: 03-01, 03-02; Wave 2: 03-03, 03-04). Update Progress table plan counts for Phase 3 if present.</action>
  <verify>
    <automated>grep -q '03-04-PLAN.md' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`.planning/ROADMAP.md` contains the substring `03-04-PLAN.md`.</criterion>
    <criterion>Phase 3 **Plans** line is not `TBD`.</criterion>
  </acceptance_criteria>
  <done>Roadmap reflects planned execution.</done>
</task>

</tasks>
