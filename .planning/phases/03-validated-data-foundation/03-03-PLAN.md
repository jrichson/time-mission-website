---
phase: 03-validated-data-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 03-02-PLAN.md
files_modified:
  - src/data/site/groups.json
  - src/data/site/missions.json
  - src/data/site/faqs.json
  - src/data/site/seo-defaults.json
  - scripts/check-site-data.js
autonomous: true
requirements:
  - DATA-03
must_haves:
  truths:
    - "Group/event summaries, mission cards, shared FAQ entries, and default page SEO tokens are data-backed."
    - "check-site-data validates new files without weakening href/route rules."
  artifacts:
    - path: "src/data/site/groups.json"
      provides: "Group landing summaries for nav/sections"
    - path: "src/data/site/missions.json"
      provides: "Mission card titles/descriptions/imagery keys"
    - path: "src/data/site/faqs.json"
      provides: "Reusable FAQ Q/A lists keyed by section"
    - path: "src/data/site/seo-defaults.json"
      provides: "Default title/description templates with placeholders"
---

<objective>
Add validated marketing content catalogs (groups, missions, FAQs, SEO defaults) so Phase 4 templates can render from data instead of duplicating literals (DATA-03 continuation).
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| XSS via FAQ answers stored as HTML | Medium | Store plain text only in JSON; HTML escaping remains template responsibility in Phase 4 |
| Wrong CTA links in group entries | Low | Optional `ctaHref` fields must pass same internal-path rules as nav when present |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@missions.html
@groups.html
@faq.html
@src/data/site/navigation.json
@scripts/check-site-data.js
@src/data/routes.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: groups.json catalog</name>
  <files>src/data/site/groups.json</files>
  <read_first>
    <file>groups.html</file>
    <file>src/data/routes.json</file>
  </read_first>
  <action>Create `src/data/site/groups.json` with array `items`, each `{ "id": string, "title": string, "blurb": string, "canonicalPath": string, "badge": optional string }` with one entry per **existing** group route under `src/data/routes.json` where `canonicalPath` starts with `/groups/` and is not `/groups` itself (at minimum: bachelor-ette, birthdays, corporate, field-trips, holidays, private-events). `canonicalPath` must exactly match a route entry. Blurbs are short plain text (≤ 400 chars).</action>
  <verify>
    <automated>node -e "const j=JSON.parse(require('fs').readFileSync('src/data/site/groups.json','utf8'));if(j.items.length<6)throw 0;console.log('groups ok');"</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`src/data/site/groups.json` exists with ≥ 6 items.</criterion>
    <criterion>Every `canonicalPath` appears in `src/data/routes.json` as some route’s `canonicalPath` (verify with `node -e` one-liner comparing sets).</criterion>
  </acceptance_criteria>
  <done>Group cards can be driven from JSON.</done>
</task>

<task type="auto">
  <name>Task 2: missions.json catalog</name>
  <files>src/data/site/missions.json</files>
  <read_first>
    <file>missions.html</file>
  </read_first>
  <action>Create `src/data/site/missions.json` with `{ "missions": [ { "id": string, "title": string, "tagline": string, "order": number } ] }`. Populate at least 4 missions by extracting titles/taglines from visible mission cards/sections in `missions.html` (do not invent new IP). Use lower-kebab `id` values.</action>
  <verify>
    <automated>node -e "const j=JSON.parse(require('fs').readFileSync('src/data/site/missions.json','utf8'));if(j.missions.length<4)throw 0;console.log('missions ok');"</automated>
  </verify>
  <acceptance_criteria>
    <criterion>≥ 4 missions with unique `id`.</criterion>
    <criterion>Every `title` is a non-empty string ≤ 120 characters.</criterion>
  </acceptance_criteria>
  <done>Mission list is data-backed.</done>
</task>

<task type="auto">
  <name>Task 3: faqs.json shared snippets</name>
  <files>src/data/site/faqs.json</files>
  <read_first>
    <file>faq.html</file>
  </read_first>
  <action>Create `src/data/site/faqs.json` with `{ "sections": [ { "id": string, "heading": string, "items": [ { "q": string, "a": string } ] } ] }`. Include at least 2 sections and 8 total Q&As copied verbatim (plain text) from `faq.html` primary content — no HTML tags inside strings.</action>
  <verify>
    <automated>node -e "const j=JSON.parse(require('fs').readFileSync('src/data/site/faqs.json','utf8'));const n=j.sections.reduce((s,x)=>s+x.items.length,0);if(n<8)throw 0;console.log('faqs ok');"</automated>
  </verify>
  <acceptance_criteria>
    <criterion>≥ 8 FAQ items total.</criterion>
    <criterion>No answer string contains `<` or `>` characters.</criterion>
  </acceptance_criteria>
  <done>FAQ content exists as structured data.</done>
</task>

<task type="auto">
  <name>Task 4: seo-defaults.json</name>
  <files>src/data/site/seo-defaults.json</files>
  <read_first>
    <file>.planning/PROJECT.md</file>
    <file>index.html</file>
  </read_first>
  <action>Create `src/data/site/seo-defaults.json` with `{ "siteName": "Time Mission", "defaultTitleTemplate": string including `| {{siteName}}` style suffix, "defaultDescription": string, "ogType": "website", "twitterCard": "summary_large_image", "locale": "en_US" }`. Templates use `{{placeholder}}` syntax for Phase 7 consumers. `defaultDescription` may duplicate homepage meta description text from `index.html`.</action>
  <verify>
    <automated>node -e "const j=JSON.parse(require('fs').readFileSync('src/data/site/seo-defaults.json','utf8'));if(!j.defaultTitleTemplate.includes('{{')) throw 0;console.log('seo defaults ok');"</automated>
  </verify>
  <acceptance_criteria>
    <criterion>File includes `siteName`, `defaultTitleTemplate`, `defaultDescription`.</criterion>
    <criterion>`defaultTitleTemplate` contains substring `{{`.</criterion>
  </acceptance_criteria>
  <done>SEO tokens centralized.</done>
</task>

<task type="auto">
  <name>Task 5: Extend check-site-data for new files</name>
  <files>scripts/check-site-data.js</files>
  <read_first>
    <file>scripts/check-site-data.js</file>
    <file>src/data/routes.json</file>
  </read_first>
  <action>Extend `scripts/check-site-data.js` to load and structurally validate: `groups.json` (required fields), `missions.json`, `faqs.json`, `seo-defaults.json`. For each item in `groups.json` `items`, require `canonicalPath` ∈ routes Set. Reject if any text field in faqs contains `<`. Print the same success line on completion.</action>
  <verify>
    <automated>npm run check:site-data</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`npm run check:site-data` exits 0.</criterion>
    <criterion>Script references literal filename `groups.json` in source.</criterion>
  </acceptance_criteria>
  <done>Validators cover full site data module set.</done>
</task>

</tasks>
