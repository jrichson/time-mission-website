---
phase: 02-route-registry-clean-url-contract
plan: 05
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - groups/bachelor-ette.html
  - groups/birthdays.html
  - groups/corporate.html
  - groups/field-trips.html
  - groups/holidays.html
  - groups/private-events.html
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-03
  - ROUTE-04
must_haves:
  truths:
    - "Group pages use clean route metadata and first-party links."
    - "Group page visible presentation and interactions remain unchanged."
  artifacts:
    - path: "groups/corporate.html"
      provides: "Representative group page clean URLs"
    - path: "groups/birthdays.html"
      provides: "Representative group page clean URLs"
  key_links:
    - from: "group HTML files"
      to: "src/data/routes.json"
      via: "canonical, og:url, JSON-LD URL fields, and href targets"
      pattern: "npm run check:routes -- --sources --scope=groups"
---

<objective>
Clean group-page URL surfaces without changing nav, ticket panel, location picker, Roller, or FAQ interactions.

Purpose: ROUTE-03 includes group landing page navigation links, analytics/page-path contexts, and metadata, not just redirects and sitemap entries.
Output: Clean group page URL metadata and first-party links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-route-registry-clean-url-contract/02-CONTEXT.md
@.planning/phases/02-route-registry-clean-url-contract/02-RESEARCH.md
@.planning/phases/02-route-registry-clean-url-contract/02-VALIDATION.md
@.planning/phases/02-route-registry-clean-url-contract/02-UI-SPEC.md
@src/data/routes.json
@scripts/check-route-contract.js
@groups/bachelor-ette.html
@groups/birthdays.html
@groups/corporate.html
@groups/field-trips.html
@groups/holidays.html
@groups/private-events.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean group page URL surfaces</name>
  <files>groups/bachelor-ette.html, groups/birthdays.html, groups/corporate.html, groups/field-trips.html, groups/holidays.html, groups/private-events.html</files>
  <read_first>
    <file>.planning/phases/02-route-registry-clean-url-contract/02-UI-SPEC.md</file>
    <file>src/data/routes.json</file>
    <file>groups/bachelor-ette.html</file>
    <file>groups/birthdays.html</file>
    <file>groups/corporate.html</file>
    <file>groups/field-trips.html</file>
    <file>groups/holidays.html</file>
    <file>groups/private-events.html</file>
  </read_first>
  <action>Update only URL-bearing metadata and first-party hrefs in group pages. Use these canonical URL mappings: `groups/bachelor-ette.html -> https://timemission.com/groups/bachelor-ette`, `groups/birthdays.html -> https://timemission.com/groups/birthdays`, `groups/corporate.html -> https://timemission.com/groups/corporate`, `groups/field-trips.html -> https://timemission.com/groups/field-trips`, `groups/holidays.html -> https://timemission.com/groups/holidays`, and `groups/private-events.html -> https://timemission.com/groups/private-events`. Convert relative first-party page hrefs to clean paths using the existing subdirectory style where appropriate: `../groups.html` becomes `../groups` or `/groups`, `../contact.html` becomes `../contact` or `/contact`, and location links become `/philadelphia`, `/manassas`, etc. Preserve query strings and fragments on the clean path. Do not alter visible event copy, CTA labels, CSS classes, IDs, ARIA hooks, `data-*` attributes, form markup, script includes, style blocks, or DOM structure.</action>
  <verify>
    <automated>npm run check:routes -- --sources --scope=groups</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg 'https://timemission\\.com/[^"<> ]+\\.html' groups/bachelor-ette.html groups/birthdays.html groups/corporate.html groups/field-trips.html groups/holidays.html groups/private-events.html` returns no matches.</criterion>
    <criterion>`rg 'href=["'\"''][^"'\"'']*\\.html' groups/bachelor-ette.html groups/birthdays.html groups/corporate.html groups/field-trips.html groups/holidays.html groups/private-events.html` returns no matches.</criterion>
    <criterion>`groups/corporate.html` contains `https://timemission.com/groups/corporate`.</criterion>
    <criterion>`npm run check:routes -- --sources --scope=groups` exits 0.</criterion>
  </acceptance_criteria>
  <done>Group page metadata and first-party links use clean paths and pass scoped route validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| group HTML -> browser navigation | Authored href values become user navigation paths. |
| group HTML -> crawlers/social parsers | Canonical, Open Graph, and JSON-LD URL fields are consumed by external systems. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | group href and metadata URL changes | mitigate | Only use internal clean paths from `src/data/routes.json`; leave external URLs unchanged. |
| T-02-02 | Denial of Service | group query/fragment link rewrites | mitigate | Preserve query strings and fragments on clean paths so no client-side routing loops are introduced. |
| T-02-03 | Tampering | group canonical, `og:url`, JSON-LD URL fields | mitigate | Run `npm run check:routes -- --sources --scope=groups` to compare URL surfaces to the registry. |
| T-02-04 | Tampering | group page source sweep | mitigate | Restrict edits to URL-bearing attributes/metadata and preserve visible copy, classes, IDs, ARIA hooks, script hooks, style blocks, and DOM structure. |
</threat_model>

<verification>
Run `npm run check:routes -- --sources --scope=groups`.
</verification>

<success_criteria>
- Group pages have no first-party `.html` hrefs or canonical URL metadata.
- Group page visible copy, styles, IDs, ARIA hooks, data hooks, and DOM/event behavior remain unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/02-route-registry-clean-url-contract/02-05-SUMMARY.md`.
</output>
