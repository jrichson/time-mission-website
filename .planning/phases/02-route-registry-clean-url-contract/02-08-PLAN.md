---
phase: 02-route-registry-clean-url-contract
plan: 08
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-03
files_modified:
  - js/nav.js
  - js/ticket-panel.js
  - js/roller-checkout.js
  - js/locations.js
  - tests/smoke/site.spec.js
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-03
  - ROUTE-04
must_haves:
  truths:
    - "Runtime route helpers emit clean paths while preserving query-driven booking behavior."
    - "Legacy `.html` href parsing still works during migration where runtime code must tolerate it."
    - "Smoke tests assert the clean URL contract for existing critical flows where the local server can support it."
  artifacts:
    - path: "js/ticket-panel.js"
      provides: "Clean booking fallback paths"
    - path: "js/locations.js"
      provides: "Clean selected-location home paths"
    - path: "tests/smoke/site.spec.js"
      provides: "Updated clean-path expectations for route-sensitive flows"
  key_links:
    - from: "js/ticket-panel.js"
      to: "location pages"
      via: "getLocationPage returns clean route path with preserved ?book=1"
      pattern: "getLocationPage"
    - from: "js/roller-checkout.js"
      to: "booking buttons"
      via: "href slug extraction accepts clean paths and legacy .html during transition"
      pattern: "resolveLocationForButton"
---

<objective>
Clean runtime route helpers and route-sensitive smoke expectations without changing nav, ticket panel, location picker, Roller, or FAQ interactions.

Purpose: This split keeps runtime JavaScript and tests separate from group-page HTML, addressing the original Plan 05 scope warning while preserving ROUTE-03 coverage for generated route strings and analytics/page-path contexts.
Output: Clean JS-generated page paths and smoke-test expectations aligned with clean URLs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-route-registry-clean-url-contract/02-CONTEXT.md
@.planning/phases/02-route-registry-clean-url-contract/02-RESEARCH.md
@.planning/phases/02-route-registry-clean-url-contract/02-VALIDATION.md
@.planning/phases/02-route-registry-clean-url-contract/02-UI-SPEC.md
@.planning/phases/02-route-registry-clean-url-contract/02-03-SUMMARY.md
@src/data/routes.json
@scripts/check-route-contract.js
@src/pages/index.astro
@js/nav.js
@js/ticket-panel.js
@js/roller-checkout.js
@js/locations.js
@tests/smoke/site.spec.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean runtime route helpers</name>
  <files>js/nav.js, js/ticket-panel.js, js/roller-checkout.js, js/locations.js</files>
  <read_first>
    <file>CLAUDE.md</file>
    <file>.planning/phases/02-route-registry-clean-url-contract/02-RESEARCH.md</file>
    <file>.planning/phases/02-route-registry-clean-url-contract/02-UI-SPEC.md</file>
    <file>src/data/routes.json</file>
    <file>js/nav.js</file>
    <file>js/ticket-panel.js</file>
    <file>js/roller-checkout.js</file>
    <file>js/locations.js</file>
  </read_first>
  <action>Before modifying existing JS functions, run GitNexus impact analysis with `direction: "upstream"` for the symbols you change. At minimum, analyze `getLocationPage` and `syncBookingBtn` in `js/ticket-panel.js`, `resolveLocationForButton` in `js/roller-checkout.js`, and `updateDOM` or `isIndexPage` in `js/locations.js` if edited. If GitNexus reports HIGH or CRITICAL risk, stop and report before editing. Then update runtime route strings to emit clean page paths: `getLocationPage(id)` in `js/ticket-panel.js` should return `/${slug}` or `${slug}` consistently with current relative navigation needs, but must no longer append `.html`; booking query behavior must still produce clean `?book=1` URLs such as `/manassas?book=1`. Update `js/roller-checkout.js` slug extraction so it accepts both clean paths like `/philadelphia?book=1` and legacy paths like `philadelphia.html?book=1` during transition. Update `js/locations.js` selected-location home path generation from `loc.slug + '.html'` to the clean equivalent while preserving subdirectory relative behavior (`../philadelphia` from groups/locations pages when the existing code requires relative paths). Update `js/nav.js` only where it stores or emits first-party `.html` page paths; do not change location labels, localStorage keys, menu behavior, overlay behavior, or accessibility attributes. Preserve all public globals (`window.TM`, `window.TMTicketPanel`) and event behavior.</action>
  <verify>
    <automated>npm run check:routes -- --sources --scope=shared</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg '\\.html' js/nav.js js/ticket-panel.js js/roller-checkout.js js/locations.js` returns no first-party route-generation matches; any remaining `.html` mention must be an explicit legacy parser fallback comment or regex for backward compatibility.</criterion>
    <criterion>`js/ticket-panel.js` does not contain `+ '.html'`.</criterion>
    <criterion>`js/locations.js` does not contain `loc.slug + '.html'`.</criterion>
    <criterion>`js/roller-checkout.js` accepts a clean slug pattern in addition to the legacy `.html` regex.</criterion>
    <criterion>`npm run check:routes -- --sources --scope=shared` exits 0.</criterion>
  </acceptance_criteria>
  <done>Runtime helpers emit clean route paths while tolerating legacy hrefs needed during migration.</done>
</task>

<task type="auto">
  <name>Task 2: Align route-sensitive smoke expectations</name>
  <files>tests/smoke/site.spec.js</files>
  <read_first>
    <file>tests/smoke/site.spec.js</file>
    <file>playwright.config.js</file>
    <file>.planning/phases/02-route-registry-clean-url-contract/02-VALIDATION.md</file>
    <file>.planning/phases/02-route-registry-clean-url-contract/02-UI-SPEC.md</file>
  </read_first>
  <action>Update route-sensitive assertions in `tests/smoke/site.spec.js` to match clean runtime-generated paths where the current local server can still execute the test. Change ticket button href expectations from `orland-park.html?book=1` and `manassas.html?book=1` to clean `orland-park?book=1` or `/orland-park?book=1` and `manassas?book=1` or `/manassas?book=1`, matching the implementation from Task 1. Change waitForURL expectations from `/philadelphia\\.html$/` to a clean-path equivalent that still allows query/hash absence, such as `/\\/philadelphia$/`. Keep `page.goto('/faq.html')` and `page.goto('/contact.html')` unchanged in this plan if `playwright.config.js` still uses `python3 -m http.server`, because Python does not apply Cloudflare clean URL behavior. Do not change visual, accessibility, or form assertions except route strings directly affected by Task 1.</action>
  <verify>
    <automated>npm run check:routes -- --sources --scope=shared</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`tests/smoke/site.spec.js` does not contain `orland-park.html?book=1`.</criterion>
    <criterion>`tests/smoke/site.spec.js` does not contain `manassas.html?book=1`.</criterion>
    <criterion>`tests/smoke/site.spec.js` does not contain `philadelphia\\.html`.</criterion>
    <criterion>`npm run check:routes -- --sources --scope=shared` exits 0.</criterion>
  </acceptance_criteria>
  <done>Smoke test source expectations assert clean route strings for runtime-generated hrefs without pretending Python proves Cloudflare redirect behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime JS -> browser navigation | JS-generated hrefs and redirects move users between location and booking paths. |
| legacy hrefs -> Roller resolver | `js/roller-checkout.js` extracts location slugs from possibly legacy or clean hrefs. |
| tests -> route contract confidence | Smoke expectations can hide or expose clean URL regressions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | JS-generated route strings | mitigate | Generate only first-party clean paths from location slugs and leave external checkout URLs untouched. |
| T-02-02 | Denial of Service | `?book=1` and selected-location redirects | mitigate | Preserve query-driven behavior and avoid adding client-side redirect loops. |
| T-02-03 | Tampering | runtime paths and test expectations | mitigate | Run scoped route checks for shared runtime files; reserve smoke tests for slow wave/phase verification. |
| T-02-04 | Tampering | runtime/test sweep | mitigate | Preserve visible copy, CSS classes, IDs, ARIA hooks, data hooks, public globals, and DOM/event behavior; only route strings change. |
</threat_model>

<verification>
Run `npm run check:routes -- --sources --scope=shared` for task-level feedback. Treat `npm run test:smoke` as slow wave/phase verification after Plan 08 if additional browser confidence is needed; it should not be the only task-level proof.
</verification>

<success_criteria>
- Runtime helpers emit clean page paths and keep legacy parsing fallback where needed.
- Smoke test source expectations align with clean route strings for ticket/location flows; the slower browser run remains wave/phase verification.
- Public globals and interaction behavior remain unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/02-route-registry-clean-url-contract/02-08-SUMMARY.md`.
</output>
