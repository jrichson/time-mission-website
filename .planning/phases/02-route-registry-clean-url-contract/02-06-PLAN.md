---
phase: 02-route-registry-clean-url-contract
plan: 06
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - licensing.html
  - privacy.html
  - terms.html
  - code-of-conduct.html
  - cookies.html
  - accessibility.html
  - waiver.html
  - 404.html
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-03
  - ROUTE-04
must_haves:
  truths:
    - "Legal and utility pages point first-party navigation and metadata at clean canonical paths."
    - "Legal, utility, waiver, and 404 visible presentation is unchanged."
    - "Route validation catches any remaining `.html` first-party URL surfaces in this scope."
  artifacts:
    - path: "privacy.html"
      provides: "Representative policy page clean route links and metadata"
    - path: "waiver.html"
      provides: "Waiver page clean route links and metadata"
    - path: "404.html"
      provides: "404 page URL metadata consistency where existing URL metadata is present"
  key_links:
    - from: "legal and utility HTML files"
      to: "src/data/routes.json"
      via: "canonical, og:url, JSON-LD URL fields, and href targets"
      pattern: "npm run check:routes -- --sources --scope=root-legal"
---

<objective>
Sweep legal and utility source pages so canonical tags, Open Graph URLs, JSON-LD URL fields, and first-party navigation hrefs use the clean route contract without changing visible UI.

Purpose: This split keeps the root-page URL sweep below the checker's 15-file blocker threshold while still honoring D-06 and ROUTE-03 for the policy, accessibility, licensing, waiver, cookie, code-of-conduct, and 404 surfaces.
Output: Clean URL-bearing attributes and metadata across legal and utility pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-route-registry-clean-url-contract/02-CONTEXT.md
@.planning/phases/02-route-registry-clean-url-contract/02-RESEARCH.md
@.planning/phases/02-route-registry-clean-url-contract/02-VALIDATION.md
@.planning/phases/02-route-registry-clean-url-contract/02-UI-SPEC.md
@src/data/routes.json
@scripts/check-route-contract.js
@licensing.html
@privacy.html
@terms.html
@code-of-conduct.html
@cookies.html
@accessibility.html
@waiver.html
@404.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean legal and utility metadata URLs</name>
  <files>licensing.html, privacy.html, terms.html, code-of-conduct.html, cookies.html, accessibility.html, waiver.html, 404.html</files>
  <read_first>
    <file>.planning/phases/02-route-registry-clean-url-contract/02-UI-SPEC.md</file>
    <file>src/data/routes.json</file>
    <file>licensing.html</file>
    <file>privacy.html</file>
    <file>terms.html</file>
    <file>code-of-conduct.html</file>
    <file>cookies.html</file>
    <file>accessibility.html</file>
    <file>waiver.html</file>
    <file>404.html</file>
  </read_first>
  <action>Update only URL-bearing metadata in the listed files: `<link rel="canonical">`, Open Graph `og:url`, Twitter URL values if present, JSON-LD `url` and `@id`, and any analytics/page-path literals. Use this exact canonical mapping: `licensing.html -> https://timemission.com/licensing`; `privacy.html -> https://timemission.com/privacy`; `terms.html -> https://timemission.com/terms`; `code-of-conduct.html -> https://timemission.com/code-of-conduct`; `cookies.html -> https://timemission.com/cookies`; `accessibility.html -> https://timemission.com/accessibility`; `waiver.html -> https://timemission.com/waiver`. For `404.html`, do not add a sitemap canonical; if URL metadata exists, point it to `https://timemission.com/404` only if the file already had 404-specific URL metadata. Preserve visible copy, title text, descriptions, CSS classes, IDs, ARIA attributes, `data-*` attributes, script tags, style blocks, and DOM structure per T-02-04 and the UI spec.</action>
  <verify>
    <automated>npm run check:routes -- --sources --scope=root-legal</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg 'https://timemission\\.com/[^"<> ]+\\.html' licensing.html privacy.html terms.html code-of-conduct.html cookies.html accessibility.html waiver.html 404.html` returns no matches.</criterion>
    <criterion>`waiver.html` contains `https://timemission.com/waiver`.</criterion>
    <criterion>`npm run check:routes -- --sources --scope=root-legal` exits 0.</criterion>
  </acceptance_criteria>
  <done>Legal and utility page metadata uses clean canonical URLs and passes scoped route validation with Plan 03.</done>
</task>

<task type="auto">
  <name>Task 2: Clean legal and utility first-party hrefs</name>
  <files>licensing.html, privacy.html, terms.html, code-of-conduct.html, cookies.html, accessibility.html, waiver.html, 404.html</files>
  <read_first>
    <file>.planning/phases/02-route-registry-clean-url-contract/02-UI-SPEC.md</file>
    <file>src/data/routes.json</file>
    <file>scripts/check-route-contract.js</file>
    <file>licensing.html</file>
    <file>privacy.html</file>
    <file>terms.html</file>
    <file>code-of-conduct.html</file>
    <file>cookies.html</file>
    <file>accessibility.html</file>
    <file>waiver.html</file>
    <file>404.html</file>
  </read_first>
  <action>Update first-party navigation `href` values in the listed files from `.html` page URLs to clean paths. Use root-relative clean paths for root pages: `/about`, `/missions`, `/groups`, `/gift-cards`, `/faq`, `/contact`, `/contact-thank-you`, `/licensing`, `/privacy`, `/terms`, `/code-of-conduct`, `/cookies`, `/accessibility`, `/waiver`, `/locations`, and location page paths from `src/data/routes.json`. Preserve query strings and fragments on the clean path. Do not alter external `https:`, `mailto:`, `tel:`, `sms:`, `javascript:`, protocol-relative, `data:`, CSS, JS, image, font, video, or JSON asset URLs. Do not edit visible labels or layout markup.</action>
  <verify>
    <automated>npm run check:routes -- --sources --scope=root-legal</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg 'href=["'\"''][^"'\"'']*\\.html' licensing.html privacy.html terms.html code-of-conduct.html cookies.html accessibility.html waiver.html 404.html` returns no matches.</criterion>
    <criterion>`rg 'href=["'\"'']/privacy(["'\"'']|#|\\?)' licensing.html privacy.html terms.html code-of-conduct.html cookies.html accessibility.html waiver.html 404.html` returns at least one match if those files link to privacy.</criterion>
    <criterion>`npm run check:routes -- --sources --scope=root-legal` exits 0.</criterion>
  </acceptance_criteria>
  <done>Legal and utility page first-party links use clean routes while non-page asset URLs remain untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source HTML -> browser navigation | Authored href values become user navigation paths. |
| source HTML -> crawlers/social parsers | Canonical, Open Graph, and JSON-LD URL fields are consumed by external systems. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | legal and utility href and metadata URL changes | mitigate | Only use internal clean paths from `src/data/routes.json`; leave external URLs unchanged. |
| T-02-02 | Denial of Service | query/fragment link rewrites | mitigate | Preserve query strings and fragments on clean paths so no client-side routing loops are introduced. |
| T-02-03 | Tampering | canonical, `og:url`, JSON-LD URL fields | mitigate | Run `npm run check:routes -- --sources --scope=root` to compare URL surfaces to the registry. |
| T-02-04 | Tampering | legal and utility HTML source sweep | mitigate | Restrict edits to URL-bearing attributes/metadata and preserve visible copy, classes, IDs, ARIA hooks, script hooks, style blocks, and DOM structure. |
</threat_model>

<verification>
Run `npm run check:routes -- --sources --scope=root-legal` after this plan. The combined root scope is covered by the final all-scope gate.
</verification>

<success_criteria>
- Legal and utility metadata no longer references first-party `.html` canonical URLs.
- Legal and utility first-party hrefs use clean root-relative paths.
- UI spec no-change guardrails are respected.
</success_criteria>

<output>
After completion, create `.planning/phases/02-route-registry-clean-url-contract/02-06-SUMMARY.md`.
</output>
