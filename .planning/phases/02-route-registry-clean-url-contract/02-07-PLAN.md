---
phase: 02-route-registry-clean-url-contract
plan: 07
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-04
files_modified:
  - data/locations.json
autonomous: true
requirements:
  - ROUTE-03
  - ROUTE-04
must_haves:
  truths:
    - "Location data does not store first-party `.html` page URLs."
    - "Durable location business facts remain unchanged."
    - "Existing location data validation still passes."
  artifacts:
    - path: "data/locations.json"
      provides: "Clean first-party fallback URLs where location data stores a page path"
  key_links:
    - from: "data/locations.json"
      to: "js/locations.js"
      via: "location URL fields consumed at runtime"
      pattern: "giftCardUrl"
---

<objective>
Clean first-party page URL values in the location data contract without changing venue facts or external checkout/map/contact destinations.

Purpose: This split keeps location HTML and location data changes separate, reducing the warning-level scope in the original Plan 04 while preserving ROUTE-03 and ROUTE-04 coverage.
Output: Clean first-party location data URL values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-route-registry-clean-url-contract/02-CONTEXT.md
@.planning/phases/02-route-registry-clean-url-contract/02-RESEARCH.md
@.planning/phases/02-route-registry-clean-url-contract/02-VALIDATION.md
@.planning/phases/02-route-registry-clean-url-contract/02-UI-SPEC.md
@.planning/phases/02-route-registry-clean-url-contract/02-04-SUMMARY.md
@src/data/routes.json
@scripts/check-route-contract.js
@scripts/check-location-contracts.js
@data/locations.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean first-party location data URLs</name>
  <files>data/locations.json</files>
  <read_first>
    <file>data/locations.json</file>
    <file>src/data/routes.json</file>
    <file>scripts/check-location-contracts.js</file>
    <file>.planning/phases/02-route-registry-clean-url-contract/02-UI-SPEC.md</file>
  </read_first>
  <action>Update first-party page URL values in `data/locations.json` to clean paths without changing durable business facts. Specifically replace Houston's first-party `"giftCardUrl": "houston.html"` with `"giftCardUrl": "/houston"` because Houston has no external gift-card URL and currently falls back to its own page. Do not change external ROLLER, `tickets.timemission.com`, `ecom.roller.app`, Google Maps, `mailto:`, or empty string values. Do not rename location IDs, slugs, names, status values, addresses, hours, booking URLs, Roller checkout URLs, or ticker copy.</action>
  <verify>
    <automated>npm run check:locations && npm run check:routes -- --sources --scope=locations</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`data/locations.json` contains `"giftCardUrl": "/houston"`.</criterion>
    <criterion>`rg '"[^"]+Url": "[^"]*\\.html' data/locations.json` returns no matches.</criterion>
    <criterion>`npm run check:locations && npm run check:routes -- --sources --scope=locations` exits 0 when combined with Plan 04 location HTML changes.</criterion>
  </acceptance_criteria>
  <done>Location data no longer stores first-party `.html` page URLs and existing location contract checks still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| location data -> runtime JS | `data/locations.json` values are consumed by booking, gift-card, footer, and location UI code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | location data URL values | mitigate | Keep external URLs unchanged and only convert first-party page URLs to registry clean paths. |
| T-02-02 | Tampering | location business facts | mitigate | Restrict edits to URL values; do not change IDs, slugs, venue facts, booking URLs, map URLs, or visible copy fields. |
| T-02-03 | Tampering | location data validation | mitigate | Run `npm run check:locations` and scoped route validation after edits. |
</threat_model>

<verification>
Run `npm run check:locations && npm run check:routes -- --sources --scope=locations` after both Plan 04 and Plan 07 have completed.
</verification>

<success_criteria>
- `data/locations.json` has no first-party `.html` URL value.
- Existing location data validation still passes.
- Runtime consumers continue to receive the same location facts except clean first-party fallback paths.
</success_criteria>

<output>
After completion, create `.planning/phases/02-route-registry-clean-url-contract/02-07-SUMMARY.md`.
</output>
