# OS
.DS_Store
*.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo

# Dependencies
node_modules/

# Tools
.firecrawl/

# Secrets / local env (PUBLIC_TM_MEDIA_BASE for R2-hosted hero MP4s)
.env
.env.*
!.env.example

# Archive (dev experiments, not for production)
_archive/

# Internal mockups — design references, never deploy (audit Wave 6-5)
assets/mockup-reference/

# macOS Finder duplicates ("foo 2.html", "sitemap 3.xml") — never commit
*\ [0-9].html
*\ [0-9].js
*\ [0-9].css
*\ [0-9].xml
*\ [0-9].txt
*\ [0-9]/

# Build artifacts
*.log
dist/
public/

# Astro local tooling
.astro/

# Test artifacts
test-results/
.playwright-mcp/

# Full-resolution media backups (kept locally, not in repo)
assets/photos/**/.orig-full/

# Payload / Next (cms workspace)
cms/.next/

.gitnexus
