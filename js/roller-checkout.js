/**
 * Phase 5 BOOK-03 / D-02: Default path does not load Roller iframe checkout.
 *
 * External checkout uses window.TMTicketPanel.getBookingUrl, the ticket-panel resolver,
 * and same-tab navigation (see js/ticket-panel.js).
 *
 * Optional iframe fallback must be approved in a ROADMAP/plan follow-up — do not
 * re-enable a Roller CDN loader here without explicit plan scope.
 */
(function () {
    'use strict';
    // Intentionally empty — capture-phase Roller interception removed (BOOK-03).
})();
