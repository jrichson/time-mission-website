---
import SiteLayout from '../../layouts/SiteLayout.astro';
import { definePage } from '../../lib/define-page';
import fieldTripsMain from '../../partials/field-trips-main.frag.txt?raw';
import faqs from '../../data/site/faqs.json';
import { buildGroupEventGraph, serializeGraph } from '../../lib/schema/graph';

const faqItems = faqs.sections.find((s) => s.id === 'field-trips')?.items ?? [];
const ld = serializeGraph(
    buildGroupEventGraph({
        canonicalPath: '/groups/field-trips',
        crumbs: [
            { label: 'Home', href: '/' },
            { label: 'Groups', href: '/groups' },
            { label: 'Field Trips', href: '/groups/field-trips' },
        ],
        serviceName: 'School Field Trip Hosting',
        serviceType: 'EducationalOrganization',
        serviceDescription:
            'Educational field trips at Time Mission covering physics, logic, and teamwork for student groups of 10-125+, with weekday availability and discounted chaperone rates.',
        faqs: faqItems,
    }),
);
const page = definePage({ canonicalPath: '/groups/field-trips' });
---

<SiteLayout
    canonicalPath={page.canonicalPath}
    bodyClass="has-noise"
>
    <Fragment slot="head-extra">
        <link rel="stylesheet" href="/css/footer.css?v=3" />
        <link rel="stylesheet" href="/css/newsletter.css?v=2" />
        <link rel="stylesheet" href="/css/faq.css?v=3" />
        <link rel="stylesheet" href="/css/ticket-panel.css" />
        <link rel="stylesheet" href="/css/page-group-event.css?v=1" />
    </Fragment>
    <Fragment slot="json-ld">
        <script type="application/ld+json" is:inline set:html={ld}></script>
    </Fragment>
    <div set:html={fieldTripsMain} />
    <Fragment slot="after-site-scripts">
        <script defer src="/js/page-group-event-after.js?v=1"></script>
    </Fragment>
</SiteLayout>
