---
import SiteLayout from '../../layouts/SiteLayout.astro';
import { definePage } from '../../lib/define-page';
import birthdaysMain from '../../partials/birthdays-main.frag.txt?raw';
import faqs from '../../data/site/faqs.json';
import { buildGroupEventGraph, serializeGraph } from '../../lib/schema/graph';

const faqItems = faqs.sections.find((s) => s.id === 'birthdays')?.items ?? [];
const ld = serializeGraph(
    buildGroupEventGraph({
        canonicalPath: '/groups/birthdays',
        crumbs: [
            { label: 'Home', href: '/' },
            { label: 'Groups', href: '/groups' },
            { label: 'Birthday Parties', href: '/groups/birthdays' },
        ],
        serviceName: 'Birthday Party Hosting',
        serviceType: 'PartyService',
        serviceDescription:
            'Birthday party packages at Time Mission with private mission rooms, party hosts, and group scoring across 25+ interactive challenges.',
        faqs: faqItems,
    }),
);
const page = definePage({ canonicalPath: '/groups/birthdays' });
---

<SiteLayout
    canonicalPath={page.canonicalPath}
    bodyClass="has-noise"
>
    <Fragment slot="head-extra">
        <link rel="preload" as="image" href="/assets/photos/TM-Groups-480.webp" imagesrcset="/assets/photos/TM-Groups-480.webp 480w, /assets/photos/TM-Groups-800.webp 800w, /assets/photos/TM-Groups-1200.webp 1200w, /assets/photos/TM-Groups.webp 1920w" imagesizes="100vw" type="image/webp" fetchpriority="high" />
        <link rel="stylesheet" href="/css/faq.css?v=5" />
        <link rel="stylesheet" href="/css/page-group-event.css?v=1" />
    </Fragment>
    <Fragment slot="json-ld">
        <script type="application/ld+json" is:inline set:html={ld}></script>
    </Fragment>
    <div set:html={birthdaysMain} />
    <Fragment slot="after-site-scripts">
        <script defer src="/js/page-group-event-after.js?v=1"></script>
    </Fragment>
</SiteLayout>
